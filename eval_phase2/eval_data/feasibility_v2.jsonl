{"mp_id": "hdfs::DS::run->processDatanodeOrExternalError", "caller": "DataStreamer:run()", "callee": "DataStreamer:processDatanodeOrExternalError()", "gt": "valid", "stratum": "simple", "infeasible_reason": "", "rationale": "run() calls processDatanodeOrExternalError when errorState.hasError() is true after pipeline failure.", "caller_code": "*/\n  @Override\n  public void run() {\n    TraceScope scope = null;\n    while (!streamerClosed && dfsClient.clientRunning) {\n      // if the Responder encountered an error, shutdown Responder\n      if (errorState.hasError()) {\n        closeResponder();\n      }\n\n      DFSPacket one;\n      try {\n        // process datanode IO errors if any\n        boolean doSleep = processDatanodeOrExternalError();\n\n        synchronized (dataQueue) {\n          // wait for a packet to be sent.\n          while ((!shouldStop() && dataQueue.isEmpty()) || doSleep) {\n            long timeout = 1000;\n            if (stage == BlockConstructionStage.DATA_STREAMING) {\n              timeout = sendHeartbeat();\n            }\n            try {\n              dataQueue.wait(timeout);\n            } catch (InterruptedException  e) {\n              LOG.debug(\"Thread interrupted\", e);\n            }\n            doSleep = false;\n          }\n          if (shouldStop()) {\n            continue;\n          }\n          // get packet to be sent.\n          one = dataQueue.getFirst(); // regular data packet\n          SpanContext[] parents = one.getTraceParents();\n          if (parents != null && parents.length > 0) {\n            // The original code stored multiple parents in the DFSPacket, and\n            // use them ALL here when creating a new Span. We only use the\n            // last one FOR NOW. Moreover, we don't activate the Span for now.\n            scope = dfsClient.getTracer().\n                newScope(\"dataStreamer\", parents[0], false);\n            //scope.getSpan().setParents(parents);\n          }\n        }\n\n        // The DataStreamer has to release the dataQueue before sleeping,\n        // otherwise it will cause the ResponseProcessor to accept the ACK delay.\n        try {\n          backOffIfNecessary();\n        } catch (InterruptedException e) {\n          LOG.debug(\"Thread interrupted\", e);\n        }\n\n        // get new block from namenode.\n        LOG.debug(\"stage={}, {}\", stage, this);\n\n        if (stage == BlockConstructionStage.PIPELINE_SETUP_CREATE) {\n          LOG.debug(\"Allocating new block: {}\", this);\n          setPipeline(nextBlockOutputStream());\n          initDataStreaming();\n        } else if (stage == BlockConstructionStage.PIPELINE_SETUP_APPEND) {\n          LOG.debug(\"Append to block {}\", block);", "callee_code": "* @return true if it should sleep for a while after returning.\n   */\n  private boolean processDatanodeOrExternalError() throws IOException {\n    if (!errorState.hasDatanodeError() && !shouldHandleExternalError()) {\n      return false;\n    }\n    LOG.debug(\"start process datanode/external error, {}\", this);\n    if (response != null) {\n      LOG.info(\"Error Recovery for \" + block +\n          \" waiting for responder to exit. \");\n      return true;\n    }\n    closeStream();\n\n    // move packets from ack queue to front of the data queue\n    synchronized (dataQueue) {\n      dataQueue.addAll(0, ackQueue);\n      ackQueue.clear();\n      packetSendTime.clear();\n    }\n\n    // If we had to recover the pipeline five times in a row for the\n    // same packet, this client likely has corrupt data or corrupting\n    // during transmission.\n    if (!errorState.isRestartingNode() && ++pipelineRecoveryCount > 5) {\n      LOG.warn(\"Error recovering pipeline for writing \" +\n          block + \". Already retried 5 times for the same packet.\");\n      lastException.set(new IOException(\"Failing write. Tried pipeline \" +\n          \"recovery 5 times without success.\"));\n      streamerClosed = true;\n      return false;\n    }\n\n    setupPipelineForAppendOrRecovery();\n\n    if (!streamerClosed && dfsClient.clientRunning) {\n      if (stage == BlockConstructionStage.PIPELINE_CLOSE) {\n\n        // If we had an error while closing the pipeline, we go through a fast-path\n        // where the BlockReceiver does not run. Instead, the DataNode just finalizes\n        // the block immediately during the 'connect ack' process. So, we want to pull\n        // the end-of-block packet from the dataQueue, since we don't actually have\n        // a true pipeline to send it over.\n        //\n        // We also need to set lastAckedSeqno to the end-of-block Packet's seqno, so that\n        // a client waiting on close() will be aware that the flush finished.\n        synchronized (dataQueue) {\n          DFSPacket endOfBlockPacket = dataQueue.remove();  // remove the end of block packet\n          // Close any trace span associated with this Packet\n          Span span = endOfBlockPacket.getSpan();\n          if (span != null) {\n            span.finish();\n            endOfBlockPacket.setSpan(null);\n          }\n          assert endOfBlockPacket.isLastPacketInBlock();\n          assert lastAckedSeqno == endOfBlockPacket.getSeqno() - 1;\n          lastAckedSeqno = endOfBlockPacket.getSeqno();\n          pipelineRecoveryCount = 0;\n          dataQueue.notifyAll();\n        }\n        endBlock();\n      } else {", "caller_log_raw_stripped": "[ENTRY] DataStreamer:run()\n  IF: errorState.hasError() == true\n  CALL: processDatanodeOrExternalError()", "callee_log_raw_stripped": "[ENTRY] DataStreamer:processDatanodeOrExternalError()\n  IF: !hasDatanodeError() && !shouldHandleExternalError() -> EXIT\n  [WARN] Error Recovery\n  IF: ++recoveryCount > 5 -> [ERROR] -> throw\n  IF: recoveryCount <= 5 -> endBlock()\n  EXIT", "caller_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:run()\n  IF: errorState.hasError() == true\n  CALL: processDatanodeOrExternalError()\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:run()\n  IF: errorState.hasError() == true\n  CALL: processDatanodeOrExternalError()\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "callee_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:processDatanodeOrExternalError()\n  IF: !hasDatanodeError() && !shouldHandleExternalError() -> EXIT\n  [WARN] Error Recovery\n  IF: ++recoveryCount > 5 -> [ERROR] -> throw\n  IF: recoveryCount <= 5 -> endBlock()\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:processDatanodeOrExternalError()\n  IF: !hasDatanodeError() && !shouldHandleExternalError() -> EXIT\n  [WARN] Error Recovery\n  IF: ++recoveryCount > 5 -> [ERROR] -> throw\n  IF: recoveryCount <= 5 -> endBlock()\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "parent_info": "node name is DataStreamer:run()node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:run()\n  IF: errorState.hasError() == true\n  CALL: processDatanodeOrExternalError()\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:run()\n  IF: errorState.hasError() == true\n  CALL: processDatanodeOrExternalError()\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```souce code:*/\n  @Override\n  public void run() {\n    TraceScope scope = null;\n    while (!streamerClosed && dfsClient.clientRunning) {\n      // if the Responder encountered an error, shutdown Responder\n      if (errorState.hasError()) {\n        closeResponder();\n      }\n\n      DFSPacket one;\n      try {\n        // process datanode IO errors if any\n        boolean doSleep = processDatanodeOrExternalError();\n\n        synchronized (dataQueue) {\n          // wait for a packet to be sent.\n          while ((!shouldStop() && dataQueue.isEmpty()) || doSleep) {\n            long timeout = 1000;\n            if (stage == BlockConstructionStage.DATA_STREAMING) {\n              timeout = sendHeartbeat();\n            }\n            try {\n              dataQueue.wait(timeout);\n            } catch (InterruptedException  e) {\n              LOG.debug(\"Thread interrupted\", e);\n            }\n            doSleep = false;\n          }\n          if (shouldStop()) {\n            continue;\n          }\n          // get packet to be sent.\n          one = dataQueue.getFirst(); // regular data packet\n          SpanContext[] parents = one.getTraceParents();\n          if (parents != null && parents.length > 0) {\n            // The original code stored multiple parents in the DFSPacket, and\n            // use them ALL here when creating a new Span. We only use the\n            // last one FOR NOW. Moreover, we don't activate the Span for now.\n            scope = dfsClient.getTracer().\n                newScope(\"dataStreamer\", parents[0], false);\n            //scope.getSpan().setParents(parents);\n          }\n        }\n\n        // The DataStreamer has to release the dataQueue before sleeping,\n        // otherwise it will cause the ResponseProcessor to accept the ACK delay.\n        try {\n          backOffIfNecessary();\n        } catch (InterruptedException e) {\n          LOG.debug(\"Thread interrupted\", e);\n        }\n\n        // get new block from namenode.\n        LOG.debug(\"stage={}, {}\", stage, this);\n\n        if (stage == BlockConstructionStage.PIPELINE_SETUP_CREATE) {\n          LOG.debug(\"Allocating new block: {}\", this);\n          setPipeline(nextBlockOutputStream());\n          initDataStreaming();\n        } else if (stage == BlockConstructionStage.PIPELINE_SETUP_APPEND) {\n          LOG.debug(\"Append to block {}\", block);", "child_info": "node name isDataStreamer:processDatanodeOrExternalError()node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:processDatanodeOrExternalError()\n  IF: !hasDatanodeError() && !shouldHandleExternalError() -> EXIT\n  [WARN] Error Recovery\n  IF: ++recoveryCount > 5 -> [ERROR] -> throw\n  IF: recoveryCount <= 5 -> endBlock()\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:processDatanodeOrExternalError()\n  IF: !hasDatanodeError() && !shouldHandleExternalError() -> EXIT\n  [WARN] Error Recovery\n  IF: ++recoveryCount > 5 -> [ERROR] -> throw\n  IF: recoveryCount <= 5 -> endBlock()\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```source code:* @return true if it should sleep for a while after returning.\n   */\n  private boolean processDatanodeOrExternalError() throws IOException {\n    if (!errorState.hasDatanodeError() && !shouldHandleExternalError()) {\n      return false;\n    }\n    LOG.debug(\"start process datanode/external error, {}\", this);\n    if (response != null) {\n      LOG.info(\"Error Recovery for \" + block +\n          \" waiting for responder to exit. \");\n      return true;\n    }\n    closeStream();\n\n    // move packets from ack queue to front of the data queue\n    synchronized (dataQueue) {\n      dataQueue.addAll(0, ackQueue);\n      ackQueue.clear();\n      packetSendTime.clear();\n    }\n\n    // If we had to recover the pipeline five times in a row for the\n    // same packet, this client likely has corrupt data or corrupting\n    // during transmission.\n    if (!errorState.isRestartingNode() && ++pipelineRecoveryCount > 5) {\n      LOG.warn(\"Error recovering pipeline for writing \" +\n          block + \". Already retried 5 times for the same packet.\");\n      lastException.set(new IOException(\"Failing write. Tried pipeline \" +\n          \"recovery 5 times without success.\"));\n      streamerClosed = true;\n      return false;\n    }\n\n    setupPipelineForAppendOrRecovery();\n\n    if (!streamerClosed && dfsClient.clientRunning) {\n      if (stage == BlockConstructionStage.PIPELINE_CLOSE) {\n\n        // If we had an error while closing the pipeline, we go through a fast-path\n        // where the BlockReceiver does not run. Instead, the DataNode just finalizes\n        // the block immediately during the 'connect ack' process. So, we want to pull\n        // the end-of-block packet from the dataQueue, since we don't actually have\n        // a true pipeline to send it over.\n        //\n        // We also need to set lastAckedSeqno to the end-of-block Packet's seqno, so that\n        // a client waiting on close() will be aware that the flush finished.\n        synchronized (dataQueue) {\n          DFSPacket endOfBlockPacket = dataQueue.remove();  // remove the end of block packet\n          // Close any trace span associated with this Packet\n          Span span = endOfBlockPacket.getSpan();\n          if (span != null) {\n            span.finish();\n            endOfBlockPacket.setSpan(null);\n          }\n          assert endOfBlockPacket.isLastPacketInBlock();\n          assert lastAckedSeqno == endOfBlockPacket.getSeqno() - 1;\n          lastAckedSeqno = endOfBlockPacket.getSeqno();\n          pipelineRecoveryCount = 0;\n          dataQueue.notifyAll();\n        }\n        endBlock();\n      } else {"}
{"mp_id": "hdfs::DS::run->processDatanodeOrExternalError-INFEASIBLE", "caller": "DataStreamer:run()", "callee": "DataStreamer:processDatanodeOrExternalError()", "gt": "infeasible", "stratum": "simple", "infeasible_reason": "hasError()==false && shouldHandleExternalError()==false — no error condition exists to trigger this call.", "rationale": "", "caller_code": "*/\n  @Override\n  public void run() {\n    TraceScope scope = null;\n    while (!streamerClosed && dfsClient.clientRunning) {\n      // if the Responder encountered an error, shutdown Responder\n      if (errorState.hasError()) {\n        closeResponder();\n      }\n\n      DFSPacket one;\n      try {\n        // process datanode IO errors if any\n        boolean doSleep = processDatanodeOrExternalError();\n\n        synchronized (dataQueue) {\n          // wait for a packet to be sent.\n          while ((!shouldStop() && dataQueue.isEmpty()) || doSleep) {\n            long timeout = 1000;\n            if (stage == BlockConstructionStage.DATA_STREAMING) {\n              timeout = sendHeartbeat();\n            }\n            try {\n              dataQueue.wait(timeout);\n            } catch (InterruptedException  e) {\n              LOG.debug(\"Thread interrupted\", e);\n            }\n            doSleep = false;\n          }\n          if (shouldStop()) {\n            continue;\n          }\n          // get packet to be sent.\n          one = dataQueue.getFirst(); // regular data packet\n          SpanContext[] parents = one.getTraceParents();\n          if (parents != null && parents.length > 0) {\n            // The original code stored multiple parents in the DFSPacket, and\n            // use them ALL here when creating a new Span. We only use the\n            // last one FOR NOW. Moreover, we don't activate the Span for now.\n            scope = dfsClient.getTracer().\n                newScope(\"dataStreamer\", parents[0], false);\n            //scope.getSpan().setParents(parents);\n          }\n        }\n\n        // The DataStreamer has to release the dataQueue before sleeping,\n        // otherwise it will cause the ResponseProcessor to accept the ACK delay.\n        try {\n          backOffIfNecessary();\n        } catch (InterruptedException e) {\n          LOG.debug(\"Thread interrupted\", e);\n        }\n\n        // get new block from namenode.\n        LOG.debug(\"stage={}, {}\", stage, this);\n\n        if (stage == BlockConstructionStage.PIPELINE_SETUP_CREATE) {\n          LOG.debug(\"Allocating new block: {}\", this);\n          setPipeline(nextBlockOutputStream());\n          initDataStreaming();\n        } else if (stage == BlockConstructionStage.PIPELINE_SETUP_APPEND) {\n          LOG.debug(\"Append to block {}\", block);", "callee_code": "* @return true if it should sleep for a while after returning.\n   */\n  private boolean processDatanodeOrExternalError() throws IOException {\n    if (!errorState.hasDatanodeError() && !shouldHandleExternalError()) {\n      return false;\n    }\n    LOG.debug(\"start process datanode/external error, {}\", this);\n    if (response != null) {\n      LOG.info(\"Error Recovery for \" + block +\n          \" waiting for responder to exit. \");\n      return true;\n    }\n    closeStream();\n\n    // move packets from ack queue to front of the data queue\n    synchronized (dataQueue) {\n      dataQueue.addAll(0, ackQueue);\n      ackQueue.clear();\n      packetSendTime.clear();\n    }\n\n    // If we had to recover the pipeline five times in a row for the\n    // same packet, this client likely has corrupt data or corrupting\n    // during transmission.\n    if (!errorState.isRestartingNode() && ++pipelineRecoveryCount > 5) {\n      LOG.warn(\"Error recovering pipeline for writing \" +\n          block + \". Already retried 5 times for the same packet.\");\n      lastException.set(new IOException(\"Failing write. Tried pipeline \" +\n          \"recovery 5 times without success.\"));\n      streamerClosed = true;\n      return false;\n    }\n\n    setupPipelineForAppendOrRecovery();\n\n    if (!streamerClosed && dfsClient.clientRunning) {\n      if (stage == BlockConstructionStage.PIPELINE_CLOSE) {\n\n        // If we had an error while closing the pipeline, we go through a fast-path\n        // where the BlockReceiver does not run. Instead, the DataNode just finalizes\n        // the block immediately during the 'connect ack' process. So, we want to pull\n        // the end-of-block packet from the dataQueue, since we don't actually have\n        // a true pipeline to send it over.\n        //\n        // We also need to set lastAckedSeqno to the end-of-block Packet's seqno, so that\n        // a client waiting on close() will be aware that the flush finished.\n        synchronized (dataQueue) {\n          DFSPacket endOfBlockPacket = dataQueue.remove();  // remove the end of block packet\n          // Close any trace span associated with this Packet\n          Span span = endOfBlockPacket.getSpan();\n          if (span != null) {\n            span.finish();\n            endOfBlockPacket.setSpan(null);\n          }\n          assert endOfBlockPacket.isLastPacketInBlock();\n          assert lastAckedSeqno == endOfBlockPacket.getSeqno() - 1;\n          lastAckedSeqno = endOfBlockPacket.getSeqno();\n          pipelineRecoveryCount = 0;\n          dataQueue.notifyAll();\n        }\n        endBlock();\n      } else {", "caller_log_raw_stripped": "[ENTRY] DataStreamer:run()\n  IF: errorState.hasError()==False AND shouldHandleExternalError()==False \n  CALL: processDatanodeOrExternalError()", "callee_log_raw_stripped": "[ENTRY] DataStreamer:processDatanodeOrExternalError()\n  IF: !hasDatanodeError() && !shouldHandleExternalError() -> EXIT (no-op)", "caller_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:run()\n  IF: errorState.hasError()==False AND shouldHandleExternalError()==False \n  CALL: processDatanodeOrExternalError()\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:run()\n  IF: errorState.hasError()==False AND shouldHandleExternalError()==False \n  CALL: processDatanodeOrExternalError()\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "callee_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:processDatanodeOrExternalError()\n  IF: !hasDatanodeError() && !shouldHandleExternalError() -> EXIT (no-op)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:processDatanodeOrExternalError()\n  IF: !hasDatanodeError() && !shouldHandleExternalError() -> EXIT (no-op)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "parent_info": "node name is DataStreamer:run()node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:run()\n  IF: errorState.hasError()==False AND shouldHandleExternalError()==False \n  CALL: processDatanodeOrExternalError()\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:run()\n  IF: errorState.hasError()==False AND shouldHandleExternalError()==False \n  CALL: processDatanodeOrExternalError()\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```souce code:*/\n  @Override\n  public void run() {\n    TraceScope scope = null;\n    while (!streamerClosed && dfsClient.clientRunning) {\n      // if the Responder encountered an error, shutdown Responder\n      if (errorState.hasError()) {\n        closeResponder();\n      }\n\n      DFSPacket one;\n      try {\n        // process datanode IO errors if any\n        boolean doSleep = processDatanodeOrExternalError();\n\n        synchronized (dataQueue) {\n          // wait for a packet to be sent.\n          while ((!shouldStop() && dataQueue.isEmpty()) || doSleep) {\n            long timeout = 1000;\n            if (stage == BlockConstructionStage.DATA_STREAMING) {\n              timeout = sendHeartbeat();\n            }\n            try {\n              dataQueue.wait(timeout);\n            } catch (InterruptedException  e) {\n              LOG.debug(\"Thread interrupted\", e);\n            }\n            doSleep = false;\n          }\n          if (shouldStop()) {\n            continue;\n          }\n          // get packet to be sent.\n          one = dataQueue.getFirst(); // regular data packet\n          SpanContext[] parents = one.getTraceParents();\n          if (parents != null && parents.length > 0) {\n            // The original code stored multiple parents in the DFSPacket, and\n            // use them ALL here when creating a new Span. We only use the\n            // last one FOR NOW. Moreover, we don't activate the Span for now.\n            scope = dfsClient.getTracer().\n                newScope(\"dataStreamer\", parents[0], false);\n            //scope.getSpan().setParents(parents);\n          }\n        }\n\n        // The DataStreamer has to release the dataQueue before sleeping,\n        // otherwise it will cause the ResponseProcessor to accept the ACK delay.\n        try {\n          backOffIfNecessary();\n        } catch (InterruptedException e) {\n          LOG.debug(\"Thread interrupted\", e);\n        }\n\n        // get new block from namenode.\n        LOG.debug(\"stage={}, {}\", stage, this);\n\n        if (stage == BlockConstructionStage.PIPELINE_SETUP_CREATE) {\n          LOG.debug(\"Allocating new block: {}\", this);\n          setPipeline(nextBlockOutputStream());\n          initDataStreaming();\n        } else if (stage == BlockConstructionStage.PIPELINE_SETUP_APPEND) {\n          LOG.debug(\"Append to block {}\", block);", "child_info": "node name isDataStreamer:processDatanodeOrExternalError()node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:processDatanodeOrExternalError()\n  IF: !hasDatanodeError() && !shouldHandleExternalError() -> EXIT (no-op)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:processDatanodeOrExternalError()\n  IF: !hasDatanodeError() && !shouldHandleExternalError() -> EXIT (no-op)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```source code:* @return true if it should sleep for a while after returning.\n   */\n  private boolean processDatanodeOrExternalError() throws IOException {\n    if (!errorState.hasDatanodeError() && !shouldHandleExternalError()) {\n      return false;\n    }\n    LOG.debug(\"start process datanode/external error, {}\", this);\n    if (response != null) {\n      LOG.info(\"Error Recovery for \" + block +\n          \" waiting for responder to exit. \");\n      return true;\n    }\n    closeStream();\n\n    // move packets from ack queue to front of the data queue\n    synchronized (dataQueue) {\n      dataQueue.addAll(0, ackQueue);\n      ackQueue.clear();\n      packetSendTime.clear();\n    }\n\n    // If we had to recover the pipeline five times in a row for the\n    // same packet, this client likely has corrupt data or corrupting\n    // during transmission.\n    if (!errorState.isRestartingNode() && ++pipelineRecoveryCount > 5) {\n      LOG.warn(\"Error recovering pipeline for writing \" +\n          block + \". Already retried 5 times for the same packet.\");\n      lastException.set(new IOException(\"Failing write. Tried pipeline \" +\n          \"recovery 5 times without success.\"));\n      streamerClosed = true;\n      return false;\n    }\n\n    setupPipelineForAppendOrRecovery();\n\n    if (!streamerClosed && dfsClient.clientRunning) {\n      if (stage == BlockConstructionStage.PIPELINE_CLOSE) {\n\n        // If we had an error while closing the pipeline, we go through a fast-path\n        // where the BlockReceiver does not run. Instead, the DataNode just finalizes\n        // the block immediately during the 'connect ack' process. So, we want to pull\n        // the end-of-block packet from the dataQueue, since we don't actually have\n        // a true pipeline to send it over.\n        //\n        // We also need to set lastAckedSeqno to the end-of-block Packet's seqno, so that\n        // a client waiting on close() will be aware that the flush finished.\n        synchronized (dataQueue) {\n          DFSPacket endOfBlockPacket = dataQueue.remove();  // remove the end of block packet\n          // Close any trace span associated with this Packet\n          Span span = endOfBlockPacket.getSpan();\n          if (span != null) {\n            span.finish();\n            endOfBlockPacket.setSpan(null);\n          }\n          assert endOfBlockPacket.isLastPacketInBlock();\n          assert lastAckedSeqno == endOfBlockPacket.getSeqno() - 1;\n          lastAckedSeqno = endOfBlockPacket.getSeqno();\n          pipelineRecoveryCount = 0;\n          dataQueue.notifyAll();\n        }\n        endBlock();\n      } else {"}
{"mp_id": "hdfs::DS::pdoe->endBlock", "caller": "DataStreamer:processDatanodeOrExternalError()", "callee": "DataStreamer:endBlock()", "gt": "valid", "stratum": "simple", "infeasible_reason": "", "rationale": "After pipeline recovery (pipelineRecoveryCount<=5), calls endBlock to finalize current block.", "caller_code": "* @return true if it should sleep for a while after returning.\n   */\n  private boolean processDatanodeOrExternalError() throws IOException {\n    if (!errorState.hasDatanodeError() && !shouldHandleExternalError()) {\n      return false;\n    }\n    LOG.debug(\"start process datanode/external error, {}\", this);\n    if (response != null) {\n      LOG.info(\"Error Recovery for \" + block +\n          \" waiting for responder to exit. \");\n      return true;\n    }\n    closeStream();\n\n    // move packets from ack queue to front of the data queue\n    synchronized (dataQueue) {\n      dataQueue.addAll(0, ackQueue);\n      ackQueue.clear();\n      packetSendTime.clear();\n    }\n\n    // If we had to recover the pipeline five times in a row for the\n    // same packet, this client likely has corrupt data or corrupting\n    // during transmission.\n    if (!errorState.isRestartingNode() && ++pipelineRecoveryCount > 5) {\n      LOG.warn(\"Error recovering pipeline for writing \" +\n          block + \". Already retried 5 times for the same packet.\");\n      lastException.set(new IOException(\"Failing write. Tried pipeline \" +\n          \"recovery 5 times without success.\"));\n      streamerClosed = true;\n      return false;\n    }\n\n    setupPipelineForAppendOrRecovery();\n\n    if (!streamerClosed && dfsClient.clientRunning) {\n      if (stage == BlockConstructionStage.PIPELINE_CLOSE) {\n\n        // If we had an error while closing the pipeline, we go through a fast-path\n        // where the BlockReceiver does not run. Instead, the DataNode just finalizes\n        // the block immediately during the 'connect ack' process. So, we want to pull\n        // the end-of-block packet from the dataQueue, since we don't actually have\n        // a true pipeline to send it over.\n        //\n        // We also need to set lastAckedSeqno to the end-of-block Packet's seqno, so that\n        // a client waiting on close() will be aware that the flush finished.\n        synchronized (dataQueue) {\n          DFSPacket endOfBlockPacket = dataQueue.remove();  // remove the end of block packet\n          // Close any trace span associated with this Packet\n          Span span = endOfBlockPacket.getSpan();\n          if (span != null) {\n            span.finish();\n            endOfBlockPacket.setSpan(null);\n          }\n          assert endOfBlockPacket.isLastPacketInBlock();\n          assert lastAckedSeqno == endOfBlockPacket.getSeqno() - 1;\n          lastAckedSeqno = endOfBlockPacket.getSeqno();\n          pipelineRecoveryCount = 0;\n          dataQueue.notifyAll();\n        }\n        endBlock();\n      } else {", "callee_code": "}\n\n  protected void endBlock() {\n    LOG.debug(\"Closing old block {}\", block);\n    this.setName(\"DataStreamer for file \" + src);\n    closeResponder();\n    closeStream();\n    setPipeline(null, null, null);\n    stage = BlockConstructionStage.PIPELINE_SETUP_CREATE;\n  }\n\n  private boolean shouldStop() {\n    return streamerClosed || errorState.hasError() || !dfsClient.clientRunning;\n  }\n\n  /*\n   * streamer thread is the only thread that opens streams to datanode,\n   * and closes them. Any error recovery is also done by this thread.\n   */\n  @Override\n  public void run() {\n    TraceScope scope = null;\n    while (!streamerClosed && dfsClient.clientRunning) {\n      // if the Responder encountered an error, shutdown Responder\n      if (errorState.hasError()) {\n        closeResponder();\n      }\n\n      DFSPacket one;\n      try {\n        // process datanode IO errors if any\n        boolean doSleep = processDatanodeOrExternalError();\n\n        synchronized (dataQueue) {\n          // wait for a packet to be sent.\n          while ((!shouldStop() && dataQueue.isEmpty()) || doSleep) {\n            long timeout = 1000;\n            if (stage == BlockConstructionStage.DATA_STREAMING) {\n              timeout = sendHeartbeat();\n            }\n            try {\n              dataQueue.wait(timeout);\n            } catch (InterruptedException  e) {\n              LOG.debug(\"Thread interrupted\", e);\n            }\n            doSleep = false;\n          }\n          if (shouldStop()) {\n            continue;\n          }\n          // get packet to be sent.\n          one = dataQueue.getFirst(); // regular data packet\n          SpanContext[] parents = one.getTraceParents();\n          if (parents != null && parents.length > 0) {\n            // The original code stored multiple parents in the DFSPacket, and\n            // use them ALL here when creating a new Span. We only use the\n            // last one FOR NOW. Moreover, we don't activate the Span for now.\n            scope = dfsClient.getTracer().\n                newScope(\"dataStreamer\", parents[0], false);\n            //scope.getSpan().setParents(parents);\n          }\n        }", "caller_log_raw_stripped": "[ENTRY] DataStreamer:processDatanodeOrExternalError()\n  IF: pipelineRecoveryCount <= 5\n  CALL: endBlock()", "callee_log_raw_stripped": "[ENTRY] DataStreamer:endBlock()\n  [INFO] Ending block {blockId}\n  EXIT", "caller_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:processDatanodeOrExternalError()\n  IF: pipelineRecoveryCount <= 5\n  CALL: endBlock()\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:processDatanodeOrExternalError()\n  IF: pipelineRecoveryCount <= 5\n  CALL: endBlock()\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "callee_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:endBlock()\n  [INFO] Ending block {blockId}\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:endBlock()\n  [INFO] Ending block {blockId}\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "parent_info": "node name is DataStreamer:processDatanodeOrExternalError()node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:processDatanodeOrExternalError()\n  IF: pipelineRecoveryCount <= 5\n  CALL: endBlock()\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:processDatanodeOrExternalError()\n  IF: pipelineRecoveryCount <= 5\n  CALL: endBlock()\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```souce code:* @return true if it should sleep for a while after returning.\n   */\n  private boolean processDatanodeOrExternalError() throws IOException {\n    if (!errorState.hasDatanodeError() && !shouldHandleExternalError()) {\n      return false;\n    }\n    LOG.debug(\"start process datanode/external error, {}\", this);\n    if (response != null) {\n      LOG.info(\"Error Recovery for \" + block +\n          \" waiting for responder to exit. \");\n      return true;\n    }\n    closeStream();\n\n    // move packets from ack queue to front of the data queue\n    synchronized (dataQueue) {\n      dataQueue.addAll(0, ackQueue);\n      ackQueue.clear();\n      packetSendTime.clear();\n    }\n\n    // If we had to recover the pipeline five times in a row for the\n    // same packet, this client likely has corrupt data or corrupting\n    // during transmission.\n    if (!errorState.isRestartingNode() && ++pipelineRecoveryCount > 5) {\n      LOG.warn(\"Error recovering pipeline for writing \" +\n          block + \". Already retried 5 times for the same packet.\");\n      lastException.set(new IOException(\"Failing write. Tried pipeline \" +\n          \"recovery 5 times without success.\"));\n      streamerClosed = true;\n      return false;\n    }\n\n    setupPipelineForAppendOrRecovery();\n\n    if (!streamerClosed && dfsClient.clientRunning) {\n      if (stage == BlockConstructionStage.PIPELINE_CLOSE) {\n\n        // If we had an error while closing the pipeline, we go through a fast-path\n        // where the BlockReceiver does not run. Instead, the DataNode just finalizes\n        // the block immediately during the 'connect ack' process. So, we want to pull\n        // the end-of-block packet from the dataQueue, since we don't actually have\n        // a true pipeline to send it over.\n        //\n        // We also need to set lastAckedSeqno to the end-of-block Packet's seqno, so that\n        // a client waiting on close() will be aware that the flush finished.\n        synchronized (dataQueue) {\n          DFSPacket endOfBlockPacket = dataQueue.remove();  // remove the end of block packet\n          // Close any trace span associated with this Packet\n          Span span = endOfBlockPacket.getSpan();\n          if (span != null) {\n            span.finish();\n            endOfBlockPacket.setSpan(null);\n          }\n          assert endOfBlockPacket.isLastPacketInBlock();\n          assert lastAckedSeqno == endOfBlockPacket.getSeqno() - 1;\n          lastAckedSeqno = endOfBlockPacket.getSeqno();\n          pipelineRecoveryCount = 0;\n          dataQueue.notifyAll();\n        }\n        endBlock();\n      } else {", "child_info": "node name isDataStreamer:endBlock()node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:endBlock()\n  [INFO] Ending block {blockId}\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:endBlock()\n  [INFO] Ending block {blockId}\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```source code:}\n\n  protected void endBlock() {\n    LOG.debug(\"Closing old block {}\", block);\n    this.setName(\"DataStreamer for file \" + src);\n    closeResponder();\n    closeStream();\n    setPipeline(null, null, null);\n    stage = BlockConstructionStage.PIPELINE_SETUP_CREATE;\n  }\n\n  private boolean shouldStop() {\n    return streamerClosed || errorState.hasError() || !dfsClient.clientRunning;\n  }\n\n  /*\n   * streamer thread is the only thread that opens streams to datanode,\n   * and closes them. Any error recovery is also done by this thread.\n   */\n  @Override\n  public void run() {\n    TraceScope scope = null;\n    while (!streamerClosed && dfsClient.clientRunning) {\n      // if the Responder encountered an error, shutdown Responder\n      if (errorState.hasError()) {\n        closeResponder();\n      }\n\n      DFSPacket one;\n      try {\n        // process datanode IO errors if any\n        boolean doSleep = processDatanodeOrExternalError();\n\n        synchronized (dataQueue) {\n          // wait for a packet to be sent.\n          while ((!shouldStop() && dataQueue.isEmpty()) || doSleep) {\n            long timeout = 1000;\n            if (stage == BlockConstructionStage.DATA_STREAMING) {\n              timeout = sendHeartbeat();\n            }\n            try {\n              dataQueue.wait(timeout);\n            } catch (InterruptedException  e) {\n              LOG.debug(\"Thread interrupted\", e);\n            }\n            doSleep = false;\n          }\n          if (shouldStop()) {\n            continue;\n          }\n          // get packet to be sent.\n          one = dataQueue.getFirst(); // regular data packet\n          SpanContext[] parents = one.getTraceParents();\n          if (parents != null && parents.length > 0) {\n            // The original code stored multiple parents in the DFSPacket, and\n            // use them ALL here when creating a new Span. We only use the\n            // last one FOR NOW. Moreover, we don't activate the Span for now.\n            scope = dfsClient.getTracer().\n                newScope(\"dataStreamer\", parents[0], false);\n            //scope.getSpan().setParents(parents);\n          }\n        }"}
{"mp_id": "hdfs::DS::pdoe->endBlock-INFEASIBLE", "caller": "DataStreamer:processDatanodeOrExternalError()", "callee": "DataStreamer:endBlock()", "gt": "infeasible", "stratum": "simple", "infeasible_reason": "pipelineRecoveryCount>5 — IOException thrown before reaching endBlock().", "rationale": "", "caller_code": "* @return true if it should sleep for a while after returning.\n   */\n  private boolean processDatanodeOrExternalError() throws IOException {\n    if (!errorState.hasDatanodeError() && !shouldHandleExternalError()) {\n      return false;\n    }\n    LOG.debug(\"start process datanode/external error, {}\", this);\n    if (response != null) {\n      LOG.info(\"Error Recovery for \" + block +\n          \" waiting for responder to exit. \");\n      return true;\n    }\n    closeStream();\n\n    // move packets from ack queue to front of the data queue\n    synchronized (dataQueue) {\n      dataQueue.addAll(0, ackQueue);\n      ackQueue.clear();\n      packetSendTime.clear();\n    }\n\n    // If we had to recover the pipeline five times in a row for the\n    // same packet, this client likely has corrupt data or corrupting\n    // during transmission.\n    if (!errorState.isRestartingNode() && ++pipelineRecoveryCount > 5) {\n      LOG.warn(\"Error recovering pipeline for writing \" +\n          block + \". Already retried 5 times for the same packet.\");\n      lastException.set(new IOException(\"Failing write. Tried pipeline \" +\n          \"recovery 5 times without success.\"));\n      streamerClosed = true;\n      return false;\n    }\n\n    setupPipelineForAppendOrRecovery();\n\n    if (!streamerClosed && dfsClient.clientRunning) {\n      if (stage == BlockConstructionStage.PIPELINE_CLOSE) {\n\n        // If we had an error while closing the pipeline, we go through a fast-path\n        // where the BlockReceiver does not run. Instead, the DataNode just finalizes\n        // the block immediately during the 'connect ack' process. So, we want to pull\n        // the end-of-block packet from the dataQueue, since we don't actually have\n        // a true pipeline to send it over.\n        //\n        // We also need to set lastAckedSeqno to the end-of-block Packet's seqno, so that\n        // a client waiting on close() will be aware that the flush finished.\n        synchronized (dataQueue) {\n          DFSPacket endOfBlockPacket = dataQueue.remove();  // remove the end of block packet\n          // Close any trace span associated with this Packet\n          Span span = endOfBlockPacket.getSpan();\n          if (span != null) {\n            span.finish();\n            endOfBlockPacket.setSpan(null);\n          }\n          assert endOfBlockPacket.isLastPacketInBlock();\n          assert lastAckedSeqno == endOfBlockPacket.getSeqno() - 1;\n          lastAckedSeqno = endOfBlockPacket.getSeqno();\n          pipelineRecoveryCount = 0;\n          dataQueue.notifyAll();\n        }\n        endBlock();\n      } else {", "callee_code": "}\n\n  protected void endBlock() {\n    LOG.debug(\"Closing old block {}\", block);\n    this.setName(\"DataStreamer for file \" + src);\n    closeResponder();\n    closeStream();\n    setPipeline(null, null, null);\n    stage = BlockConstructionStage.PIPELINE_SETUP_CREATE;\n  }\n\n  private boolean shouldStop() {\n    return streamerClosed || errorState.hasError() || !dfsClient.clientRunning;\n  }\n\n  /*\n   * streamer thread is the only thread that opens streams to datanode,\n   * and closes them. Any error recovery is also done by this thread.\n   */\n  @Override\n  public void run() {\n    TraceScope scope = null;\n    while (!streamerClosed && dfsClient.clientRunning) {\n      // if the Responder encountered an error, shutdown Responder\n      if (errorState.hasError()) {\n        closeResponder();\n      }\n\n      DFSPacket one;\n      try {\n        // process datanode IO errors if any\n        boolean doSleep = processDatanodeOrExternalError();\n\n        synchronized (dataQueue) {\n          // wait for a packet to be sent.\n          while ((!shouldStop() && dataQueue.isEmpty()) || doSleep) {\n            long timeout = 1000;\n            if (stage == BlockConstructionStage.DATA_STREAMING) {\n              timeout = sendHeartbeat();\n            }\n            try {\n              dataQueue.wait(timeout);\n            } catch (InterruptedException  e) {\n              LOG.debug(\"Thread interrupted\", e);\n            }\n            doSleep = false;\n          }\n          if (shouldStop()) {\n            continue;\n          }\n          // get packet to be sent.\n          one = dataQueue.getFirst(); // regular data packet\n          SpanContext[] parents = one.getTraceParents();\n          if (parents != null && parents.length > 0) {\n            // The original code stored multiple parents in the DFSPacket, and\n            // use them ALL here when creating a new Span. We only use the\n            // last one FOR NOW. Moreover, we don't activate the Span for now.\n            scope = dfsClient.getTracer().\n                newScope(\"dataStreamer\", parents[0], false);\n            //scope.getSpan().setParents(parents);\n          }\n        }", "caller_log_raw_stripped": "[ENTRY] DataStreamer:processDatanodeOrExternalError()\n  IF: pipelineRecoveryCount==6 (exceeded) \n  throw new IOException(\"Pipeline recovery failed\")", "callee_log_raw_stripped": "[ENTRY] DataStreamer:endBlock()\n  [INFO] Ending block {blockId}\n  EXIT", "caller_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:processDatanodeOrExternalError()\n  IF: pipelineRecoveryCount==6 (exceeded) \n  throw new IOException(\"Pipeline recovery failed\")\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:processDatanodeOrExternalError()\n  IF: pipelineRecoveryCount==6 (exceeded) \n  throw new IOException(\"Pipeline recovery failed\")\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "callee_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:endBlock()\n  [INFO] Ending block {blockId}\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:endBlock()\n  [INFO] Ending block {blockId}\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "parent_info": "node name is DataStreamer:processDatanodeOrExternalError()node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:processDatanodeOrExternalError()\n  IF: pipelineRecoveryCount==6 (exceeded) \n  throw new IOException(\"Pipeline recovery failed\")\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:processDatanodeOrExternalError()\n  IF: pipelineRecoveryCount==6 (exceeded) \n  throw new IOException(\"Pipeline recovery failed\")\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```souce code:* @return true if it should sleep for a while after returning.\n   */\n  private boolean processDatanodeOrExternalError() throws IOException {\n    if (!errorState.hasDatanodeError() && !shouldHandleExternalError()) {\n      return false;\n    }\n    LOG.debug(\"start process datanode/external error, {}\", this);\n    if (response != null) {\n      LOG.info(\"Error Recovery for \" + block +\n          \" waiting for responder to exit. \");\n      return true;\n    }\n    closeStream();\n\n    // move packets from ack queue to front of the data queue\n    synchronized (dataQueue) {\n      dataQueue.addAll(0, ackQueue);\n      ackQueue.clear();\n      packetSendTime.clear();\n    }\n\n    // If we had to recover the pipeline five times in a row for the\n    // same packet, this client likely has corrupt data or corrupting\n    // during transmission.\n    if (!errorState.isRestartingNode() && ++pipelineRecoveryCount > 5) {\n      LOG.warn(\"Error recovering pipeline for writing \" +\n          block + \". Already retried 5 times for the same packet.\");\n      lastException.set(new IOException(\"Failing write. Tried pipeline \" +\n          \"recovery 5 times without success.\"));\n      streamerClosed = true;\n      return false;\n    }\n\n    setupPipelineForAppendOrRecovery();\n\n    if (!streamerClosed && dfsClient.clientRunning) {\n      if (stage == BlockConstructionStage.PIPELINE_CLOSE) {\n\n        // If we had an error while closing the pipeline, we go through a fast-path\n        // where the BlockReceiver does not run. Instead, the DataNode just finalizes\n        // the block immediately during the 'connect ack' process. So, we want to pull\n        // the end-of-block packet from the dataQueue, since we don't actually have\n        // a true pipeline to send it over.\n        //\n        // We also need to set lastAckedSeqno to the end-of-block Packet's seqno, so that\n        // a client waiting on close() will be aware that the flush finished.\n        synchronized (dataQueue) {\n          DFSPacket endOfBlockPacket = dataQueue.remove();  // remove the end of block packet\n          // Close any trace span associated with this Packet\n          Span span = endOfBlockPacket.getSpan();\n          if (span != null) {\n            span.finish();\n            endOfBlockPacket.setSpan(null);\n          }\n          assert endOfBlockPacket.isLastPacketInBlock();\n          assert lastAckedSeqno == endOfBlockPacket.getSeqno() - 1;\n          lastAckedSeqno = endOfBlockPacket.getSeqno();\n          pipelineRecoveryCount = 0;\n          dataQueue.notifyAll();\n        }\n        endBlock();\n      } else {", "child_info": "node name isDataStreamer:endBlock()node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:endBlock()\n  [INFO] Ending block {blockId}\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:endBlock()\n  [INFO] Ending block {blockId}\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```source code:}\n\n  protected void endBlock() {\n    LOG.debug(\"Closing old block {}\", block);\n    this.setName(\"DataStreamer for file \" + src);\n    closeResponder();\n    closeStream();\n    setPipeline(null, null, null);\n    stage = BlockConstructionStage.PIPELINE_SETUP_CREATE;\n  }\n\n  private boolean shouldStop() {\n    return streamerClosed || errorState.hasError() || !dfsClient.clientRunning;\n  }\n\n  /*\n   * streamer thread is the only thread that opens streams to datanode,\n   * and closes them. Any error recovery is also done by this thread.\n   */\n  @Override\n  public void run() {\n    TraceScope scope = null;\n    while (!streamerClosed && dfsClient.clientRunning) {\n      // if the Responder encountered an error, shutdown Responder\n      if (errorState.hasError()) {\n        closeResponder();\n      }\n\n      DFSPacket one;\n      try {\n        // process datanode IO errors if any\n        boolean doSleep = processDatanodeOrExternalError();\n\n        synchronized (dataQueue) {\n          // wait for a packet to be sent.\n          while ((!shouldStop() && dataQueue.isEmpty()) || doSleep) {\n            long timeout = 1000;\n            if (stage == BlockConstructionStage.DATA_STREAMING) {\n              timeout = sendHeartbeat();\n            }\n            try {\n              dataQueue.wait(timeout);\n            } catch (InterruptedException  e) {\n              LOG.debug(\"Thread interrupted\", e);\n            }\n            doSleep = false;\n          }\n          if (shouldStop()) {\n            continue;\n          }\n          // get packet to be sent.\n          one = dataQueue.getFirst(); // regular data packet\n          SpanContext[] parents = one.getTraceParents();\n          if (parents != null && parents.length > 0) {\n            // The original code stored multiple parents in the DFSPacket, and\n            // use them ALL here when creating a new Span. We only use the\n            // last one FOR NOW. Moreover, we don't activate the Span for now.\n            scope = dfsClient.getTracer().\n                newScope(\"dataStreamer\", parents[0], false);\n            //scope.getSpan().setParents(parents);\n          }\n        }"}
{"mp_id": "hdfs::DS::pdoe->setupPipelineForAppendOrRecovery", "caller": "DataStreamer:processDatanodeOrExternalError()", "callee": "DataStreamer:setupPipelineForAppendOrRecovery()", "gt": "valid", "stratum": "simple", "infeasible_reason": "", "rationale": "Called when error is recoverable (errorState.isRestartingNode()).", "caller_code": "* @return true if it should sleep for a while after returning.\n   */\n  private boolean processDatanodeOrExternalError() throws IOException {\n    if (!errorState.hasDatanodeError() && !shouldHandleExternalError()) {\n      return false;\n    }\n    LOG.debug(\"start process datanode/external error, {}\", this);\n    if (response != null) {\n      LOG.info(\"Error Recovery for \" + block +\n          \" waiting for responder to exit. \");\n      return true;\n    }\n    closeStream();\n\n    // move packets from ack queue to front of the data queue\n    synchronized (dataQueue) {\n      dataQueue.addAll(0, ackQueue);\n      ackQueue.clear();\n      packetSendTime.clear();\n    }\n\n    // If we had to recover the pipeline five times in a row for the\n    // same packet, this client likely has corrupt data or corrupting\n    // during transmission.\n    if (!errorState.isRestartingNode() && ++pipelineRecoveryCount > 5) {\n      LOG.warn(\"Error recovering pipeline for writing \" +\n          block + \". Already retried 5 times for the same packet.\");\n      lastException.set(new IOException(\"Failing write. Tried pipeline \" +\n          \"recovery 5 times without success.\"));\n      streamerClosed = true;\n      return false;\n    }\n\n    setupPipelineForAppendOrRecovery();\n\n    if (!streamerClosed && dfsClient.clientRunning) {\n      if (stage == BlockConstructionStage.PIPELINE_CLOSE) {\n\n        // If we had an error while closing the pipeline, we go through a fast-path\n        // where the BlockReceiver does not run. Instead, the DataNode just finalizes\n        // the block immediately during the 'connect ack' process. So, we want to pull\n        // the end-of-block packet from the dataQueue, since we don't actually have\n        // a true pipeline to send it over.\n        //\n        // We also need to set lastAckedSeqno to the end-of-block Packet's seqno, so that\n        // a client waiting on close() will be aware that the flush finished.\n        synchronized (dataQueue) {\n          DFSPacket endOfBlockPacket = dataQueue.remove();  // remove the end of block packet\n          // Close any trace span associated with this Packet\n          Span span = endOfBlockPacket.getSpan();\n          if (span != null) {\n            span.finish();\n            endOfBlockPacket.setSpan(null);\n          }\n          assert endOfBlockPacket.isLastPacketInBlock();\n          assert lastAckedSeqno == endOfBlockPacket.getSeqno() - 1;\n          lastAckedSeqno = endOfBlockPacket.getSeqno();\n          pipelineRecoveryCount = 0;\n          dataQueue.notifyAll();\n        }\n        endBlock();\n      } else {", "callee_code": "* It keeps on trying until a pipeline is setup\n   */\n  private void setupPipelineForAppendOrRecovery() throws IOException {\n    // Check number of datanodes. Note that if there is no healthy datanode,\n    // this must be internal error because we mark external error in striped\n    // outputstream only when all the streamers are in the DATA_STREAMING stage\n    if (nodes == null || nodes.length == 0) {\n      String msg = \"Could not get block locations. \" + \"Source file \\\"\"\n          + src + \"\\\" - Aborting...\" + this;\n      LOG.warn(msg);\n      lastException.set(new IOException(msg));\n      streamerClosed = true;\n      return;\n    }\n    setupPipelineInternal(nodes, storageTypes, storageIDs);\n  }\n\n  protected void setupPipelineInternal(DatanodeInfo[] datanodes,\n      StorageType[] nodeStorageTypes, String[] nodeStorageIDs)\n      throws IOException {\n    boolean success = false;\n    long newGS = 0L;\n    while (!success && !streamerClosed && dfsClient.clientRunning) {\n      if (!handleRestartingDatanode()) {\n        return;\n      }\n\n      final boolean isRecovery = errorState.hasInternalError();\n      if (!handleBadDatanode()) {\n        return;\n      }\n\n      handleDatanodeReplacement();\n\n      // get a new generation stamp and an access token\n      final LocatedBlock lb = updateBlockForPipeline();\n      newGS = lb.getBlock().getGenerationStamp();\n      accessToken = lb.getBlockToken();\n\n      // set up the pipeline again with the remaining nodes\n      success = createBlockOutputStream(nodes, storageTypes, storageIDs, newGS,\n          isRecovery);\n\n      failPacket4Testing();\n\n      errorState.checkRestartingNodeDeadline(nodes);\n    } // while\n\n    if (success) {\n      updatePipeline(newGS);\n    }\n  }\n\n  /**\n   * Sleep if a node is restarting.\n   * This process is repeated until the deadline or the node starts back up.\n   * @return true if it should continue.\n   */\n  boolean handleRestartingDatanode() {\n    if (errorState.isRestartingNode()) {\n      if (!errorState.doWaitForRestart()) {\n        // If node is restarting and not worth to wait for restart then can go", "caller_log_raw_stripped": "[ENTRY] DataStreamer:processDatanodeOrExternalError()\n  IF: errorState.isRestartingNode()\n  CALL: setupPipelineForAppendOrRecovery()", "callee_log_raw_stripped": "[ENTRY] DataStreamer:setupPipelineForAppendOrRecovery()\n  EXIT", "caller_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:processDatanodeOrExternalError()\n  IF: errorState.isRestartingNode()\n  CALL: setupPipelineForAppendOrRecovery()\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:processDatanodeOrExternalError()\n  IF: errorState.isRestartingNode()\n  CALL: setupPipelineForAppendOrRecovery()\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "callee_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:setupPipelineForAppendOrRecovery()\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:setupPipelineForAppendOrRecovery()\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "parent_info": "node name is DataStreamer:processDatanodeOrExternalError()node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:processDatanodeOrExternalError()\n  IF: errorState.isRestartingNode()\n  CALL: setupPipelineForAppendOrRecovery()\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:processDatanodeOrExternalError()\n  IF: errorState.isRestartingNode()\n  CALL: setupPipelineForAppendOrRecovery()\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```souce code:* @return true if it should sleep for a while after returning.\n   */\n  private boolean processDatanodeOrExternalError() throws IOException {\n    if (!errorState.hasDatanodeError() && !shouldHandleExternalError()) {\n      return false;\n    }\n    LOG.debug(\"start process datanode/external error, {}\", this);\n    if (response != null) {\n      LOG.info(\"Error Recovery for \" + block +\n          \" waiting for responder to exit. \");\n      return true;\n    }\n    closeStream();\n\n    // move packets from ack queue to front of the data queue\n    synchronized (dataQueue) {\n      dataQueue.addAll(0, ackQueue);\n      ackQueue.clear();\n      packetSendTime.clear();\n    }\n\n    // If we had to recover the pipeline five times in a row for the\n    // same packet, this client likely has corrupt data or corrupting\n    // during transmission.\n    if (!errorState.isRestartingNode() && ++pipelineRecoveryCount > 5) {\n      LOG.warn(\"Error recovering pipeline for writing \" +\n          block + \". Already retried 5 times for the same packet.\");\n      lastException.set(new IOException(\"Failing write. Tried pipeline \" +\n          \"recovery 5 times without success.\"));\n      streamerClosed = true;\n      return false;\n    }\n\n    setupPipelineForAppendOrRecovery();\n\n    if (!streamerClosed && dfsClient.clientRunning) {\n      if (stage == BlockConstructionStage.PIPELINE_CLOSE) {\n\n        // If we had an error while closing the pipeline, we go through a fast-path\n        // where the BlockReceiver does not run. Instead, the DataNode just finalizes\n        // the block immediately during the 'connect ack' process. So, we want to pull\n        // the end-of-block packet from the dataQueue, since we don't actually have\n        // a true pipeline to send it over.\n        //\n        // We also need to set lastAckedSeqno to the end-of-block Packet's seqno, so that\n        // a client waiting on close() will be aware that the flush finished.\n        synchronized (dataQueue) {\n          DFSPacket endOfBlockPacket = dataQueue.remove();  // remove the end of block packet\n          // Close any trace span associated with this Packet\n          Span span = endOfBlockPacket.getSpan();\n          if (span != null) {\n            span.finish();\n            endOfBlockPacket.setSpan(null);\n          }\n          assert endOfBlockPacket.isLastPacketInBlock();\n          assert lastAckedSeqno == endOfBlockPacket.getSeqno() - 1;\n          lastAckedSeqno = endOfBlockPacket.getSeqno();\n          pipelineRecoveryCount = 0;\n          dataQueue.notifyAll();\n        }\n        endBlock();\n      } else {", "child_info": "node name isDataStreamer:setupPipelineForAppendOrRecovery()node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:setupPipelineForAppendOrRecovery()\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:setupPipelineForAppendOrRecovery()\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```source code:* It keeps on trying until a pipeline is setup\n   */\n  private void setupPipelineForAppendOrRecovery() throws IOException {\n    // Check number of datanodes. Note that if there is no healthy datanode,\n    // this must be internal error because we mark external error in striped\n    // outputstream only when all the streamers are in the DATA_STREAMING stage\n    if (nodes == null || nodes.length == 0) {\n      String msg = \"Could not get block locations. \" + \"Source file \\\"\"\n          + src + \"\\\" - Aborting...\" + this;\n      LOG.warn(msg);\n      lastException.set(new IOException(msg));\n      streamerClosed = true;\n      return;\n    }\n    setupPipelineInternal(nodes, storageTypes, storageIDs);\n  }\n\n  protected void setupPipelineInternal(DatanodeInfo[] datanodes,\n      StorageType[] nodeStorageTypes, String[] nodeStorageIDs)\n      throws IOException {\n    boolean success = false;\n    long newGS = 0L;\n    while (!success && !streamerClosed && dfsClient.clientRunning) {\n      if (!handleRestartingDatanode()) {\n        return;\n      }\n\n      final boolean isRecovery = errorState.hasInternalError();\n      if (!handleBadDatanode()) {\n        return;\n      }\n\n      handleDatanodeReplacement();\n\n      // get a new generation stamp and an access token\n      final LocatedBlock lb = updateBlockForPipeline();\n      newGS = lb.getBlock().getGenerationStamp();\n      accessToken = lb.getBlockToken();\n\n      // set up the pipeline again with the remaining nodes\n      success = createBlockOutputStream(nodes, storageTypes, storageIDs, newGS,\n          isRecovery);\n\n      failPacket4Testing();\n\n      errorState.checkRestartingNodeDeadline(nodes);\n    } // while\n\n    if (success) {\n      updatePipeline(newGS);\n    }\n  }\n\n  /**\n   * Sleep if a node is restarting.\n   * This process is repeated until the deadline or the node starts back up.\n   * @return true if it should continue.\n   */\n  boolean handleRestartingDatanode() {\n    if (errorState.isRestartingNode()) {\n      if (!errorState.doWaitForRestart()) {\n        // If node is restarting and not worth to wait for restart then can go"}
{"mp_id": "hdfs::DS::pdoe->initDataStreaming", "caller": "DataStreamer:processDatanodeOrExternalError()", "callee": "DataStreamer:initDataStreaming()", "gt": "valid", "stratum": "simple", "infeasible_reason": "", "rationale": "After handling external error (shouldHandleExternalError()==true), re-initializes stream.", "caller_code": "* @return true if it should sleep for a while after returning.\n   */\n  private boolean processDatanodeOrExternalError() throws IOException {\n    if (!errorState.hasDatanodeError() && !shouldHandleExternalError()) {\n      return false;\n    }\n    LOG.debug(\"start process datanode/external error, {}\", this);\n    if (response != null) {\n      LOG.info(\"Error Recovery for \" + block +\n          \" waiting for responder to exit. \");\n      return true;\n    }\n    closeStream();\n\n    // move packets from ack queue to front of the data queue\n    synchronized (dataQueue) {\n      dataQueue.addAll(0, ackQueue);\n      ackQueue.clear();\n      packetSendTime.clear();\n    }\n\n    // If we had to recover the pipeline five times in a row for the\n    // same packet, this client likely has corrupt data or corrupting\n    // during transmission.\n    if (!errorState.isRestartingNode() && ++pipelineRecoveryCount > 5) {\n      LOG.warn(\"Error recovering pipeline for writing \" +\n          block + \". Already retried 5 times for the same packet.\");\n      lastException.set(new IOException(\"Failing write. Tried pipeline \" +\n          \"recovery 5 times without success.\"));\n      streamerClosed = true;\n      return false;\n    }\n\n    setupPipelineForAppendOrRecovery();\n\n    if (!streamerClosed && dfsClient.clientRunning) {\n      if (stage == BlockConstructionStage.PIPELINE_CLOSE) {\n\n        // If we had an error while closing the pipeline, we go through a fast-path\n        // where the BlockReceiver does not run. Instead, the DataNode just finalizes\n        // the block immediately during the 'connect ack' process. So, we want to pull\n        // the end-of-block packet from the dataQueue, since we don't actually have\n        // a true pipeline to send it over.\n        //\n        // We also need to set lastAckedSeqno to the end-of-block Packet's seqno, so that\n        // a client waiting on close() will be aware that the flush finished.\n        synchronized (dataQueue) {\n          DFSPacket endOfBlockPacket = dataQueue.remove();  // remove the end of block packet\n          // Close any trace span associated with this Packet\n          Span span = endOfBlockPacket.getSpan();\n          if (span != null) {\n            span.finish();\n            endOfBlockPacket.setSpan(null);\n          }\n          assert endOfBlockPacket.isLastPacketInBlock();\n          assert lastAckedSeqno == endOfBlockPacket.getSeqno() - 1;\n          lastAckedSeqno = endOfBlockPacket.getSeqno();\n          pipelineRecoveryCount = 0;\n          dataQueue.notifyAll();\n        }\n        endBlock();\n      } else {", "callee_code": "* Initialize for data streaming\n   */\n  private void initDataStreaming() {\n    this.setName(\"DataStreamer for file \" + src +\n        \" block \" + block);\n    if (LOG.isDebugEnabled()) {\n      LOG.debug(\"nodes {} storageTypes {} storageIDs {}\",\n          Arrays.toString(nodes),\n          Arrays.toString(storageTypes),\n          Arrays.toString(storageIDs));\n    }\n    response = new ResponseProcessor(nodes);\n    response.start();\n    stage = BlockConstructionStage.DATA_STREAMING;\n    lastPacket = Time.monotonicNow();\n  }\n\n  protected void endBlock() {\n    LOG.debug(\"Closing old block {}\", block);\n    this.setName(\"DataStreamer for file \" + src);\n    closeResponder();\n    closeStream();\n    setPipeline(null, null, null);\n    stage = BlockConstructionStage.PIPELINE_SETUP_CREATE;\n  }\n\n  private boolean shouldStop() {\n    return streamerClosed || errorState.hasError() || !dfsClient.clientRunning;\n  }\n\n  /*\n   * streamer thread is the only thread that opens streams to datanode,\n   * and closes them. Any error recovery is also done by this thread.\n   */\n  @Override\n  public void run() {\n    TraceScope scope = null;\n    while (!streamerClosed && dfsClient.clientRunning) {\n      // if the Responder encountered an error, shutdown Responder\n      if (errorState.hasError()) {\n        closeResponder();\n      }\n\n      DFSPacket one;\n      try {\n        // process datanode IO errors if any\n        boolean doSleep = processDatanodeOrExternalError();\n\n        synchronized (dataQueue) {\n          // wait for a packet to be sent.\n          while ((!shouldStop() && dataQueue.isEmpty()) || doSleep) {\n            long timeout = 1000;\n            if (stage == BlockConstructionStage.DATA_STREAMING) {\n              timeout = sendHeartbeat();\n            }\n            try {\n              dataQueue.wait(timeout);\n            } catch (InterruptedException  e) {\n              LOG.debug(\"Thread interrupted\", e);\n            }\n            doSleep = false;\n          }", "caller_log_raw_stripped": "[ENTRY] DataStreamer:processDatanodeOrExternalError()\n  IF: shouldHandleExternalError()\n  CALL: initDataStreaming()", "callee_log_raw_stripped": "[ENTRY] DataStreamer:initDataStreaming()\n  EXIT", "caller_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:processDatanodeOrExternalError()\n  IF: shouldHandleExternalError()\n  CALL: initDataStreaming()\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:processDatanodeOrExternalError()\n  IF: shouldHandleExternalError()\n  CALL: initDataStreaming()\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "callee_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:initDataStreaming()\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:initDataStreaming()\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "parent_info": "node name is DataStreamer:processDatanodeOrExternalError()node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:processDatanodeOrExternalError()\n  IF: shouldHandleExternalError()\n  CALL: initDataStreaming()\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:processDatanodeOrExternalError()\n  IF: shouldHandleExternalError()\n  CALL: initDataStreaming()\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```souce code:* @return true if it should sleep for a while after returning.\n   */\n  private boolean processDatanodeOrExternalError() throws IOException {\n    if (!errorState.hasDatanodeError() && !shouldHandleExternalError()) {\n      return false;\n    }\n    LOG.debug(\"start process datanode/external error, {}\", this);\n    if (response != null) {\n      LOG.info(\"Error Recovery for \" + block +\n          \" waiting for responder to exit. \");\n      return true;\n    }\n    closeStream();\n\n    // move packets from ack queue to front of the data queue\n    synchronized (dataQueue) {\n      dataQueue.addAll(0, ackQueue);\n      ackQueue.clear();\n      packetSendTime.clear();\n    }\n\n    // If we had to recover the pipeline five times in a row for the\n    // same packet, this client likely has corrupt data or corrupting\n    // during transmission.\n    if (!errorState.isRestartingNode() && ++pipelineRecoveryCount > 5) {\n      LOG.warn(\"Error recovering pipeline for writing \" +\n          block + \". Already retried 5 times for the same packet.\");\n      lastException.set(new IOException(\"Failing write. Tried pipeline \" +\n          \"recovery 5 times without success.\"));\n      streamerClosed = true;\n      return false;\n    }\n\n    setupPipelineForAppendOrRecovery();\n\n    if (!streamerClosed && dfsClient.clientRunning) {\n      if (stage == BlockConstructionStage.PIPELINE_CLOSE) {\n\n        // If we had an error while closing the pipeline, we go through a fast-path\n        // where the BlockReceiver does not run. Instead, the DataNode just finalizes\n        // the block immediately during the 'connect ack' process. So, we want to pull\n        // the end-of-block packet from the dataQueue, since we don't actually have\n        // a true pipeline to send it over.\n        //\n        // We also need to set lastAckedSeqno to the end-of-block Packet's seqno, so that\n        // a client waiting on close() will be aware that the flush finished.\n        synchronized (dataQueue) {\n          DFSPacket endOfBlockPacket = dataQueue.remove();  // remove the end of block packet\n          // Close any trace span associated with this Packet\n          Span span = endOfBlockPacket.getSpan();\n          if (span != null) {\n            span.finish();\n            endOfBlockPacket.setSpan(null);\n          }\n          assert endOfBlockPacket.isLastPacketInBlock();\n          assert lastAckedSeqno == endOfBlockPacket.getSeqno() - 1;\n          lastAckedSeqno = endOfBlockPacket.getSeqno();\n          pipelineRecoveryCount = 0;\n          dataQueue.notifyAll();\n        }\n        endBlock();\n      } else {", "child_info": "node name isDataStreamer:initDataStreaming()node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:initDataStreaming()\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:initDataStreaming()\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```source code:* Initialize for data streaming\n   */\n  private void initDataStreaming() {\n    this.setName(\"DataStreamer for file \" + src +\n        \" block \" + block);\n    if (LOG.isDebugEnabled()) {\n      LOG.debug(\"nodes {} storageTypes {} storageIDs {}\",\n          Arrays.toString(nodes),\n          Arrays.toString(storageTypes),\n          Arrays.toString(storageIDs));\n    }\n    response = new ResponseProcessor(nodes);\n    response.start();\n    stage = BlockConstructionStage.DATA_STREAMING;\n    lastPacket = Time.monotonicNow();\n  }\n\n  protected void endBlock() {\n    LOG.debug(\"Closing old block {}\", block);\n    this.setName(\"DataStreamer for file \" + src);\n    closeResponder();\n    closeStream();\n    setPipeline(null, null, null);\n    stage = BlockConstructionStage.PIPELINE_SETUP_CREATE;\n  }\n\n  private boolean shouldStop() {\n    return streamerClosed || errorState.hasError() || !dfsClient.clientRunning;\n  }\n\n  /*\n   * streamer thread is the only thread that opens streams to datanode,\n   * and closes them. Any error recovery is also done by this thread.\n   */\n  @Override\n  public void run() {\n    TraceScope scope = null;\n    while (!streamerClosed && dfsClient.clientRunning) {\n      // if the Responder encountered an error, shutdown Responder\n      if (errorState.hasError()) {\n        closeResponder();\n      }\n\n      DFSPacket one;\n      try {\n        // process datanode IO errors if any\n        boolean doSleep = processDatanodeOrExternalError();\n\n        synchronized (dataQueue) {\n          // wait for a packet to be sent.\n          while ((!shouldStop() && dataQueue.isEmpty()) || doSleep) {\n            long timeout = 1000;\n            if (stage == BlockConstructionStage.DATA_STREAMING) {\n              timeout = sendHeartbeat();\n            }\n            try {\n              dataQueue.wait(timeout);\n            } catch (InterruptedException  e) {\n              LOG.debug(\"Thread interrupted\", e);\n            }\n            doSleep = false;\n          }"}
{"mp_id": "hdfs::DS::run->endBlock", "caller": "DataStreamer:run()", "callee": "DataStreamer:endBlock()", "gt": "valid", "stratum": "simple", "infeasible_reason": "", "rationale": "Called when final ACK received; stage transitions DATA_STREAMING→PIPELINE_CLOSE.", "caller_code": "*/\n  @Override\n  public void run() {\n    TraceScope scope = null;\n    while (!streamerClosed && dfsClient.clientRunning) {\n      // if the Responder encountered an error, shutdown Responder\n      if (errorState.hasError()) {\n        closeResponder();\n      }\n\n      DFSPacket one;\n      try {\n        // process datanode IO errors if any\n        boolean doSleep = processDatanodeOrExternalError();\n\n        synchronized (dataQueue) {\n          // wait for a packet to be sent.\n          while ((!shouldStop() && dataQueue.isEmpty()) || doSleep) {\n            long timeout = 1000;\n            if (stage == BlockConstructionStage.DATA_STREAMING) {\n              timeout = sendHeartbeat();\n            }\n            try {\n              dataQueue.wait(timeout);\n            } catch (InterruptedException  e) {\n              LOG.debug(\"Thread interrupted\", e);\n            }\n            doSleep = false;\n          }\n          if (shouldStop()) {\n            continue;\n          }\n          // get packet to be sent.\n          one = dataQueue.getFirst(); // regular data packet\n          SpanContext[] parents = one.getTraceParents();\n          if (parents != null && parents.length > 0) {\n            // The original code stored multiple parents in the DFSPacket, and\n            // use them ALL here when creating a new Span. We only use the\n            // last one FOR NOW. Moreover, we don't activate the Span for now.\n            scope = dfsClient.getTracer().\n                newScope(\"dataStreamer\", parents[0], false);\n            //scope.getSpan().setParents(parents);\n          }\n        }\n\n        // The DataStreamer has to release the dataQueue before sleeping,\n        // otherwise it will cause the ResponseProcessor to accept the ACK delay.\n        try {\n          backOffIfNecessary();\n        } catch (InterruptedException e) {\n          LOG.debug(\"Thread interrupted\", e);\n        }\n\n        // get new block from namenode.\n        LOG.debug(\"stage={}, {}\", stage, this);\n\n        if (stage == BlockConstructionStage.PIPELINE_SETUP_CREATE) {\n          LOG.debug(\"Allocating new block: {}\", this);\n          setPipeline(nextBlockOutputStream());\n          initDataStreaming();\n        } else if (stage == BlockConstructionStage.PIPELINE_SETUP_APPEND) {\n          LOG.debug(\"Append to block {}\", block);", "callee_code": "}\n\n  protected void endBlock() {\n    LOG.debug(\"Closing old block {}\", block);\n    this.setName(\"DataStreamer for file \" + src);\n    closeResponder();\n    closeStream();\n    setPipeline(null, null, null);\n    stage = BlockConstructionStage.PIPELINE_SETUP_CREATE;\n  }\n\n  private boolean shouldStop() {\n    return streamerClosed || errorState.hasError() || !dfsClient.clientRunning;\n  }\n\n  /*\n   * streamer thread is the only thread that opens streams to datanode,\n   * and closes them. Any error recovery is also done by this thread.\n   */\n  @Override\n  public void run() {\n    TraceScope scope = null;\n    while (!streamerClosed && dfsClient.clientRunning) {\n      // if the Responder encountered an error, shutdown Responder\n      if (errorState.hasError()) {\n        closeResponder();\n      }\n\n      DFSPacket one;\n      try {\n        // process datanode IO errors if any\n        boolean doSleep = processDatanodeOrExternalError();\n\n        synchronized (dataQueue) {\n          // wait for a packet to be sent.\n          while ((!shouldStop() && dataQueue.isEmpty()) || doSleep) {\n            long timeout = 1000;\n            if (stage == BlockConstructionStage.DATA_STREAMING) {\n              timeout = sendHeartbeat();\n            }\n            try {\n              dataQueue.wait(timeout);\n            } catch (InterruptedException  e) {\n              LOG.debug(\"Thread interrupted\", e);\n            }\n            doSleep = false;\n          }\n          if (shouldStop()) {\n            continue;\n          }\n          // get packet to be sent.\n          one = dataQueue.getFirst(); // regular data packet\n          SpanContext[] parents = one.getTraceParents();\n          if (parents != null && parents.length > 0) {\n            // The original code stored multiple parents in the DFSPacket, and\n            // use them ALL here when creating a new Span. We only use the\n            // last one FOR NOW. Moreover, we don't activate the Span for now.\n            scope = dfsClient.getTracer().\n                newScope(\"dataStreamer\", parents[0], false);\n            //scope.getSpan().setParents(parents);\n          }\n        }", "caller_log_raw_stripped": "[ENTRY] DataStreamer:run()\n  IF: one.isLastPacketInBlock() && ack confirmed\n  CALL: endBlock()", "callee_log_raw_stripped": "[ENTRY] DataStreamer:endBlock()\n  [INFO] Ending block {blockId}\n  EXIT", "caller_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:run()\n  IF: one.isLastPacketInBlock() && ack confirmed\n  CALL: endBlock()\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:run()\n  IF: one.isLastPacketInBlock() && ack confirmed\n  CALL: endBlock()\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "callee_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:endBlock()\n  [INFO] Ending block {blockId}\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:endBlock()\n  [INFO] Ending block {blockId}\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "parent_info": "node name is DataStreamer:run()node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:run()\n  IF: one.isLastPacketInBlock() && ack confirmed\n  CALL: endBlock()\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:run()\n  IF: one.isLastPacketInBlock() && ack confirmed\n  CALL: endBlock()\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```souce code:*/\n  @Override\n  public void run() {\n    TraceScope scope = null;\n    while (!streamerClosed && dfsClient.clientRunning) {\n      // if the Responder encountered an error, shutdown Responder\n      if (errorState.hasError()) {\n        closeResponder();\n      }\n\n      DFSPacket one;\n      try {\n        // process datanode IO errors if any\n        boolean doSleep = processDatanodeOrExternalError();\n\n        synchronized (dataQueue) {\n          // wait for a packet to be sent.\n          while ((!shouldStop() && dataQueue.isEmpty()) || doSleep) {\n            long timeout = 1000;\n            if (stage == BlockConstructionStage.DATA_STREAMING) {\n              timeout = sendHeartbeat();\n            }\n            try {\n              dataQueue.wait(timeout);\n            } catch (InterruptedException  e) {\n              LOG.debug(\"Thread interrupted\", e);\n            }\n            doSleep = false;\n          }\n          if (shouldStop()) {\n            continue;\n          }\n          // get packet to be sent.\n          one = dataQueue.getFirst(); // regular data packet\n          SpanContext[] parents = one.getTraceParents();\n          if (parents != null && parents.length > 0) {\n            // The original code stored multiple parents in the DFSPacket, and\n            // use them ALL here when creating a new Span. We only use the\n            // last one FOR NOW. Moreover, we don't activate the Span for now.\n            scope = dfsClient.getTracer().\n                newScope(\"dataStreamer\", parents[0], false);\n            //scope.getSpan().setParents(parents);\n          }\n        }\n\n        // The DataStreamer has to release the dataQueue before sleeping,\n        // otherwise it will cause the ResponseProcessor to accept the ACK delay.\n        try {\n          backOffIfNecessary();\n        } catch (InterruptedException e) {\n          LOG.debug(\"Thread interrupted\", e);\n        }\n\n        // get new block from namenode.\n        LOG.debug(\"stage={}, {}\", stage, this);\n\n        if (stage == BlockConstructionStage.PIPELINE_SETUP_CREATE) {\n          LOG.debug(\"Allocating new block: {}\", this);\n          setPipeline(nextBlockOutputStream());\n          initDataStreaming();\n        } else if (stage == BlockConstructionStage.PIPELINE_SETUP_APPEND) {\n          LOG.debug(\"Append to block {}\", block);", "child_info": "node name isDataStreamer:endBlock()node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:endBlock()\n  [INFO] Ending block {blockId}\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:endBlock()\n  [INFO] Ending block {blockId}\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```source code:}\n\n  protected void endBlock() {\n    LOG.debug(\"Closing old block {}\", block);\n    this.setName(\"DataStreamer for file \" + src);\n    closeResponder();\n    closeStream();\n    setPipeline(null, null, null);\n    stage = BlockConstructionStage.PIPELINE_SETUP_CREATE;\n  }\n\n  private boolean shouldStop() {\n    return streamerClosed || errorState.hasError() || !dfsClient.clientRunning;\n  }\n\n  /*\n   * streamer thread is the only thread that opens streams to datanode,\n   * and closes them. Any error recovery is also done by this thread.\n   */\n  @Override\n  public void run() {\n    TraceScope scope = null;\n    while (!streamerClosed && dfsClient.clientRunning) {\n      // if the Responder encountered an error, shutdown Responder\n      if (errorState.hasError()) {\n        closeResponder();\n      }\n\n      DFSPacket one;\n      try {\n        // process datanode IO errors if any\n        boolean doSleep = processDatanodeOrExternalError();\n\n        synchronized (dataQueue) {\n          // wait for a packet to be sent.\n          while ((!shouldStop() && dataQueue.isEmpty()) || doSleep) {\n            long timeout = 1000;\n            if (stage == BlockConstructionStage.DATA_STREAMING) {\n              timeout = sendHeartbeat();\n            }\n            try {\n              dataQueue.wait(timeout);\n            } catch (InterruptedException  e) {\n              LOG.debug(\"Thread interrupted\", e);\n            }\n            doSleep = false;\n          }\n          if (shouldStop()) {\n            continue;\n          }\n          // get packet to be sent.\n          one = dataQueue.getFirst(); // regular data packet\n          SpanContext[] parents = one.getTraceParents();\n          if (parents != null && parents.length > 0) {\n            // The original code stored multiple parents in the DFSPacket, and\n            // use them ALL here when creating a new Span. We only use the\n            // last one FOR NOW. Moreover, we don't activate the Span for now.\n            scope = dfsClient.getTracer().\n                newScope(\"dataStreamer\", parents[0], false);\n            //scope.getSpan().setParents(parents);\n          }\n        }"}
{"mp_id": "hdfs::DS::run->closeInternal", "caller": "DataStreamer:run()", "callee": "DataStreamer:closeInternal()", "gt": "valid", "stratum": "simple", "infeasible_reason": "", "rationale": "Unconditionally reachable in finally block.", "caller_code": "*/\n  @Override\n  public void run() {\n    TraceScope scope = null;\n    while (!streamerClosed && dfsClient.clientRunning) {\n      // if the Responder encountered an error, shutdown Responder\n      if (errorState.hasError()) {\n        closeResponder();\n      }\n\n      DFSPacket one;\n      try {\n        // process datanode IO errors if any\n        boolean doSleep = processDatanodeOrExternalError();\n\n        synchronized (dataQueue) {\n          // wait for a packet to be sent.\n          while ((!shouldStop() && dataQueue.isEmpty()) || doSleep) {\n            long timeout = 1000;\n            if (stage == BlockConstructionStage.DATA_STREAMING) {\n              timeout = sendHeartbeat();\n            }\n            try {\n              dataQueue.wait(timeout);\n            } catch (InterruptedException  e) {\n              LOG.debug(\"Thread interrupted\", e);\n            }\n            doSleep = false;\n          }\n          if (shouldStop()) {\n            continue;\n          }\n          // get packet to be sent.\n          one = dataQueue.getFirst(); // regular data packet\n          SpanContext[] parents = one.getTraceParents();\n          if (parents != null && parents.length > 0) {\n            // The original code stored multiple parents in the DFSPacket, and\n            // use them ALL here when creating a new Span. We only use the\n            // last one FOR NOW. Moreover, we don't activate the Span for now.\n            scope = dfsClient.getTracer().\n                newScope(\"dataStreamer\", parents[0], false);\n            //scope.getSpan().setParents(parents);\n          }\n        }\n\n        // The DataStreamer has to release the dataQueue before sleeping,\n        // otherwise it will cause the ResponseProcessor to accept the ACK delay.\n        try {\n          backOffIfNecessary();\n        } catch (InterruptedException e) {\n          LOG.debug(\"Thread interrupted\", e);\n        }\n\n        // get new block from namenode.\n        LOG.debug(\"stage={}, {}\", stage, this);\n\n        if (stage == BlockConstructionStage.PIPELINE_SETUP_CREATE) {\n          LOG.debug(\"Allocating new block: {}\", this);\n          setPipeline(nextBlockOutputStream());\n          initDataStreaming();\n        } else if (stage == BlockConstructionStage.PIPELINE_SETUP_APPEND) {\n          LOG.debug(\"Append to block {}\", block);", "callee_code": "}\n\n  private void closeInternal() {\n    closeResponder();       // close and join\n    closeStream();\n    streamerClosed = true;\n    release();\n    synchronized (dataQueue) {\n      dataQueue.notifyAll();\n    }\n  }\n\n  /**\n   * release the DFSPackets in the two queues\n   *\n   */\n  void release() {\n    synchronized (dataQueue) {\n      releaseBuffer(dataQueue, byteArrayManager);\n      releaseBuffer(ackQueue, byteArrayManager);\n    }\n  }\n\n  /**\n   * wait for the ack of seqno\n   *\n   * @param seqno the sequence number to be acked\n   * @throws IOException\n   */\n  void waitForAckedSeqno(long seqno) throws IOException {\n    try (TraceScope ignored = dfsClient.getTracer().\n        newScope(\"waitForAckedSeqno\")) {\n      LOG.debug(\"{} waiting for ack for: {}\", this, seqno);\n      int dnodes = nodes != null ? nodes.length : 3;\n      int writeTimeout = dfsClient.getDatanodeWriteTimeout(dnodes);\n      long begin = Time.monotonicNow();\n      try {\n        synchronized (dataQueue) {\n          while (!streamerClosed) {\n            checkClosed();\n            if (lastAckedSeqno >= seqno) {\n              break;\n            }\n            try {\n              dataQueue.wait(1000); // when we receive an ack, we notify on\n              long duration = Time.monotonicNow() - begin;\n              if (duration > writeTimeout) {\n                LOG.error(\"No ack received, took {}ms (threshold={}ms). \"\n                    + \"File being written: {}, block: {}, \"\n                    + \"Write pipeline datanodes: {}.\",\n                    duration, writeTimeout, src, block, nodes);\n                throw new InterruptedIOException(\"No ack received after \" +\n                    duration / 1000 + \"s and a timeout of \" +\n                    writeTimeout / 1000 + \"s\");\n              }\n              // dataQueue\n            } catch (InterruptedException ie) {\n              throw new InterruptedIOException(\n                  \"Interrupted while waiting for data to be acknowledged by pipeline\");\n            }\n          }\n        }", "caller_log_raw_stripped": "[ENTRY] DataStreamer:run()\n  finally BLOCK\n  CALL: closeInternal()", "callee_log_raw_stripped": "[ENTRY] DataStreamer:closeInternal()\n  IF: streamerClosed -> EXIT\n  closeResponder()\n  EXIT", "caller_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:run()\n  finally BLOCK\n  CALL: closeInternal()\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:run()\n  finally BLOCK\n  CALL: closeInternal()\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "callee_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:closeInternal()\n  IF: streamerClosed -> EXIT\n  closeResponder()\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:closeInternal()\n  IF: streamerClosed -> EXIT\n  closeResponder()\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "parent_info": "node name is DataStreamer:run()node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:run()\n  finally BLOCK\n  CALL: closeInternal()\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:run()\n  finally BLOCK\n  CALL: closeInternal()\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```souce code:*/\n  @Override\n  public void run() {\n    TraceScope scope = null;\n    while (!streamerClosed && dfsClient.clientRunning) {\n      // if the Responder encountered an error, shutdown Responder\n      if (errorState.hasError()) {\n        closeResponder();\n      }\n\n      DFSPacket one;\n      try {\n        // process datanode IO errors if any\n        boolean doSleep = processDatanodeOrExternalError();\n\n        synchronized (dataQueue) {\n          // wait for a packet to be sent.\n          while ((!shouldStop() && dataQueue.isEmpty()) || doSleep) {\n            long timeout = 1000;\n            if (stage == BlockConstructionStage.DATA_STREAMING) {\n              timeout = sendHeartbeat();\n            }\n            try {\n              dataQueue.wait(timeout);\n            } catch (InterruptedException  e) {\n              LOG.debug(\"Thread interrupted\", e);\n            }\n            doSleep = false;\n          }\n          if (shouldStop()) {\n            continue;\n          }\n          // get packet to be sent.\n          one = dataQueue.getFirst(); // regular data packet\n          SpanContext[] parents = one.getTraceParents();\n          if (parents != null && parents.length > 0) {\n            // The original code stored multiple parents in the DFSPacket, and\n            // use them ALL here when creating a new Span. We only use the\n            // last one FOR NOW. Moreover, we don't activate the Span for now.\n            scope = dfsClient.getTracer().\n                newScope(\"dataStreamer\", parents[0], false);\n            //scope.getSpan().setParents(parents);\n          }\n        }\n\n        // The DataStreamer has to release the dataQueue before sleeping,\n        // otherwise it will cause the ResponseProcessor to accept the ACK delay.\n        try {\n          backOffIfNecessary();\n        } catch (InterruptedException e) {\n          LOG.debug(\"Thread interrupted\", e);\n        }\n\n        // get new block from namenode.\n        LOG.debug(\"stage={}, {}\", stage, this);\n\n        if (stage == BlockConstructionStage.PIPELINE_SETUP_CREATE) {\n          LOG.debug(\"Allocating new block: {}\", this);\n          setPipeline(nextBlockOutputStream());\n          initDataStreaming();\n        } else if (stage == BlockConstructionStage.PIPELINE_SETUP_APPEND) {\n          LOG.debug(\"Append to block {}\", block);", "child_info": "node name isDataStreamer:closeInternal()node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:closeInternal()\n  IF: streamerClosed -> EXIT\n  closeResponder()\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:closeInternal()\n  IF: streamerClosed -> EXIT\n  closeResponder()\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```source code:}\n\n  private void closeInternal() {\n    closeResponder();       // close and join\n    closeStream();\n    streamerClosed = true;\n    release();\n    synchronized (dataQueue) {\n      dataQueue.notifyAll();\n    }\n  }\n\n  /**\n   * release the DFSPackets in the two queues\n   *\n   */\n  void release() {\n    synchronized (dataQueue) {\n      releaseBuffer(dataQueue, byteArrayManager);\n      releaseBuffer(ackQueue, byteArrayManager);\n    }\n  }\n\n  /**\n   * wait for the ack of seqno\n   *\n   * @param seqno the sequence number to be acked\n   * @throws IOException\n   */\n  void waitForAckedSeqno(long seqno) throws IOException {\n    try (TraceScope ignored = dfsClient.getTracer().\n        newScope(\"waitForAckedSeqno\")) {\n      LOG.debug(\"{} waiting for ack for: {}\", this, seqno);\n      int dnodes = nodes != null ? nodes.length : 3;\n      int writeTimeout = dfsClient.getDatanodeWriteTimeout(dnodes);\n      long begin = Time.monotonicNow();\n      try {\n        synchronized (dataQueue) {\n          while (!streamerClosed) {\n            checkClosed();\n            if (lastAckedSeqno >= seqno) {\n              break;\n            }\n            try {\n              dataQueue.wait(1000); // when we receive an ack, we notify on\n              long duration = Time.monotonicNow() - begin;\n              if (duration > writeTimeout) {\n                LOG.error(\"No ack received, took {}ms (threshold={}ms). \"\n                    + \"File being written: {}, block: {}, \"\n                    + \"Write pipeline datanodes: {}.\",\n                    duration, writeTimeout, src, block, nodes);\n                throw new InterruptedIOException(\"No ack received after \" +\n                    duration / 1000 + \"s and a timeout of \" +\n                    writeTimeout / 1000 + \"s\");\n              }\n              // dataQueue\n            } catch (InterruptedException ie) {\n              throw new InterruptedIOException(\n                  \"Interrupted while waiting for data to be acknowledged by pipeline\");\n            }\n          }\n        }"}
{"mp_id": "hdfs::DS::run->closeInternal-INFEASIBLE", "caller": "DataStreamer:run()", "callee": "DataStreamer:closeInternal()", "gt": "infeasible", "stratum": "simple", "infeasible_reason": "streamerClosed==true but callee path continues past guard: closeInternal checks if(streamerClosed) return; first.", "rationale": "", "caller_code": "*/\n  @Override\n  public void run() {\n    TraceScope scope = null;\n    while (!streamerClosed && dfsClient.clientRunning) {\n      // if the Responder encountered an error, shutdown Responder\n      if (errorState.hasError()) {\n        closeResponder();\n      }\n\n      DFSPacket one;\n      try {\n        // process datanode IO errors if any\n        boolean doSleep = processDatanodeOrExternalError();\n\n        synchronized (dataQueue) {\n          // wait for a packet to be sent.\n          while ((!shouldStop() && dataQueue.isEmpty()) || doSleep) {\n            long timeout = 1000;\n            if (stage == BlockConstructionStage.DATA_STREAMING) {\n              timeout = sendHeartbeat();\n            }\n            try {\n              dataQueue.wait(timeout);\n            } catch (InterruptedException  e) {\n              LOG.debug(\"Thread interrupted\", e);\n            }\n            doSleep = false;\n          }\n          if (shouldStop()) {\n            continue;\n          }\n          // get packet to be sent.\n          one = dataQueue.getFirst(); // regular data packet\n          SpanContext[] parents = one.getTraceParents();\n          if (parents != null && parents.length > 0) {\n            // The original code stored multiple parents in the DFSPacket, and\n            // use them ALL here when creating a new Span. We only use the\n            // last one FOR NOW. Moreover, we don't activate the Span for now.\n            scope = dfsClient.getTracer().\n                newScope(\"dataStreamer\", parents[0], false);\n            //scope.getSpan().setParents(parents);\n          }\n        }\n\n        // The DataStreamer has to release the dataQueue before sleeping,\n        // otherwise it will cause the ResponseProcessor to accept the ACK delay.\n        try {\n          backOffIfNecessary();\n        } catch (InterruptedException e) {\n          LOG.debug(\"Thread interrupted\", e);\n        }\n\n        // get new block from namenode.\n        LOG.debug(\"stage={}, {}\", stage, this);\n\n        if (stage == BlockConstructionStage.PIPELINE_SETUP_CREATE) {\n          LOG.debug(\"Allocating new block: {}\", this);\n          setPipeline(nextBlockOutputStream());\n          initDataStreaming();\n        } else if (stage == BlockConstructionStage.PIPELINE_SETUP_APPEND) {\n          LOG.debug(\"Append to block {}\", block);", "callee_code": "}\n\n  private void closeInternal() {\n    closeResponder();       // close and join\n    closeStream();\n    streamerClosed = true;\n    release();\n    synchronized (dataQueue) {\n      dataQueue.notifyAll();\n    }\n  }\n\n  /**\n   * release the DFSPackets in the two queues\n   *\n   */\n  void release() {\n    synchronized (dataQueue) {\n      releaseBuffer(dataQueue, byteArrayManager);\n      releaseBuffer(ackQueue, byteArrayManager);\n    }\n  }\n\n  /**\n   * wait for the ack of seqno\n   *\n   * @param seqno the sequence number to be acked\n   * @throws IOException\n   */\n  void waitForAckedSeqno(long seqno) throws IOException {\n    try (TraceScope ignored = dfsClient.getTracer().\n        newScope(\"waitForAckedSeqno\")) {\n      LOG.debug(\"{} waiting for ack for: {}\", this, seqno);\n      int dnodes = nodes != null ? nodes.length : 3;\n      int writeTimeout = dfsClient.getDatanodeWriteTimeout(dnodes);\n      long begin = Time.monotonicNow();\n      try {\n        synchronized (dataQueue) {\n          while (!streamerClosed) {\n            checkClosed();\n            if (lastAckedSeqno >= seqno) {\n              break;\n            }\n            try {\n              dataQueue.wait(1000); // when we receive an ack, we notify on\n              long duration = Time.monotonicNow() - begin;\n              if (duration > writeTimeout) {\n                LOG.error(\"No ack received, took {}ms (threshold={}ms). \"\n                    + \"File being written: {}, block: {}, \"\n                    + \"Write pipeline datanodes: {}.\",\n                    duration, writeTimeout, src, block, nodes);\n                throw new InterruptedIOException(\"No ack received after \" +\n                    duration / 1000 + \"s and a timeout of \" +\n                    writeTimeout / 1000 + \"s\");\n              }\n              // dataQueue\n            } catch (InterruptedException ie) {\n              throw new InterruptedIOException(\n                  \"Interrupted while waiting for data to be acknowledged by pipeline\");\n            }\n          }\n        }", "caller_log_raw_stripped": "[ENTRY] DataStreamer:run()\n  finally: streamerClosed==true \n  CALL: closeInternal()", "callee_log_raw_stripped": "[ENTRY] DataStreamer:closeInternal()\n  IF: streamerClosed -> EXIT (guard line 1)", "caller_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:run()\n  finally: streamerClosed==true \n  CALL: closeInternal()\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:run()\n  finally: streamerClosed==true \n  CALL: closeInternal()\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "callee_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:closeInternal()\n  IF: streamerClosed -> EXIT (guard line 1)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:closeInternal()\n  IF: streamerClosed -> EXIT (guard line 1)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "parent_info": "node name is DataStreamer:run()node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:run()\n  finally: streamerClosed==true \n  CALL: closeInternal()\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:run()\n  finally: streamerClosed==true \n  CALL: closeInternal()\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```souce code:*/\n  @Override\n  public void run() {\n    TraceScope scope = null;\n    while (!streamerClosed && dfsClient.clientRunning) {\n      // if the Responder encountered an error, shutdown Responder\n      if (errorState.hasError()) {\n        closeResponder();\n      }\n\n      DFSPacket one;\n      try {\n        // process datanode IO errors if any\n        boolean doSleep = processDatanodeOrExternalError();\n\n        synchronized (dataQueue) {\n          // wait for a packet to be sent.\n          while ((!shouldStop() && dataQueue.isEmpty()) || doSleep) {\n            long timeout = 1000;\n            if (stage == BlockConstructionStage.DATA_STREAMING) {\n              timeout = sendHeartbeat();\n            }\n            try {\n              dataQueue.wait(timeout);\n            } catch (InterruptedException  e) {\n              LOG.debug(\"Thread interrupted\", e);\n            }\n            doSleep = false;\n          }\n          if (shouldStop()) {\n            continue;\n          }\n          // get packet to be sent.\n          one = dataQueue.getFirst(); // regular data packet\n          SpanContext[] parents = one.getTraceParents();\n          if (parents != null && parents.length > 0) {\n            // The original code stored multiple parents in the DFSPacket, and\n            // use them ALL here when creating a new Span. We only use the\n            // last one FOR NOW. Moreover, we don't activate the Span for now.\n            scope = dfsClient.getTracer().\n                newScope(\"dataStreamer\", parents[0], false);\n            //scope.getSpan().setParents(parents);\n          }\n        }\n\n        // The DataStreamer has to release the dataQueue before sleeping,\n        // otherwise it will cause the ResponseProcessor to accept the ACK delay.\n        try {\n          backOffIfNecessary();\n        } catch (InterruptedException e) {\n          LOG.debug(\"Thread interrupted\", e);\n        }\n\n        // get new block from namenode.\n        LOG.debug(\"stage={}, {}\", stage, this);\n\n        if (stage == BlockConstructionStage.PIPELINE_SETUP_CREATE) {\n          LOG.debug(\"Allocating new block: {}\", this);\n          setPipeline(nextBlockOutputStream());\n          initDataStreaming();\n        } else if (stage == BlockConstructionStage.PIPELINE_SETUP_APPEND) {\n          LOG.debug(\"Append to block {}\", block);", "child_info": "node name isDataStreamer:closeInternal()node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:closeInternal()\n  IF: streamerClosed -> EXIT (guard line 1)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:closeInternal()\n  IF: streamerClosed -> EXIT (guard line 1)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```source code:}\n\n  private void closeInternal() {\n    closeResponder();       // close and join\n    closeStream();\n    streamerClosed = true;\n    release();\n    synchronized (dataQueue) {\n      dataQueue.notifyAll();\n    }\n  }\n\n  /**\n   * release the DFSPackets in the two queues\n   *\n   */\n  void release() {\n    synchronized (dataQueue) {\n      releaseBuffer(dataQueue, byteArrayManager);\n      releaseBuffer(ackQueue, byteArrayManager);\n    }\n  }\n\n  /**\n   * wait for the ack of seqno\n   *\n   * @param seqno the sequence number to be acked\n   * @throws IOException\n   */\n  void waitForAckedSeqno(long seqno) throws IOException {\n    try (TraceScope ignored = dfsClient.getTracer().\n        newScope(\"waitForAckedSeqno\")) {\n      LOG.debug(\"{} waiting for ack for: {}\", this, seqno);\n      int dnodes = nodes != null ? nodes.length : 3;\n      int writeTimeout = dfsClient.getDatanodeWriteTimeout(dnodes);\n      long begin = Time.monotonicNow();\n      try {\n        synchronized (dataQueue) {\n          while (!streamerClosed) {\n            checkClosed();\n            if (lastAckedSeqno >= seqno) {\n              break;\n            }\n            try {\n              dataQueue.wait(1000); // when we receive an ack, we notify on\n              long duration = Time.monotonicNow() - begin;\n              if (duration > writeTimeout) {\n                LOG.error(\"No ack received, took {}ms (threshold={}ms). \"\n                    + \"File being written: {}, block: {}, \"\n                    + \"Write pipeline datanodes: {}.\",\n                    duration, writeTimeout, src, block, nodes);\n                throw new InterruptedIOException(\"No ack received after \" +\n                    duration / 1000 + \"s and a timeout of \" +\n                    writeTimeout / 1000 + \"s\");\n              }\n              // dataQueue\n            } catch (InterruptedException ie) {\n              throw new InterruptedIOException(\n                  \"Interrupted while waiting for data to be acknowledged by pipeline\");\n            }\n          }\n        }"}
{"mp_id": "hdfs::DS::run->nextBlockOutputStream", "caller": "DataStreamer:run()", "callee": "DataStreamer:nextBlockOutputStream()", "gt": "valid", "stratum": "simple", "infeasible_reason": "", "rationale": "After endBlock(), creates output stream for next block.", "caller_code": "*/\n  @Override\n  public void run() {\n    TraceScope scope = null;\n    while (!streamerClosed && dfsClient.clientRunning) {\n      // if the Responder encountered an error, shutdown Responder\n      if (errorState.hasError()) {\n        closeResponder();\n      }\n\n      DFSPacket one;\n      try {\n        // process datanode IO errors if any\n        boolean doSleep = processDatanodeOrExternalError();\n\n        synchronized (dataQueue) {\n          // wait for a packet to be sent.\n          while ((!shouldStop() && dataQueue.isEmpty()) || doSleep) {\n            long timeout = 1000;\n            if (stage == BlockConstructionStage.DATA_STREAMING) {\n              timeout = sendHeartbeat();\n            }\n            try {\n              dataQueue.wait(timeout);\n            } catch (InterruptedException  e) {\n              LOG.debug(\"Thread interrupted\", e);\n            }\n            doSleep = false;\n          }\n          if (shouldStop()) {\n            continue;\n          }\n          // get packet to be sent.\n          one = dataQueue.getFirst(); // regular data packet\n          SpanContext[] parents = one.getTraceParents();\n          if (parents != null && parents.length > 0) {\n            // The original code stored multiple parents in the DFSPacket, and\n            // use them ALL here when creating a new Span. We only use the\n            // last one FOR NOW. Moreover, we don't activate the Span for now.\n            scope = dfsClient.getTracer().\n                newScope(\"dataStreamer\", parents[0], false);\n            //scope.getSpan().setParents(parents);\n          }\n        }\n\n        // The DataStreamer has to release the dataQueue before sleeping,\n        // otherwise it will cause the ResponseProcessor to accept the ACK delay.\n        try {\n          backOffIfNecessary();\n        } catch (InterruptedException e) {\n          LOG.debug(\"Thread interrupted\", e);\n        }\n\n        // get new block from namenode.\n        LOG.debug(\"stage={}, {}\", stage, this);\n\n        if (stage == BlockConstructionStage.PIPELINE_SETUP_CREATE) {\n          LOG.debug(\"Allocating new block: {}\", this);\n          setPipeline(nextBlockOutputStream());\n          initDataStreaming();\n        } else if (stage == BlockConstructionStage.PIPELINE_SETUP_APPEND) {\n          LOG.debug(\"Append to block {}\", block);", "callee_code": "* Returns the list of target datanodes.\n   */\n  protected LocatedBlock nextBlockOutputStream() throws IOException {\n    LocatedBlock lb;\n    DatanodeInfo[] nodes;\n    StorageType[] nextStorageTypes;\n    String[] nextStorageIDs;\n    int count = dfsClient.getConf().getNumBlockWriteRetry();\n    boolean success;\n    final ExtendedBlock oldBlock = block.getCurrentBlock();\n    do {\n      errorState.resetInternalError();\n      lastException.clear();\n\n      DatanodeInfo[] excluded = getExcludedNodes();\n      lb = locateFollowingBlock(\n          excluded.length > 0 ? excluded : null, oldBlock);\n      block.setCurrentBlock(lb.getBlock());\n      block.setNumBytes(0);\n      bytesSent = 0;\n      accessToken = lb.getBlockToken();\n      nodes = lb.getLocations();\n      nextStorageTypes = lb.getStorageTypes();\n      nextStorageIDs = lb.getStorageIDs();\n\n      // Connect to first DataNode in the list.\n      success = createBlockOutputStream(nodes, nextStorageTypes, nextStorageIDs,\n          0L, false);\n\n      if (!success) {\n        LOG.warn(\"Abandoning \" + block);\n        dfsClient.namenode.abandonBlock(block.getCurrentBlock(),\n            stat.getFileId(), src, dfsClient.clientName);\n        block.setCurrentBlock(null);\n        final DatanodeInfo badNode = nodes[errorState.getBadNodeIndex()];\n        LOG.warn(\"Excluding datanode \" + badNode);\n        excludedNodes.put(badNode, badNode);\n      }\n    } while (!success && --count >= 0);\n\n    if (!success) {\n      throw new IOException(\"Unable to create new block.\");\n    }\n    return lb;\n  }\n\n  // connects to the first datanode in the pipeline\n  // Returns true if success, otherwise return failure.\n  //\n  boolean createBlockOutputStream(DatanodeInfo[] nodes,\n      StorageType[] nodeStorageTypes, String[] nodeStorageIDs,\n      long newGS, boolean recoveryFlag) {\n    if (nodes.length == 0) {\n      LOG.info(\"nodes are empty for write pipeline of \" + block);\n      return false;\n    }\n    String firstBadLink = \"\";\n    boolean checkRestart = false;\n    if (LOG.isDebugEnabled()) {\n      LOG.debug(\"pipeline = \" + Arrays.toString(nodes) + \", \" + this);\n    }", "caller_log_raw_stripped": "[ENTRY] DataStreamer:run()\n  after endBlock()\n  CALL: nextBlockOutputStream()", "callee_log_raw_stripped": "[ENTRY] DataStreamer:nextBlockOutputStream()\n  WHILE retry loop\n  EXIT", "caller_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:run()\n  after endBlock()\n  CALL: nextBlockOutputStream()\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:run()\n  after endBlock()\n  CALL: nextBlockOutputStream()\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "callee_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:nextBlockOutputStream()\n  WHILE retry loop\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:nextBlockOutputStream()\n  WHILE retry loop\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "parent_info": "node name is DataStreamer:run()node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:run()\n  after endBlock()\n  CALL: nextBlockOutputStream()\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:run()\n  after endBlock()\n  CALL: nextBlockOutputStream()\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```souce code:*/\n  @Override\n  public void run() {\n    TraceScope scope = null;\n    while (!streamerClosed && dfsClient.clientRunning) {\n      // if the Responder encountered an error, shutdown Responder\n      if (errorState.hasError()) {\n        closeResponder();\n      }\n\n      DFSPacket one;\n      try {\n        // process datanode IO errors if any\n        boolean doSleep = processDatanodeOrExternalError();\n\n        synchronized (dataQueue) {\n          // wait for a packet to be sent.\n          while ((!shouldStop() && dataQueue.isEmpty()) || doSleep) {\n            long timeout = 1000;\n            if (stage == BlockConstructionStage.DATA_STREAMING) {\n              timeout = sendHeartbeat();\n            }\n            try {\n              dataQueue.wait(timeout);\n            } catch (InterruptedException  e) {\n              LOG.debug(\"Thread interrupted\", e);\n            }\n            doSleep = false;\n          }\n          if (shouldStop()) {\n            continue;\n          }\n          // get packet to be sent.\n          one = dataQueue.getFirst(); // regular data packet\n          SpanContext[] parents = one.getTraceParents();\n          if (parents != null && parents.length > 0) {\n            // The original code stored multiple parents in the DFSPacket, and\n            // use them ALL here when creating a new Span. We only use the\n            // last one FOR NOW. Moreover, we don't activate the Span for now.\n            scope = dfsClient.getTracer().\n                newScope(\"dataStreamer\", parents[0], false);\n            //scope.getSpan().setParents(parents);\n          }\n        }\n\n        // The DataStreamer has to release the dataQueue before sleeping,\n        // otherwise it will cause the ResponseProcessor to accept the ACK delay.\n        try {\n          backOffIfNecessary();\n        } catch (InterruptedException e) {\n          LOG.debug(\"Thread interrupted\", e);\n        }\n\n        // get new block from namenode.\n        LOG.debug(\"stage={}, {}\", stage, this);\n\n        if (stage == BlockConstructionStage.PIPELINE_SETUP_CREATE) {\n          LOG.debug(\"Allocating new block: {}\", this);\n          setPipeline(nextBlockOutputStream());\n          initDataStreaming();\n        } else if (stage == BlockConstructionStage.PIPELINE_SETUP_APPEND) {\n          LOG.debug(\"Append to block {}\", block);", "child_info": "node name isDataStreamer:nextBlockOutputStream()node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:nextBlockOutputStream()\n  WHILE retry loop\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:nextBlockOutputStream()\n  WHILE retry loop\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```source code:* Returns the list of target datanodes.\n   */\n  protected LocatedBlock nextBlockOutputStream() throws IOException {\n    LocatedBlock lb;\n    DatanodeInfo[] nodes;\n    StorageType[] nextStorageTypes;\n    String[] nextStorageIDs;\n    int count = dfsClient.getConf().getNumBlockWriteRetry();\n    boolean success;\n    final ExtendedBlock oldBlock = block.getCurrentBlock();\n    do {\n      errorState.resetInternalError();\n      lastException.clear();\n\n      DatanodeInfo[] excluded = getExcludedNodes();\n      lb = locateFollowingBlock(\n          excluded.length > 0 ? excluded : null, oldBlock);\n      block.setCurrentBlock(lb.getBlock());\n      block.setNumBytes(0);\n      bytesSent = 0;\n      accessToken = lb.getBlockToken();\n      nodes = lb.getLocations();\n      nextStorageTypes = lb.getStorageTypes();\n      nextStorageIDs = lb.getStorageIDs();\n\n      // Connect to first DataNode in the list.\n      success = createBlockOutputStream(nodes, nextStorageTypes, nextStorageIDs,\n          0L, false);\n\n      if (!success) {\n        LOG.warn(\"Abandoning \" + block);\n        dfsClient.namenode.abandonBlock(block.getCurrentBlock(),\n            stat.getFileId(), src, dfsClient.clientName);\n        block.setCurrentBlock(null);\n        final DatanodeInfo badNode = nodes[errorState.getBadNodeIndex()];\n        LOG.warn(\"Excluding datanode \" + badNode);\n        excludedNodes.put(badNode, badNode);\n      }\n    } while (!success && --count >= 0);\n\n    if (!success) {\n      throw new IOException(\"Unable to create new block.\");\n    }\n    return lb;\n  }\n\n  // connects to the first datanode in the pipeline\n  // Returns true if success, otherwise return failure.\n  //\n  boolean createBlockOutputStream(DatanodeInfo[] nodes,\n      StorageType[] nodeStorageTypes, String[] nodeStorageIDs,\n      long newGS, boolean recoveryFlag) {\n    if (nodes.length == 0) {\n      LOG.info(\"nodes are empty for write pipeline of \" + block);\n      return false;\n    }\n    String firstBadLink = \"\";\n    boolean checkRestart = false;\n    if (LOG.isDebugEnabled()) {\n      LOG.debug(\"pipeline = \" + Arrays.toString(nodes) + \", \" + this);\n    }"}
{"mp_id": "hdfs::DS::nbo->createBlockOutputStream", "caller": "DataStreamer:nextBlockOutputStream()", "callee": "DataStreamer:createBlockOutputStream(java.util.List,int,long)", "gt": "valid", "stratum": "simple", "infeasible_reason": "", "rationale": "nextBlockOutputStream calls createBlockOutputStream in retry loop.", "caller_code": "* Returns the list of target datanodes.\n   */\n  protected LocatedBlock nextBlockOutputStream() throws IOException {\n    LocatedBlock lb;\n    DatanodeInfo[] nodes;\n    StorageType[] nextStorageTypes;\n    String[] nextStorageIDs;\n    int count = dfsClient.getConf().getNumBlockWriteRetry();\n    boolean success;\n    final ExtendedBlock oldBlock = block.getCurrentBlock();\n    do {\n      errorState.resetInternalError();\n      lastException.clear();\n\n      DatanodeInfo[] excluded = getExcludedNodes();\n      lb = locateFollowingBlock(\n          excluded.length > 0 ? excluded : null, oldBlock);\n      block.setCurrentBlock(lb.getBlock());\n      block.setNumBytes(0);\n      bytesSent = 0;\n      accessToken = lb.getBlockToken();\n      nodes = lb.getLocations();\n      nextStorageTypes = lb.getStorageTypes();\n      nextStorageIDs = lb.getStorageIDs();\n\n      // Connect to first DataNode in the list.\n      success = createBlockOutputStream(nodes, nextStorageTypes, nextStorageIDs,\n          0L, false);\n\n      if (!success) {\n        LOG.warn(\"Abandoning \" + block);\n        dfsClient.namenode.abandonBlock(block.getCurrentBlock(),\n            stat.getFileId(), src, dfsClient.clientName);\n        block.setCurrentBlock(null);\n        final DatanodeInfo badNode = nodes[errorState.getBadNodeIndex()];\n        LOG.warn(\"Excluding datanode \" + badNode);\n        excludedNodes.put(badNode, badNode);\n      }\n    } while (!success && --count >= 0);\n\n    if (!success) {\n      throw new IOException(\"Unable to create new block.\");\n    }\n    return lb;\n  }\n\n  // connects to the first datanode in the pipeline\n  // Returns true if success, otherwise return failure.\n  //\n  boolean createBlockOutputStream(DatanodeInfo[] nodes,\n      StorageType[] nodeStorageTypes, String[] nodeStorageIDs,\n      long newGS, boolean recoveryFlag) {\n    if (nodes.length == 0) {\n      LOG.info(\"nodes are empty for write pipeline of \" + block);\n      return false;\n    }\n    String firstBadLink = \"\";\n    boolean checkRestart = false;\n    if (LOG.isDebugEnabled()) {\n      LOG.debug(\"pipeline = \" + Arrays.toString(nodes) + \", \" + this);\n    }", "callee_code": "// Returns true if success, otherwise return failure.\n  //\n  boolean createBlockOutputStream(DatanodeInfo[] nodes,\n      StorageType[] nodeStorageTypes, String[] nodeStorageIDs,\n      long newGS, boolean recoveryFlag) {\n    if (nodes.length == 0) {\n      LOG.info(\"nodes are empty for write pipeline of \" + block);\n      return false;\n    }\n    String firstBadLink = \"\";\n    boolean checkRestart = false;\n    if (LOG.isDebugEnabled()) {\n      LOG.debug(\"pipeline = \" + Arrays.toString(nodes) + \", \" + this);\n    }\n\n    // persist blocks on namenode on next flush\n    persistBlocks.set(true);\n\n    int refetchEncryptionKey = 1;\n    while (true) {\n      boolean result = false;\n      DataOutputStream out = null;\n      try {\n        assert null == s : \"Previous socket unclosed\";\n        assert null == blockReplyStream : \"Previous blockReplyStream unclosed\";\n        s = createSocketForPipeline(nodes[0], nodes.length, dfsClient);\n        long writeTimeout = dfsClient.getDatanodeWriteTimeout(nodes.length);\n        long readTimeout = dfsClient.getDatanodeReadTimeout(nodes.length);\n\n        OutputStream unbufOut = NetUtils.getOutputStream(s, writeTimeout);\n        InputStream unbufIn = NetUtils.getInputStream(s, readTimeout);\n        IOStreamPair saslStreams = dfsClient.saslClient.socketSend(s,\n            unbufOut, unbufIn, dfsClient, accessToken, nodes[0]);\n        unbufOut = saslStreams.out;\n        unbufIn = saslStreams.in;\n        out = new DataOutputStream(new BufferedOutputStream(unbufOut,\n            DFSUtilClient.getSmallBufferSize(dfsClient.getConfiguration())));\n        blockReplyStream = new DataInputStream(unbufIn);\n\n        //\n        // Xmit header info to datanode\n        //\n\n        BlockConstructionStage bcs = recoveryFlag ?\n            stage.getRecoveryStage() : stage;\n\n        // We cannot change the block length in 'block' as it counts the number\n        // of bytes ack'ed.\n        ExtendedBlock blockCopy = block.getCurrentBlock();\n        blockCopy.setNumBytes(stat.getBlockSize());\n\n        boolean[] targetPinnings = getPinnings(nodes);\n        // send the request\n        new Sender(out).writeBlock(blockCopy, nodeStorageTypes[0], accessToken,\n            dfsClient.clientName, nodes, nodeStorageTypes, null, bcs,\n            nodes.length, block.getNumBytes(), bytesSent, newGS,\n            checksum4WriteBlock, cachingStrategy.get(), isLazyPersistFile,\n            (targetPinnings != null && targetPinnings[0]), targetPinnings,\n            nodeStorageIDs[0], nodeStorageIDs);\n\n        // receive ack for connect\n        BlockOpResponseProto resp = BlockOpResponseProto.parseFrom(", "caller_log_raw_stripped": "[ENTRY] DataStreamer:nextBlockOutputStream()\n  WHILE: retry\n  IF: !errorState.isRestartingNode()\n  CALL: createBlockOutputStream(nodes,...)", "callee_log_raw_stripped": "[ENTRY] DataStreamer:createBlockOutputStream(List,...)\n  IF: connection fail -> setInternalError()\n  EXIT", "caller_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:nextBlockOutputStream()\n  WHILE: retry\n  IF: !errorState.isRestartingNode()\n  CALL: createBlockOutputStream(nodes,...)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:nextBlockOutputStream()\n  WHILE: retry\n  IF: !errorState.isRestartingNode()\n  CALL: createBlockOutputStream(nodes,...)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "callee_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:createBlockOutputStream(List,...)\n  IF: connection fail -> setInternalError()\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:createBlockOutputStream(List,...)\n  IF: connection fail -> setInternalError()\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "parent_info": "node name is DataStreamer:nextBlockOutputStream()node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:nextBlockOutputStream()\n  WHILE: retry\n  IF: !errorState.isRestartingNode()\n  CALL: createBlockOutputStream(nodes,...)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:nextBlockOutputStream()\n  WHILE: retry\n  IF: !errorState.isRestartingNode()\n  CALL: createBlockOutputStream(nodes,...)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```souce code:* Returns the list of target datanodes.\n   */\n  protected LocatedBlock nextBlockOutputStream() throws IOException {\n    LocatedBlock lb;\n    DatanodeInfo[] nodes;\n    StorageType[] nextStorageTypes;\n    String[] nextStorageIDs;\n    int count = dfsClient.getConf().getNumBlockWriteRetry();\n    boolean success;\n    final ExtendedBlock oldBlock = block.getCurrentBlock();\n    do {\n      errorState.resetInternalError();\n      lastException.clear();\n\n      DatanodeInfo[] excluded = getExcludedNodes();\n      lb = locateFollowingBlock(\n          excluded.length > 0 ? excluded : null, oldBlock);\n      block.setCurrentBlock(lb.getBlock());\n      block.setNumBytes(0);\n      bytesSent = 0;\n      accessToken = lb.getBlockToken();\n      nodes = lb.getLocations();\n      nextStorageTypes = lb.getStorageTypes();\n      nextStorageIDs = lb.getStorageIDs();\n\n      // Connect to first DataNode in the list.\n      success = createBlockOutputStream(nodes, nextStorageTypes, nextStorageIDs,\n          0L, false);\n\n      if (!success) {\n        LOG.warn(\"Abandoning \" + block);\n        dfsClient.namenode.abandonBlock(block.getCurrentBlock(),\n            stat.getFileId(), src, dfsClient.clientName);\n        block.setCurrentBlock(null);\n        final DatanodeInfo badNode = nodes[errorState.getBadNodeIndex()];\n        LOG.warn(\"Excluding datanode \" + badNode);\n        excludedNodes.put(badNode, badNode);\n      }\n    } while (!success && --count >= 0);\n\n    if (!success) {\n      throw new IOException(\"Unable to create new block.\");\n    }\n    return lb;\n  }\n\n  // connects to the first datanode in the pipeline\n  // Returns true if success, otherwise return failure.\n  //\n  boolean createBlockOutputStream(DatanodeInfo[] nodes,\n      StorageType[] nodeStorageTypes, String[] nodeStorageIDs,\n      long newGS, boolean recoveryFlag) {\n    if (nodes.length == 0) {\n      LOG.info(\"nodes are empty for write pipeline of \" + block);\n      return false;\n    }\n    String firstBadLink = \"\";\n    boolean checkRestart = false;\n    if (LOG.isDebugEnabled()) {\n      LOG.debug(\"pipeline = \" + Arrays.toString(nodes) + \", \" + this);\n    }", "child_info": "node name isDataStreamer:createBlockOutputStream(java.util.List,int,long)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:createBlockOutputStream(List,...)\n  IF: connection fail -> setInternalError()\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:createBlockOutputStream(List,...)\n  IF: connection fail -> setInternalError()\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```source code:// Returns true if success, otherwise return failure.\n  //\n  boolean createBlockOutputStream(DatanodeInfo[] nodes,\n      StorageType[] nodeStorageTypes, String[] nodeStorageIDs,\n      long newGS, boolean recoveryFlag) {\n    if (nodes.length == 0) {\n      LOG.info(\"nodes are empty for write pipeline of \" + block);\n      return false;\n    }\n    String firstBadLink = \"\";\n    boolean checkRestart = false;\n    if (LOG.isDebugEnabled()) {\n      LOG.debug(\"pipeline = \" + Arrays.toString(nodes) + \", \" + this);\n    }\n\n    // persist blocks on namenode on next flush\n    persistBlocks.set(true);\n\n    int refetchEncryptionKey = 1;\n    while (true) {\n      boolean result = false;\n      DataOutputStream out = null;\n      try {\n        assert null == s : \"Previous socket unclosed\";\n        assert null == blockReplyStream : \"Previous blockReplyStream unclosed\";\n        s = createSocketForPipeline(nodes[0], nodes.length, dfsClient);\n        long writeTimeout = dfsClient.getDatanodeWriteTimeout(nodes.length);\n        long readTimeout = dfsClient.getDatanodeReadTimeout(nodes.length);\n\n        OutputStream unbufOut = NetUtils.getOutputStream(s, writeTimeout);\n        InputStream unbufIn = NetUtils.getInputStream(s, readTimeout);\n        IOStreamPair saslStreams = dfsClient.saslClient.socketSend(s,\n            unbufOut, unbufIn, dfsClient, accessToken, nodes[0]);\n        unbufOut = saslStreams.out;\n        unbufIn = saslStreams.in;\n        out = new DataOutputStream(new BufferedOutputStream(unbufOut,\n            DFSUtilClient.getSmallBufferSize(dfsClient.getConfiguration())));\n        blockReplyStream = new DataInputStream(unbufIn);\n\n        //\n        // Xmit header info to datanode\n        //\n\n        BlockConstructionStage bcs = recoveryFlag ?\n            stage.getRecoveryStage() : stage;\n\n        // We cannot change the block length in 'block' as it counts the number\n        // of bytes ack'ed.\n        ExtendedBlock blockCopy = block.getCurrentBlock();\n        blockCopy.setNumBytes(stat.getBlockSize());\n\n        boolean[] targetPinnings = getPinnings(nodes);\n        // send the request\n        new Sender(out).writeBlock(blockCopy, nodeStorageTypes[0], accessToken,\n            dfsClient.clientName, nodes, nodeStorageTypes, null, bcs,\n            nodes.length, block.getNumBytes(), bytesSent, newGS,\n            checksum4WriteBlock, cachingStrategy.get(), isLazyPersistFile,\n            (targetPinnings != null && targetPinnings[0]), targetPinnings,\n            nodeStorageIDs[0], nodeStorageIDs);\n\n        // receive ack for connect\n        BlockOpResponseProto resp = BlockOpResponseProto.parseFrom("}
{"mp_id": "hdfs::DS::nbo->createBlockOutputStream-INFEASIBLE", "caller": "DataStreamer:nextBlockOutputStream()", "callee": "DataStreamer:createBlockOutputStream(java.util.List,int,long)", "gt": "infeasible", "stratum": "simple", "infeasible_reason": "errorState.isRestartingNode()==true — loop continues without calling createBlockOutputStream.", "rationale": "", "caller_code": "* Returns the list of target datanodes.\n   */\n  protected LocatedBlock nextBlockOutputStream() throws IOException {\n    LocatedBlock lb;\n    DatanodeInfo[] nodes;\n    StorageType[] nextStorageTypes;\n    String[] nextStorageIDs;\n    int count = dfsClient.getConf().getNumBlockWriteRetry();\n    boolean success;\n    final ExtendedBlock oldBlock = block.getCurrentBlock();\n    do {\n      errorState.resetInternalError();\n      lastException.clear();\n\n      DatanodeInfo[] excluded = getExcludedNodes();\n      lb = locateFollowingBlock(\n          excluded.length > 0 ? excluded : null, oldBlock);\n      block.setCurrentBlock(lb.getBlock());\n      block.setNumBytes(0);\n      bytesSent = 0;\n      accessToken = lb.getBlockToken();\n      nodes = lb.getLocations();\n      nextStorageTypes = lb.getStorageTypes();\n      nextStorageIDs = lb.getStorageIDs();\n\n      // Connect to first DataNode in the list.\n      success = createBlockOutputStream(nodes, nextStorageTypes, nextStorageIDs,\n          0L, false);\n\n      if (!success) {\n        LOG.warn(\"Abandoning \" + block);\n        dfsClient.namenode.abandonBlock(block.getCurrentBlock(),\n            stat.getFileId(), src, dfsClient.clientName);\n        block.setCurrentBlock(null);\n        final DatanodeInfo badNode = nodes[errorState.getBadNodeIndex()];\n        LOG.warn(\"Excluding datanode \" + badNode);\n        excludedNodes.put(badNode, badNode);\n      }\n    } while (!success && --count >= 0);\n\n    if (!success) {\n      throw new IOException(\"Unable to create new block.\");\n    }\n    return lb;\n  }\n\n  // connects to the first datanode in the pipeline\n  // Returns true if success, otherwise return failure.\n  //\n  boolean createBlockOutputStream(DatanodeInfo[] nodes,\n      StorageType[] nodeStorageTypes, String[] nodeStorageIDs,\n      long newGS, boolean recoveryFlag) {\n    if (nodes.length == 0) {\n      LOG.info(\"nodes are empty for write pipeline of \" + block);\n      return false;\n    }\n    String firstBadLink = \"\";\n    boolean checkRestart = false;\n    if (LOG.isDebugEnabled()) {\n      LOG.debug(\"pipeline = \" + Arrays.toString(nodes) + \", \" + this);\n    }", "callee_code": "// Returns true if success, otherwise return failure.\n  //\n  boolean createBlockOutputStream(DatanodeInfo[] nodes,\n      StorageType[] nodeStorageTypes, String[] nodeStorageIDs,\n      long newGS, boolean recoveryFlag) {\n    if (nodes.length == 0) {\n      LOG.info(\"nodes are empty for write pipeline of \" + block);\n      return false;\n    }\n    String firstBadLink = \"\";\n    boolean checkRestart = false;\n    if (LOG.isDebugEnabled()) {\n      LOG.debug(\"pipeline = \" + Arrays.toString(nodes) + \", \" + this);\n    }\n\n    // persist blocks on namenode on next flush\n    persistBlocks.set(true);\n\n    int refetchEncryptionKey = 1;\n    while (true) {\n      boolean result = false;\n      DataOutputStream out = null;\n      try {\n        assert null == s : \"Previous socket unclosed\";\n        assert null == blockReplyStream : \"Previous blockReplyStream unclosed\";\n        s = createSocketForPipeline(nodes[0], nodes.length, dfsClient);\n        long writeTimeout = dfsClient.getDatanodeWriteTimeout(nodes.length);\n        long readTimeout = dfsClient.getDatanodeReadTimeout(nodes.length);\n\n        OutputStream unbufOut = NetUtils.getOutputStream(s, writeTimeout);\n        InputStream unbufIn = NetUtils.getInputStream(s, readTimeout);\n        IOStreamPair saslStreams = dfsClient.saslClient.socketSend(s,\n            unbufOut, unbufIn, dfsClient, accessToken, nodes[0]);\n        unbufOut = saslStreams.out;\n        unbufIn = saslStreams.in;\n        out = new DataOutputStream(new BufferedOutputStream(unbufOut,\n            DFSUtilClient.getSmallBufferSize(dfsClient.getConfiguration())));\n        blockReplyStream = new DataInputStream(unbufIn);\n\n        //\n        // Xmit header info to datanode\n        //\n\n        BlockConstructionStage bcs = recoveryFlag ?\n            stage.getRecoveryStage() : stage;\n\n        // We cannot change the block length in 'block' as it counts the number\n        // of bytes ack'ed.\n        ExtendedBlock blockCopy = block.getCurrentBlock();\n        blockCopy.setNumBytes(stat.getBlockSize());\n\n        boolean[] targetPinnings = getPinnings(nodes);\n        // send the request\n        new Sender(out).writeBlock(blockCopy, nodeStorageTypes[0], accessToken,\n            dfsClient.clientName, nodes, nodeStorageTypes, null, bcs,\n            nodes.length, block.getNumBytes(), bytesSent, newGS,\n            checksum4WriteBlock, cachingStrategy.get(), isLazyPersistFile,\n            (targetPinnings != null && targetPinnings[0]), targetPinnings,\n            nodeStorageIDs[0], nodeStorageIDs);\n\n        // receive ack for connect\n        BlockOpResponseProto resp = BlockOpResponseProto.parseFrom(", "caller_log_raw_stripped": "[ENTRY] DataStreamer:nextBlockOutputStream()\n  WHILE: retry\n  IF: errorState.isRestartingNode()==true  -> continue (skip)", "callee_log_raw_stripped": "[ENTRY] DataStreamer:createBlockOutputStream(...)", "caller_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:nextBlockOutputStream()\n  WHILE: retry\n  IF: errorState.isRestartingNode()==true  -> continue (skip)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:nextBlockOutputStream()\n  WHILE: retry\n  IF: errorState.isRestartingNode()==true  -> continue (skip)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "callee_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:createBlockOutputStream(...)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:createBlockOutputStream(...)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "parent_info": "node name is DataStreamer:nextBlockOutputStream()node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:nextBlockOutputStream()\n  WHILE: retry\n  IF: errorState.isRestartingNode()==true  -> continue (skip)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:nextBlockOutputStream()\n  WHILE: retry\n  IF: errorState.isRestartingNode()==true  -> continue (skip)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```souce code:* Returns the list of target datanodes.\n   */\n  protected LocatedBlock nextBlockOutputStream() throws IOException {\n    LocatedBlock lb;\n    DatanodeInfo[] nodes;\n    StorageType[] nextStorageTypes;\n    String[] nextStorageIDs;\n    int count = dfsClient.getConf().getNumBlockWriteRetry();\n    boolean success;\n    final ExtendedBlock oldBlock = block.getCurrentBlock();\n    do {\n      errorState.resetInternalError();\n      lastException.clear();\n\n      DatanodeInfo[] excluded = getExcludedNodes();\n      lb = locateFollowingBlock(\n          excluded.length > 0 ? excluded : null, oldBlock);\n      block.setCurrentBlock(lb.getBlock());\n      block.setNumBytes(0);\n      bytesSent = 0;\n      accessToken = lb.getBlockToken();\n      nodes = lb.getLocations();\n      nextStorageTypes = lb.getStorageTypes();\n      nextStorageIDs = lb.getStorageIDs();\n\n      // Connect to first DataNode in the list.\n      success = createBlockOutputStream(nodes, nextStorageTypes, nextStorageIDs,\n          0L, false);\n\n      if (!success) {\n        LOG.warn(\"Abandoning \" + block);\n        dfsClient.namenode.abandonBlock(block.getCurrentBlock(),\n            stat.getFileId(), src, dfsClient.clientName);\n        block.setCurrentBlock(null);\n        final DatanodeInfo badNode = nodes[errorState.getBadNodeIndex()];\n        LOG.warn(\"Excluding datanode \" + badNode);\n        excludedNodes.put(badNode, badNode);\n      }\n    } while (!success && --count >= 0);\n\n    if (!success) {\n      throw new IOException(\"Unable to create new block.\");\n    }\n    return lb;\n  }\n\n  // connects to the first datanode in the pipeline\n  // Returns true if success, otherwise return failure.\n  //\n  boolean createBlockOutputStream(DatanodeInfo[] nodes,\n      StorageType[] nodeStorageTypes, String[] nodeStorageIDs,\n      long newGS, boolean recoveryFlag) {\n    if (nodes.length == 0) {\n      LOG.info(\"nodes are empty for write pipeline of \" + block);\n      return false;\n    }\n    String firstBadLink = \"\";\n    boolean checkRestart = false;\n    if (LOG.isDebugEnabled()) {\n      LOG.debug(\"pipeline = \" + Arrays.toString(nodes) + \", \" + this);\n    }", "child_info": "node name isDataStreamer:createBlockOutputStream(java.util.List,int,long)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:createBlockOutputStream(...)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:createBlockOutputStream(...)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```source code:// Returns true if success, otherwise return failure.\n  //\n  boolean createBlockOutputStream(DatanodeInfo[] nodes,\n      StorageType[] nodeStorageTypes, String[] nodeStorageIDs,\n      long newGS, boolean recoveryFlag) {\n    if (nodes.length == 0) {\n      LOG.info(\"nodes are empty for write pipeline of \" + block);\n      return false;\n    }\n    String firstBadLink = \"\";\n    boolean checkRestart = false;\n    if (LOG.isDebugEnabled()) {\n      LOG.debug(\"pipeline = \" + Arrays.toString(nodes) + \", \" + this);\n    }\n\n    // persist blocks on namenode on next flush\n    persistBlocks.set(true);\n\n    int refetchEncryptionKey = 1;\n    while (true) {\n      boolean result = false;\n      DataOutputStream out = null;\n      try {\n        assert null == s : \"Previous socket unclosed\";\n        assert null == blockReplyStream : \"Previous blockReplyStream unclosed\";\n        s = createSocketForPipeline(nodes[0], nodes.length, dfsClient);\n        long writeTimeout = dfsClient.getDatanodeWriteTimeout(nodes.length);\n        long readTimeout = dfsClient.getDatanodeReadTimeout(nodes.length);\n\n        OutputStream unbufOut = NetUtils.getOutputStream(s, writeTimeout);\n        InputStream unbufIn = NetUtils.getInputStream(s, readTimeout);\n        IOStreamPair saslStreams = dfsClient.saslClient.socketSend(s,\n            unbufOut, unbufIn, dfsClient, accessToken, nodes[0]);\n        unbufOut = saslStreams.out;\n        unbufIn = saslStreams.in;\n        out = new DataOutputStream(new BufferedOutputStream(unbufOut,\n            DFSUtilClient.getSmallBufferSize(dfsClient.getConfiguration())));\n        blockReplyStream = new DataInputStream(unbufIn);\n\n        //\n        // Xmit header info to datanode\n        //\n\n        BlockConstructionStage bcs = recoveryFlag ?\n            stage.getRecoveryStage() : stage;\n\n        // We cannot change the block length in 'block' as it counts the number\n        // of bytes ack'ed.\n        ExtendedBlock blockCopy = block.getCurrentBlock();\n        blockCopy.setNumBytes(stat.getBlockSize());\n\n        boolean[] targetPinnings = getPinnings(nodes);\n        // send the request\n        new Sender(out).writeBlock(blockCopy, nodeStorageTypes[0], accessToken,\n            dfsClient.clientName, nodes, nodeStorageTypes, null, bcs,\n            nodes.length, block.getNumBytes(), bytesSent, newGS,\n            checksum4WriteBlock, cachingStrategy.get(), isLazyPersistFile,\n            (targetPinnings != null && targetPinnings[0]), targetPinnings,\n            nodeStorageIDs[0], nodeStorageIDs);\n\n        // receive ack for connect\n        BlockOpResponseProto resp = BlockOpResponseProto.parseFrom("}
{"mp_id": "hdfs::BM::addStoredBlock->DSI::addBlock", "caller": "BlockManager:addStoredBlock(BlockInfo,DatanodeStorageInfo,DatanodeDescriptor,boolean)", "callee": "DatanodeStorageInfo:addBlock(BlockInfo)", "gt": "valid", "stratum": "simple", "infeasible_reason": "", "rationale": "Registers block on DatanodeStorageInfo after block report.", "caller_code": "* Faster version of {@link #addStoredBlock},\n   * intended for use with initial block report at startup. If not in startup\n   * safe mode, will call standard addStoredBlock(). Assumes this method is\n   * called \"immediately\" so there is no need to refresh the storedBlock from\n   * blocksMap. Doesn't handle low redundancy/extra redundancy, or worry about\n   * pendingReplications or corruptReplicas, because it's in startup safe mode.\n   * Doesn't log every block, because there are typically millions of them.\n   * \n   * @throws IOException\n   */\n  private void addStoredBlockImmediate(BlockInfo storedBlock, Block reported,\n      DatanodeStorageInfo storageInfo)\n  throws IOException {\n    assert (storedBlock != null && namesystem.hasWriteLock());\n    if (!namesystem.isInStartupSafeMode()\n        || isPopulatingReplQueues()) {\n      addStoredBlock(storedBlock, reported, storageInfo, null, false);\n      return;\n    }\n\n    // just add it\n    AddBlockResult result = storageInfo.addBlock(storedBlock, reported);\n\n    // Now check for completion of blocks and safe block count\n    int numCurrentReplica = countLiveNodes(storedBlock);\n    if (storedBlock.getBlockUCState() == BlockUCState.COMMITTED\n        && hasMinStorage(storedBlock, numCurrentReplica)) {\n      completeBlock(storedBlock, null, false);\n    } else if (storedBlock.isComplete() && result == AddBlockResult.ADDED) {\n      // check whether safe replication is reached for the block\n      // only complete blocks are counted towards that.\n      // In the case that the block just became complete above, completeBlock()\n      // handles the safe block count maintenance.\n      bmSafeMode.incrementSafeBlockCount(numCurrentReplica, storedBlock);\n    }\n  }\n\n  /**\n   * Modify (block-->datanode) map. Remove block from set of\n   * needed reconstruction if this takes care of the problem.\n   * @return the block that is stored in blocksMap.\n   */\n  private Block addStoredBlock(final BlockInfo block,\n                               final Block reportedBlock,\n                               DatanodeStorageInfo storageInfo,\n                               DatanodeDescriptor delNodeHint,\n                               boolean logEveryBlock)\n  throws IOException {\n    assert block != null && namesystem.hasWriteLock();\n    BlockInfo storedBlock;\n    DatanodeDescriptor node = storageInfo.getDatanodeDescriptor();\n    if (!block.isComplete()) {\n      //refresh our copy in case the block got completed in another thread\n      storedBlock = getStoredBlock(block);\n    } else {\n      storedBlock = block;\n    }\n    if (storedBlock == null || storedBlock.isDeleted()) {\n      // If this block does not belong to anyfile, then we are done.\n      blockLog.debug(\"BLOCK* addStoredBlock: {} on {} size {} but it does not\" +\n          \" belong to any file\", block, node, block.getNumBytes());", "callee_code": "}\n\n  public AddBlockResult addBlock(BlockInfo b, Block reportedBlock) {\n    // First check whether the block belongs to a different storage\n    // on the same DN.\n    AddBlockResult result = AddBlockResult.ADDED;\n    DatanodeStorageInfo otherStorage =\n        b.findStorageInfo(getDatanodeDescriptor());\n\n    if (otherStorage != null) {\n      if (otherStorage != this) {\n        // The block belongs to a different storage. Remove it first.\n        otherStorage.removeBlock(b);\n        result = AddBlockResult.REPLACED;\n      } else {\n        // The block is already associated with this storage.\n        return AddBlockResult.ALREADY_EXIST;\n      }\n    }\n\n    // add to the head of the data-node list\n    b.addStorage(this, reportedBlock);\n    insertToList(b);\n    return result;\n  }\n\n  AddBlockResult addBlock(BlockInfo b) {\n    return addBlock(b, b);\n  }\n\n  public void insertToList(BlockInfo b) {\n    blockList = b.listInsert(blockList, this);\n    numBlocks++;\n  }\n  boolean removeBlock(BlockInfo b) {\n    blockList = b.listRemove(blockList, this);\n    if (b.removeStorage(this)) {\n      numBlocks--;\n      return true;\n    } else {\n      return false;\n    }\n  }\n\n  int numBlocks() {\n    return numBlocks;\n  }\n\n  Iterator<BlockInfo> getBlockIterator() {\n    return new BlockIterator(blockList);\n  }\n\n  /**\n   * Move block to the head of the list of blocks belonging to the data-node.\n   * @return the index of the head of the blockList\n   */\n  int moveBlockToHead(BlockInfo b, int curIndex, int headIndex) {\n    blockList = b.moveBlockToHead(blockList, this, curIndex, headIndex);\n    return curIndex;\n  }", "caller_log_raw_stripped": "[ENTRY] BlockManager:addStoredBlock(BlockInfo, storage, ...)\n  IF: storage != null\n  CALL: storageInfo.addBlock(storedBlock, reportedBlock)", "callee_log_raw_stripped": "[ENTRY] DatanodeStorageInfo:addBlock(BlockInfo)\n  this.blocks.add(block)\n  EXIT", "caller_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] BlockManager:addStoredBlock(BlockInfo, storage, ...)\n  IF: storage != null\n  CALL: storageInfo.addBlock(storedBlock, reportedBlock)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] BlockManager:addStoredBlock(BlockInfo, storage, ...)\n  IF: storage != null\n  CALL: storageInfo.addBlock(storedBlock, reportedBlock)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "callee_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DatanodeStorageInfo:addBlock(BlockInfo)\n  this.blocks.add(block)\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DatanodeStorageInfo:addBlock(BlockInfo)\n  this.blocks.add(block)\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "parent_info": "node name is BlockManager:addStoredBlock(BlockInfo,DatanodeStorageInfo,DatanodeDescriptor,boolean)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] BlockManager:addStoredBlock(BlockInfo, storage, ...)\n  IF: storage != null\n  CALL: storageInfo.addBlock(storedBlock, reportedBlock)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] BlockManager:addStoredBlock(BlockInfo, storage, ...)\n  IF: storage != null\n  CALL: storageInfo.addBlock(storedBlock, reportedBlock)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```souce code:* Faster version of {@link #addStoredBlock},\n   * intended for use with initial block report at startup. If not in startup\n   * safe mode, will call standard addStoredBlock(). Assumes this method is\n   * called \"immediately\" so there is no need to refresh the storedBlock from\n   * blocksMap. Doesn't handle low redundancy/extra redundancy, or worry about\n   * pendingReplications or corruptReplicas, because it's in startup safe mode.\n   * Doesn't log every block, because there are typically millions of them.\n   * \n   * @throws IOException\n   */\n  private void addStoredBlockImmediate(BlockInfo storedBlock, Block reported,\n      DatanodeStorageInfo storageInfo)\n  throws IOException {\n    assert (storedBlock != null && namesystem.hasWriteLock());\n    if (!namesystem.isInStartupSafeMode()\n        || isPopulatingReplQueues()) {\n      addStoredBlock(storedBlock, reported, storageInfo, null, false);\n      return;\n    }\n\n    // just add it\n    AddBlockResult result = storageInfo.addBlock(storedBlock, reported);\n\n    // Now check for completion of blocks and safe block count\n    int numCurrentReplica = countLiveNodes(storedBlock);\n    if (storedBlock.getBlockUCState() == BlockUCState.COMMITTED\n        && hasMinStorage(storedBlock, numCurrentReplica)) {\n      completeBlock(storedBlock, null, false);\n    } else if (storedBlock.isComplete() && result == AddBlockResult.ADDED) {\n      // check whether safe replication is reached for the block\n      // only complete blocks are counted towards that.\n      // In the case that the block just became complete above, completeBlock()\n      // handles the safe block count maintenance.\n      bmSafeMode.incrementSafeBlockCount(numCurrentReplica, storedBlock);\n    }\n  }\n\n  /**\n   * Modify (block-->datanode) map. Remove block from set of\n   * needed reconstruction if this takes care of the problem.\n   * @return the block that is stored in blocksMap.\n   */\n  private Block addStoredBlock(final BlockInfo block,\n                               final Block reportedBlock,\n                               DatanodeStorageInfo storageInfo,\n                               DatanodeDescriptor delNodeHint,\n                               boolean logEveryBlock)\n  throws IOException {\n    assert block != null && namesystem.hasWriteLock();\n    BlockInfo storedBlock;\n    DatanodeDescriptor node = storageInfo.getDatanodeDescriptor();\n    if (!block.isComplete()) {\n      //refresh our copy in case the block got completed in another thread\n      storedBlock = getStoredBlock(block);\n    } else {\n      storedBlock = block;\n    }\n    if (storedBlock == null || storedBlock.isDeleted()) {\n      // If this block does not belong to anyfile, then we are done.\n      blockLog.debug(\"BLOCK* addStoredBlock: {} on {} size {} but it does not\" +\n          \" belong to any file\", block, node, block.getNumBytes());", "child_info": "node name isDatanodeStorageInfo:addBlock(BlockInfo)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DatanodeStorageInfo:addBlock(BlockInfo)\n  this.blocks.add(block)\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DatanodeStorageInfo:addBlock(BlockInfo)\n  this.blocks.add(block)\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```source code:}\n\n  public AddBlockResult addBlock(BlockInfo b, Block reportedBlock) {\n    // First check whether the block belongs to a different storage\n    // on the same DN.\n    AddBlockResult result = AddBlockResult.ADDED;\n    DatanodeStorageInfo otherStorage =\n        b.findStorageInfo(getDatanodeDescriptor());\n\n    if (otherStorage != null) {\n      if (otherStorage != this) {\n        // The block belongs to a different storage. Remove it first.\n        otherStorage.removeBlock(b);\n        result = AddBlockResult.REPLACED;\n      } else {\n        // The block is already associated with this storage.\n        return AddBlockResult.ALREADY_EXIST;\n      }\n    }\n\n    // add to the head of the data-node list\n    b.addStorage(this, reportedBlock);\n    insertToList(b);\n    return result;\n  }\n\n  AddBlockResult addBlock(BlockInfo b) {\n    return addBlock(b, b);\n  }\n\n  public void insertToList(BlockInfo b) {\n    blockList = b.listInsert(blockList, this);\n    numBlocks++;\n  }\n  boolean removeBlock(BlockInfo b) {\n    blockList = b.listRemove(blockList, this);\n    if (b.removeStorage(this)) {\n      numBlocks--;\n      return true;\n    } else {\n      return false;\n    }\n  }\n\n  int numBlocks() {\n    return numBlocks;\n  }\n\n  Iterator<BlockInfo> getBlockIterator() {\n    return new BlockIterator(blockList);\n  }\n\n  /**\n   * Move block to the head of the list of blocks belonging to the data-node.\n   * @return the index of the head of the blockList\n   */\n  int moveBlockToHead(BlockInfo b, int curIndex, int headIndex) {\n    blockList = b.moveBlockToHead(blockList, this, curIndex, headIndex);\n    return curIndex;\n  }"}
{"mp_id": "hdfs::BM::addStoredBlock->DSI::addBlock-INFEASIBLE", "caller": "BlockManager:addStoredBlock(...)", "callee": "DatanodeStorageInfo:addBlock(...)", "gt": "infeasible", "stratum": "simple", "infeasible_reason": "storageInfo==null — caller guards with null check.", "rationale": "", "caller_code": "* Faster version of {@link #addStoredBlock},\n   * intended for use with initial block report at startup. If not in startup\n   * safe mode, will call standard addStoredBlock(). Assumes this method is\n   * called \"immediately\" so there is no need to refresh the storedBlock from\n   * blocksMap. Doesn't handle low redundancy/extra redundancy, or worry about\n   * pendingReplications or corruptReplicas, because it's in startup safe mode.\n   * Doesn't log every block, because there are typically millions of them.\n   * \n   * @throws IOException\n   */\n  private void addStoredBlockImmediate(BlockInfo storedBlock, Block reported,\n      DatanodeStorageInfo storageInfo)\n  throws IOException {\n    assert (storedBlock != null && namesystem.hasWriteLock());\n    if (!namesystem.isInStartupSafeMode()\n        || isPopulatingReplQueues()) {\n      addStoredBlock(storedBlock, reported, storageInfo, null, false);\n      return;\n    }\n\n    // just add it\n    AddBlockResult result = storageInfo.addBlock(storedBlock, reported);\n\n    // Now check for completion of blocks and safe block count\n    int numCurrentReplica = countLiveNodes(storedBlock);\n    if (storedBlock.getBlockUCState() == BlockUCState.COMMITTED\n        && hasMinStorage(storedBlock, numCurrentReplica)) {\n      completeBlock(storedBlock, null, false);\n    } else if (storedBlock.isComplete() && result == AddBlockResult.ADDED) {\n      // check whether safe replication is reached for the block\n      // only complete blocks are counted towards that.\n      // In the case that the block just became complete above, completeBlock()\n      // handles the safe block count maintenance.\n      bmSafeMode.incrementSafeBlockCount(numCurrentReplica, storedBlock);\n    }\n  }\n\n  /**\n   * Modify (block-->datanode) map. Remove block from set of\n   * needed reconstruction if this takes care of the problem.\n   * @return the block that is stored in blocksMap.\n   */\n  private Block addStoredBlock(final BlockInfo block,\n                               final Block reportedBlock,\n                               DatanodeStorageInfo storageInfo,\n                               DatanodeDescriptor delNodeHint,\n                               boolean logEveryBlock)\n  throws IOException {\n    assert block != null && namesystem.hasWriteLock();\n    BlockInfo storedBlock;\n    DatanodeDescriptor node = storageInfo.getDatanodeDescriptor();\n    if (!block.isComplete()) {\n      //refresh our copy in case the block got completed in another thread\n      storedBlock = getStoredBlock(block);\n    } else {\n      storedBlock = block;\n    }\n    if (storedBlock == null || storedBlock.isDeleted()) {\n      // If this block does not belong to anyfile, then we are done.\n      blockLog.debug(\"BLOCK* addStoredBlock: {} on {} size {} but it does not\" +\n          \" belong to any file\", block, node, block.getNumBytes());", "callee_code": "}\n\n  public AddBlockResult addBlock(BlockInfo b, Block reportedBlock) {\n    // First check whether the block belongs to a different storage\n    // on the same DN.\n    AddBlockResult result = AddBlockResult.ADDED;\n    DatanodeStorageInfo otherStorage =\n        b.findStorageInfo(getDatanodeDescriptor());\n\n    if (otherStorage != null) {\n      if (otherStorage != this) {\n        // The block belongs to a different storage. Remove it first.\n        otherStorage.removeBlock(b);\n        result = AddBlockResult.REPLACED;\n      } else {\n        // The block is already associated with this storage.\n        return AddBlockResult.ALREADY_EXIST;\n      }\n    }\n\n    // add to the head of the data-node list\n    b.addStorage(this, reportedBlock);\n    insertToList(b);\n    return result;\n  }\n\n  AddBlockResult addBlock(BlockInfo b) {\n    return addBlock(b, b);\n  }\n\n  public void insertToList(BlockInfo b) {\n    blockList = b.listInsert(blockList, this);\n    numBlocks++;\n  }\n  boolean removeBlock(BlockInfo b) {\n    blockList = b.listRemove(blockList, this);\n    if (b.removeStorage(this)) {\n      numBlocks--;\n      return true;\n    } else {\n      return false;\n    }\n  }\n\n  int numBlocks() {\n    return numBlocks;\n  }\n\n  Iterator<BlockInfo> getBlockIterator() {\n    return new BlockIterator(blockList);\n  }\n\n  /**\n   * Move block to the head of the list of blocks belonging to the data-node.\n   * @return the index of the head of the blockList\n   */\n  int moveBlockToHead(BlockInfo b, int curIndex, int headIndex) {\n    blockList = b.moveBlockToHead(blockList, this, curIndex, headIndex);\n    return curIndex;\n  }", "caller_log_raw_stripped": "[ENTRY] BlockManager:addStoredBlock(BlockInfo, storage==NULL, ...) \n  _note: NPE or early return before addBlock", "callee_log_raw_stripped": "[ENTRY] DatanodeStorageInfo:addBlock(BlockInfo)\n  this.blocks.add(block)", "caller_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] BlockManager:addStoredBlock(BlockInfo, storage==NULL, ...) \n  _note: NPE or early return before addBlock\n      </exec_flow>\n      <log_sequence>\n[ENTRY] BlockManager:addStoredBlock(BlockInfo, storage==NULL, ...) \n  _note: NPE or early return before addBlock\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "callee_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DatanodeStorageInfo:addBlock(BlockInfo)\n  this.blocks.add(block)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DatanodeStorageInfo:addBlock(BlockInfo)\n  this.blocks.add(block)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "parent_info": "node name is BlockManager:addStoredBlock(...)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] BlockManager:addStoredBlock(BlockInfo, storage==NULL, ...) \n  _note: NPE or early return before addBlock\n      </exec_flow>\n      <log_sequence>\n[ENTRY] BlockManager:addStoredBlock(BlockInfo, storage==NULL, ...) \n  _note: NPE or early return before addBlock\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```souce code:* Faster version of {@link #addStoredBlock},\n   * intended for use with initial block report at startup. If not in startup\n   * safe mode, will call standard addStoredBlock(). Assumes this method is\n   * called \"immediately\" so there is no need to refresh the storedBlock from\n   * blocksMap. Doesn't handle low redundancy/extra redundancy, or worry about\n   * pendingReplications or corruptReplicas, because it's in startup safe mode.\n   * Doesn't log every block, because there are typically millions of them.\n   * \n   * @throws IOException\n   */\n  private void addStoredBlockImmediate(BlockInfo storedBlock, Block reported,\n      DatanodeStorageInfo storageInfo)\n  throws IOException {\n    assert (storedBlock != null && namesystem.hasWriteLock());\n    if (!namesystem.isInStartupSafeMode()\n        || isPopulatingReplQueues()) {\n      addStoredBlock(storedBlock, reported, storageInfo, null, false);\n      return;\n    }\n\n    // just add it\n    AddBlockResult result = storageInfo.addBlock(storedBlock, reported);\n\n    // Now check for completion of blocks and safe block count\n    int numCurrentReplica = countLiveNodes(storedBlock);\n    if (storedBlock.getBlockUCState() == BlockUCState.COMMITTED\n        && hasMinStorage(storedBlock, numCurrentReplica)) {\n      completeBlock(storedBlock, null, false);\n    } else if (storedBlock.isComplete() && result == AddBlockResult.ADDED) {\n      // check whether safe replication is reached for the block\n      // only complete blocks are counted towards that.\n      // In the case that the block just became complete above, completeBlock()\n      // handles the safe block count maintenance.\n      bmSafeMode.incrementSafeBlockCount(numCurrentReplica, storedBlock);\n    }\n  }\n\n  /**\n   * Modify (block-->datanode) map. Remove block from set of\n   * needed reconstruction if this takes care of the problem.\n   * @return the block that is stored in blocksMap.\n   */\n  private Block addStoredBlock(final BlockInfo block,\n                               final Block reportedBlock,\n                               DatanodeStorageInfo storageInfo,\n                               DatanodeDescriptor delNodeHint,\n                               boolean logEveryBlock)\n  throws IOException {\n    assert block != null && namesystem.hasWriteLock();\n    BlockInfo storedBlock;\n    DatanodeDescriptor node = storageInfo.getDatanodeDescriptor();\n    if (!block.isComplete()) {\n      //refresh our copy in case the block got completed in another thread\n      storedBlock = getStoredBlock(block);\n    } else {\n      storedBlock = block;\n    }\n    if (storedBlock == null || storedBlock.isDeleted()) {\n      // If this block does not belong to anyfile, then we are done.\n      blockLog.debug(\"BLOCK* addStoredBlock: {} on {} size {} but it does not\" +\n          \" belong to any file\", block, node, block.getNumBytes());", "child_info": "node name isDatanodeStorageInfo:addBlock(...)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DatanodeStorageInfo:addBlock(BlockInfo)\n  this.blocks.add(block)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DatanodeStorageInfo:addBlock(BlockInfo)\n  this.blocks.add(block)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```source code:}\n\n  public AddBlockResult addBlock(BlockInfo b, Block reportedBlock) {\n    // First check whether the block belongs to a different storage\n    // on the same DN.\n    AddBlockResult result = AddBlockResult.ADDED;\n    DatanodeStorageInfo otherStorage =\n        b.findStorageInfo(getDatanodeDescriptor());\n\n    if (otherStorage != null) {\n      if (otherStorage != this) {\n        // The block belongs to a different storage. Remove it first.\n        otherStorage.removeBlock(b);\n        result = AddBlockResult.REPLACED;\n      } else {\n        // The block is already associated with this storage.\n        return AddBlockResult.ALREADY_EXIST;\n      }\n    }\n\n    // add to the head of the data-node list\n    b.addStorage(this, reportedBlock);\n    insertToList(b);\n    return result;\n  }\n\n  AddBlockResult addBlock(BlockInfo b) {\n    return addBlock(b, b);\n  }\n\n  public void insertToList(BlockInfo b) {\n    blockList = b.listInsert(blockList, this);\n    numBlocks++;\n  }\n  boolean removeBlock(BlockInfo b) {\n    blockList = b.listRemove(blockList, this);\n    if (b.removeStorage(this)) {\n      numBlocks--;\n      return true;\n    } else {\n      return false;\n    }\n  }\n\n  int numBlocks() {\n    return numBlocks;\n  }\n\n  Iterator<BlockInfo> getBlockIterator() {\n    return new BlockIterator(blockList);\n  }\n\n  /**\n   * Move block to the head of the list of blocks belonging to the data-node.\n   * @return the index of the head of the blockList\n   */\n  int moveBlockToHead(BlockInfo b, int curIndex, int headIndex) {\n    blockList = b.moveBlockToHead(blockList, this, curIndex, headIndex);\n    return curIndex;\n  }"}
{"mp_id": "hdfs::BM::processReport->addStoredBlock", "caller": "BlockManager:processReport(DatanodeDescriptor,DatanodeStorageInfo,StorageBlockReport[],boolean)", "callee": "BlockManager:addStoredBlock(BlockInfo,DatanodeStorageInfo,DatanodeDescriptor,boolean)", "gt": "valid", "stratum": "simple", "infeasible_reason": "", "rationale": "processReport iterates storage report blocks; calls addStoredBlock for each untracked block.", "caller_code": "* @throws IOException\n   */\n  public boolean processReport(final DatanodeID nodeID,\n      final DatanodeStorage storage,\n      final BlockListAsLongs newReport,\n      BlockReportContext context) throws IOException {\n    namesystem.writeLock();\n    final long startTime = Time.monotonicNow(); //after acquiring write lock\n    final long endTime;\n    DatanodeDescriptor node;\n    Collection<Block> invalidatedBlocks = Collections.emptyList();\n    String strBlockReportId =\n        context != null ? Long.toHexString(context.getReportId()) : \"\";\n    String fullBrLeaseId =\n        context != null ? Long.toHexString(context.getLeaseId()) : \"\";\n\n    try {\n      node = datanodeManager.getDatanode(nodeID);\n      if (node == null || !node.isRegistered()) {\n        throw new IOException(\n            \"ProcessReport from dead or unregistered node: \" + nodeID);\n      }\n\n      // To minimize startup time, we discard any second (or later) block reports\n      // that we receive while still in startup phase.\n      // Register DN with provided storage, not with storage owned by DN\n      // DN should still have a ref to the DNStorageInfo.\n      DatanodeStorageInfo storageInfo =\n          providedStorageMap.getStorage(node, storage);\n\n      if (storageInfo == null) {\n        // We handle this for backwards compatibility.\n        storageInfo = node.updateStorage(storage);\n      }\n      if (namesystem.isInStartupSafeMode()\n          && !StorageType.PROVIDED.equals(storageInfo.getStorageType())\n          && storageInfo.getBlockReportCount() > 0) {\n        blockLog.info(\"BLOCK* processReport 0x{} with lease ID 0x{}: \"\n            + \"discarded non-initial block report from {}\"\n            + \" because namenode still in startup phase\",\n            strBlockReportId, fullBrLeaseId, nodeID);\n        blockReportLeaseManager.removeLease(node);\n        return !node.hasStaleStorages();\n      }\n\n      if (storageInfo.getBlockReportCount() == 0) {\n        // The first block report can be processed a lot more efficiently than\n        // ordinary block reports.  This shortens restart times.\n        blockLog.info(\"BLOCK* processReport 0x{} with lease ID 0x{}: Processing first \"\n            + \"storage report for {} from datanode {}\",\n            strBlockReportId, fullBrLeaseId,\n            storageInfo.getStorageID(),\n            nodeID);\n        processFirstBlockReport(storageInfo, newReport);\n      } else {\n        // Block reports for provided storage are not\n        // maintained by DN heartbeats\n        if (!StorageType.PROVIDED.equals(storageInfo.getStorageType())) {\n          invalidatedBlocks = processReport(storageInfo, newReport);\n        }\n      }\n      storageInfo.receivedBlockReport();", "callee_code": "* Faster version of {@link #addStoredBlock},\n   * intended for use with initial block report at startup. If not in startup\n   * safe mode, will call standard addStoredBlock(). Assumes this method is\n   * called \"immediately\" so there is no need to refresh the storedBlock from\n   * blocksMap. Doesn't handle low redundancy/extra redundancy, or worry about\n   * pendingReplications or corruptReplicas, because it's in startup safe mode.\n   * Doesn't log every block, because there are typically millions of them.\n   * \n   * @throws IOException\n   */\n  private void addStoredBlockImmediate(BlockInfo storedBlock, Block reported,\n      DatanodeStorageInfo storageInfo)\n  throws IOException {\n    assert (storedBlock != null && namesystem.hasWriteLock());\n    if (!namesystem.isInStartupSafeMode()\n        || isPopulatingReplQueues()) {\n      addStoredBlock(storedBlock, reported, storageInfo, null, false);\n      return;\n    }\n\n    // just add it\n    AddBlockResult result = storageInfo.addBlock(storedBlock, reported);\n\n    // Now check for completion of blocks and safe block count\n    int numCurrentReplica = countLiveNodes(storedBlock);\n    if (storedBlock.getBlockUCState() == BlockUCState.COMMITTED\n        && hasMinStorage(storedBlock, numCurrentReplica)) {\n      completeBlock(storedBlock, null, false);\n    } else if (storedBlock.isComplete() && result == AddBlockResult.ADDED) {\n      // check whether safe replication is reached for the block\n      // only complete blocks are counted towards that.\n      // In the case that the block just became complete above, completeBlock()\n      // handles the safe block count maintenance.\n      bmSafeMode.incrementSafeBlockCount(numCurrentReplica, storedBlock);\n    }\n  }\n\n  /**\n   * Modify (block-->datanode) map. Remove block from set of\n   * needed reconstruction if this takes care of the problem.\n   * @return the block that is stored in blocksMap.\n   */\n  private Block addStoredBlock(final BlockInfo block,\n                               final Block reportedBlock,\n                               DatanodeStorageInfo storageInfo,\n                               DatanodeDescriptor delNodeHint,\n                               boolean logEveryBlock)\n  throws IOException {\n    assert block != null && namesystem.hasWriteLock();\n    BlockInfo storedBlock;\n    DatanodeDescriptor node = storageInfo.getDatanodeDescriptor();\n    if (!block.isComplete()) {\n      //refresh our copy in case the block got completed in another thread\n      storedBlock = getStoredBlock(block);\n    } else {\n      storedBlock = block;\n    }\n    if (storedBlock == null || storedBlock.isDeleted()) {\n      // If this block does not belong to anyfile, then we are done.\n      blockLog.debug(\"BLOCK* addStoredBlock: {} on {} size {} but it does not\" +\n          \" belong to any file\", block, node, block.getNumBytes());", "caller_log_raw_stripped": "[ENTRY] BlockManager:processReport(...)\n  FOR each block in report:\n  IF: !isBlockInInvalidate(blockId)\n  CALL: addStoredBlock(...)", "callee_log_raw_stripped": "[ENTRY] BlockManager:addStoredBlock(...)\n  ...", "caller_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] BlockManager:processReport(...)\n  FOR each block in report:\n  IF: !isBlockInInvalidate(blockId)\n  CALL: addStoredBlock(...)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] BlockManager:processReport(...)\n  FOR each block in report:\n  IF: !isBlockInInvalidate(blockId)\n  CALL: addStoredBlock(...)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "callee_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] BlockManager:addStoredBlock(...)\n  ...\n      </exec_flow>\n      <log_sequence>\n[ENTRY] BlockManager:addStoredBlock(...)\n  ...\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "parent_info": "node name is BlockManager:processReport(DatanodeDescriptor,DatanodeStorageInfo,StorageBlockReport[],boolean)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] BlockManager:processReport(...)\n  FOR each block in report:\n  IF: !isBlockInInvalidate(blockId)\n  CALL: addStoredBlock(...)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] BlockManager:processReport(...)\n  FOR each block in report:\n  IF: !isBlockInInvalidate(blockId)\n  CALL: addStoredBlock(...)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```souce code:* @throws IOException\n   */\n  public boolean processReport(final DatanodeID nodeID,\n      final DatanodeStorage storage,\n      final BlockListAsLongs newReport,\n      BlockReportContext context) throws IOException {\n    namesystem.writeLock();\n    final long startTime = Time.monotonicNow(); //after acquiring write lock\n    final long endTime;\n    DatanodeDescriptor node;\n    Collection<Block> invalidatedBlocks = Collections.emptyList();\n    String strBlockReportId =\n        context != null ? Long.toHexString(context.getReportId()) : \"\";\n    String fullBrLeaseId =\n        context != null ? Long.toHexString(context.getLeaseId()) : \"\";\n\n    try {\n      node = datanodeManager.getDatanode(nodeID);\n      if (node == null || !node.isRegistered()) {\n        throw new IOException(\n            \"ProcessReport from dead or unregistered node: \" + nodeID);\n      }\n\n      // To minimize startup time, we discard any second (or later) block reports\n      // that we receive while still in startup phase.\n      // Register DN with provided storage, not with storage owned by DN\n      // DN should still have a ref to the DNStorageInfo.\n      DatanodeStorageInfo storageInfo =\n          providedStorageMap.getStorage(node, storage);\n\n      if (storageInfo == null) {\n        // We handle this for backwards compatibility.\n        storageInfo = node.updateStorage(storage);\n      }\n      if (namesystem.isInStartupSafeMode()\n          && !StorageType.PROVIDED.equals(storageInfo.getStorageType())\n          && storageInfo.getBlockReportCount() > 0) {\n        blockLog.info(\"BLOCK* processReport 0x{} with lease ID 0x{}: \"\n            + \"discarded non-initial block report from {}\"\n            + \" because namenode still in startup phase\",\n            strBlockReportId, fullBrLeaseId, nodeID);\n        blockReportLeaseManager.removeLease(node);\n        return !node.hasStaleStorages();\n      }\n\n      if (storageInfo.getBlockReportCount() == 0) {\n        // The first block report can be processed a lot more efficiently than\n        // ordinary block reports.  This shortens restart times.\n        blockLog.info(\"BLOCK* processReport 0x{} with lease ID 0x{}: Processing first \"\n            + \"storage report for {} from datanode {}\",\n            strBlockReportId, fullBrLeaseId,\n            storageInfo.getStorageID(),\n            nodeID);\n        processFirstBlockReport(storageInfo, newReport);\n      } else {\n        // Block reports for provided storage are not\n        // maintained by DN heartbeats\n        if (!StorageType.PROVIDED.equals(storageInfo.getStorageType())) {\n          invalidatedBlocks = processReport(storageInfo, newReport);\n        }\n      }\n      storageInfo.receivedBlockReport();", "child_info": "node name isBlockManager:addStoredBlock(BlockInfo,DatanodeStorageInfo,DatanodeDescriptor,boolean)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] BlockManager:addStoredBlock(...)\n  ...\n      </exec_flow>\n      <log_sequence>\n[ENTRY] BlockManager:addStoredBlock(...)\n  ...\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```source code:* Faster version of {@link #addStoredBlock},\n   * intended for use with initial block report at startup. If not in startup\n   * safe mode, will call standard addStoredBlock(). Assumes this method is\n   * called \"immediately\" so there is no need to refresh the storedBlock from\n   * blocksMap. Doesn't handle low redundancy/extra redundancy, or worry about\n   * pendingReplications or corruptReplicas, because it's in startup safe mode.\n   * Doesn't log every block, because there are typically millions of them.\n   * \n   * @throws IOException\n   */\n  private void addStoredBlockImmediate(BlockInfo storedBlock, Block reported,\n      DatanodeStorageInfo storageInfo)\n  throws IOException {\n    assert (storedBlock != null && namesystem.hasWriteLock());\n    if (!namesystem.isInStartupSafeMode()\n        || isPopulatingReplQueues()) {\n      addStoredBlock(storedBlock, reported, storageInfo, null, false);\n      return;\n    }\n\n    // just add it\n    AddBlockResult result = storageInfo.addBlock(storedBlock, reported);\n\n    // Now check for completion of blocks and safe block count\n    int numCurrentReplica = countLiveNodes(storedBlock);\n    if (storedBlock.getBlockUCState() == BlockUCState.COMMITTED\n        && hasMinStorage(storedBlock, numCurrentReplica)) {\n      completeBlock(storedBlock, null, false);\n    } else if (storedBlock.isComplete() && result == AddBlockResult.ADDED) {\n      // check whether safe replication is reached for the block\n      // only complete blocks are counted towards that.\n      // In the case that the block just became complete above, completeBlock()\n      // handles the safe block count maintenance.\n      bmSafeMode.incrementSafeBlockCount(numCurrentReplica, storedBlock);\n    }\n  }\n\n  /**\n   * Modify (block-->datanode) map. Remove block from set of\n   * needed reconstruction if this takes care of the problem.\n   * @return the block that is stored in blocksMap.\n   */\n  private Block addStoredBlock(final BlockInfo block,\n                               final Block reportedBlock,\n                               DatanodeStorageInfo storageInfo,\n                               DatanodeDescriptor delNodeHint,\n                               boolean logEveryBlock)\n  throws IOException {\n    assert block != null && namesystem.hasWriteLock();\n    BlockInfo storedBlock;\n    DatanodeDescriptor node = storageInfo.getDatanodeDescriptor();\n    if (!block.isComplete()) {\n      //refresh our copy in case the block got completed in another thread\n      storedBlock = getStoredBlock(block);\n    } else {\n      storedBlock = block;\n    }\n    if (storedBlock == null || storedBlock.isDeleted()) {\n      // If this block does not belong to anyfile, then we are done.\n      blockLog.debug(\"BLOCK* addStoredBlock: {} on {} size {} but it does not\" +\n          \" belong to any file\", block, node, block.getNumBytes());"}
{"mp_id": "hdfs::BM::processReport->addStoredBlock-INFEASIBLE", "caller": "BlockManager:processReport(...)", "callee": "BlockManager:addStoredBlock(...)", "gt": "infeasible", "stratum": "simple", "infeasible_reason": "blockManager.isBlockInInvalidate(blockId)==true — blocks queued for invalidation are skipped.", "rationale": "", "caller_code": "* @throws IOException\n   */\n  public boolean processReport(final DatanodeID nodeID,\n      final DatanodeStorage storage,\n      final BlockListAsLongs newReport,\n      BlockReportContext context) throws IOException {\n    namesystem.writeLock();\n    final long startTime = Time.monotonicNow(); //after acquiring write lock\n    final long endTime;\n    DatanodeDescriptor node;\n    Collection<Block> invalidatedBlocks = Collections.emptyList();\n    String strBlockReportId =\n        context != null ? Long.toHexString(context.getReportId()) : \"\";\n    String fullBrLeaseId =\n        context != null ? Long.toHexString(context.getLeaseId()) : \"\";\n\n    try {\n      node = datanodeManager.getDatanode(nodeID);\n      if (node == null || !node.isRegistered()) {\n        throw new IOException(\n            \"ProcessReport from dead or unregistered node: \" + nodeID);\n      }\n\n      // To minimize startup time, we discard any second (or later) block reports\n      // that we receive while still in startup phase.\n      // Register DN with provided storage, not with storage owned by DN\n      // DN should still have a ref to the DNStorageInfo.\n      DatanodeStorageInfo storageInfo =\n          providedStorageMap.getStorage(node, storage);\n\n      if (storageInfo == null) {\n        // We handle this for backwards compatibility.\n        storageInfo = node.updateStorage(storage);\n      }\n      if (namesystem.isInStartupSafeMode()\n          && !StorageType.PROVIDED.equals(storageInfo.getStorageType())\n          && storageInfo.getBlockReportCount() > 0) {\n        blockLog.info(\"BLOCK* processReport 0x{} with lease ID 0x{}: \"\n            + \"discarded non-initial block report from {}\"\n            + \" because namenode still in startup phase\",\n            strBlockReportId, fullBrLeaseId, nodeID);\n        blockReportLeaseManager.removeLease(node);\n        return !node.hasStaleStorages();\n      }\n\n      if (storageInfo.getBlockReportCount() == 0) {\n        // The first block report can be processed a lot more efficiently than\n        // ordinary block reports.  This shortens restart times.\n        blockLog.info(\"BLOCK* processReport 0x{} with lease ID 0x{}: Processing first \"\n            + \"storage report for {} from datanode {}\",\n            strBlockReportId, fullBrLeaseId,\n            storageInfo.getStorageID(),\n            nodeID);\n        processFirstBlockReport(storageInfo, newReport);\n      } else {\n        // Block reports for provided storage are not\n        // maintained by DN heartbeats\n        if (!StorageType.PROVIDED.equals(storageInfo.getStorageType())) {\n          invalidatedBlocks = processReport(storageInfo, newReport);\n        }\n      }\n      storageInfo.receivedBlockReport();", "callee_code": "* Faster version of {@link #addStoredBlock},\n   * intended for use with initial block report at startup. If not in startup\n   * safe mode, will call standard addStoredBlock(). Assumes this method is\n   * called \"immediately\" so there is no need to refresh the storedBlock from\n   * blocksMap. Doesn't handle low redundancy/extra redundancy, or worry about\n   * pendingReplications or corruptReplicas, because it's in startup safe mode.\n   * Doesn't log every block, because there are typically millions of them.\n   * \n   * @throws IOException\n   */\n  private void addStoredBlockImmediate(BlockInfo storedBlock, Block reported,\n      DatanodeStorageInfo storageInfo)\n  throws IOException {\n    assert (storedBlock != null && namesystem.hasWriteLock());\n    if (!namesystem.isInStartupSafeMode()\n        || isPopulatingReplQueues()) {\n      addStoredBlock(storedBlock, reported, storageInfo, null, false);\n      return;\n    }\n\n    // just add it\n    AddBlockResult result = storageInfo.addBlock(storedBlock, reported);\n\n    // Now check for completion of blocks and safe block count\n    int numCurrentReplica = countLiveNodes(storedBlock);\n    if (storedBlock.getBlockUCState() == BlockUCState.COMMITTED\n        && hasMinStorage(storedBlock, numCurrentReplica)) {\n      completeBlock(storedBlock, null, false);\n    } else if (storedBlock.isComplete() && result == AddBlockResult.ADDED) {\n      // check whether safe replication is reached for the block\n      // only complete blocks are counted towards that.\n      // In the case that the block just became complete above, completeBlock()\n      // handles the safe block count maintenance.\n      bmSafeMode.incrementSafeBlockCount(numCurrentReplica, storedBlock);\n    }\n  }\n\n  /**\n   * Modify (block-->datanode) map. Remove block from set of\n   * needed reconstruction if this takes care of the problem.\n   * @return the block that is stored in blocksMap.\n   */\n  private Block addStoredBlock(final BlockInfo block,\n                               final Block reportedBlock,\n                               DatanodeStorageInfo storageInfo,\n                               DatanodeDescriptor delNodeHint,\n                               boolean logEveryBlock)\n  throws IOException {\n    assert block != null && namesystem.hasWriteLock();\n    BlockInfo storedBlock;\n    DatanodeDescriptor node = storageInfo.getDatanodeDescriptor();\n    if (!block.isComplete()) {\n      //refresh our copy in case the block got completed in another thread\n      storedBlock = getStoredBlock(block);\n    } else {\n      storedBlock = block;\n    }\n    if (storedBlock == null || storedBlock.isDeleted()) {\n      // If this block does not belong to anyfile, then we are done.\n      blockLog.debug(\"BLOCK* addStoredBlock: {} on {} size {} but it does not\" +\n          \" belong to any file\", block, node, block.getNumBytes());", "caller_log_raw_stripped": "[ENTRY] BlockManager:processReport(...)\n  FOR each block:\n  IF: isBlockInInvalidate(blockId)==true  -> continue (skip)", "callee_log_raw_stripped": "[ENTRY] BlockManager:addStoredBlock(...)", "caller_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] BlockManager:processReport(...)\n  FOR each block:\n  IF: isBlockInInvalidate(blockId)==true  -> continue (skip)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] BlockManager:processReport(...)\n  FOR each block:\n  IF: isBlockInInvalidate(blockId)==true  -> continue (skip)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "callee_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] BlockManager:addStoredBlock(...)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] BlockManager:addStoredBlock(...)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "parent_info": "node name is BlockManager:processReport(...)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] BlockManager:processReport(...)\n  FOR each block:\n  IF: isBlockInInvalidate(blockId)==true  -> continue (skip)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] BlockManager:processReport(...)\n  FOR each block:\n  IF: isBlockInInvalidate(blockId)==true  -> continue (skip)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```souce code:* @throws IOException\n   */\n  public boolean processReport(final DatanodeID nodeID,\n      final DatanodeStorage storage,\n      final BlockListAsLongs newReport,\n      BlockReportContext context) throws IOException {\n    namesystem.writeLock();\n    final long startTime = Time.monotonicNow(); //after acquiring write lock\n    final long endTime;\n    DatanodeDescriptor node;\n    Collection<Block> invalidatedBlocks = Collections.emptyList();\n    String strBlockReportId =\n        context != null ? Long.toHexString(context.getReportId()) : \"\";\n    String fullBrLeaseId =\n        context != null ? Long.toHexString(context.getLeaseId()) : \"\";\n\n    try {\n      node = datanodeManager.getDatanode(nodeID);\n      if (node == null || !node.isRegistered()) {\n        throw new IOException(\n            \"ProcessReport from dead or unregistered node: \" + nodeID);\n      }\n\n      // To minimize startup time, we discard any second (or later) block reports\n      // that we receive while still in startup phase.\n      // Register DN with provided storage, not with storage owned by DN\n      // DN should still have a ref to the DNStorageInfo.\n      DatanodeStorageInfo storageInfo =\n          providedStorageMap.getStorage(node, storage);\n\n      if (storageInfo == null) {\n        // We handle this for backwards compatibility.\n        storageInfo = node.updateStorage(storage);\n      }\n      if (namesystem.isInStartupSafeMode()\n          && !StorageType.PROVIDED.equals(storageInfo.getStorageType())\n          && storageInfo.getBlockReportCount() > 0) {\n        blockLog.info(\"BLOCK* processReport 0x{} with lease ID 0x{}: \"\n            + \"discarded non-initial block report from {}\"\n            + \" because namenode still in startup phase\",\n            strBlockReportId, fullBrLeaseId, nodeID);\n        blockReportLeaseManager.removeLease(node);\n        return !node.hasStaleStorages();\n      }\n\n      if (storageInfo.getBlockReportCount() == 0) {\n        // The first block report can be processed a lot more efficiently than\n        // ordinary block reports.  This shortens restart times.\n        blockLog.info(\"BLOCK* processReport 0x{} with lease ID 0x{}: Processing first \"\n            + \"storage report for {} from datanode {}\",\n            strBlockReportId, fullBrLeaseId,\n            storageInfo.getStorageID(),\n            nodeID);\n        processFirstBlockReport(storageInfo, newReport);\n      } else {\n        // Block reports for provided storage are not\n        // maintained by DN heartbeats\n        if (!StorageType.PROVIDED.equals(storageInfo.getStorageType())) {\n          invalidatedBlocks = processReport(storageInfo, newReport);\n        }\n      }\n      storageInfo.receivedBlockReport();", "child_info": "node name isBlockManager:addStoredBlock(...)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] BlockManager:addStoredBlock(...)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] BlockManager:addStoredBlock(...)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```source code:* Faster version of {@link #addStoredBlock},\n   * intended for use with initial block report at startup. If not in startup\n   * safe mode, will call standard addStoredBlock(). Assumes this method is\n   * called \"immediately\" so there is no need to refresh the storedBlock from\n   * blocksMap. Doesn't handle low redundancy/extra redundancy, or worry about\n   * pendingReplications or corruptReplicas, because it's in startup safe mode.\n   * Doesn't log every block, because there are typically millions of them.\n   * \n   * @throws IOException\n   */\n  private void addStoredBlockImmediate(BlockInfo storedBlock, Block reported,\n      DatanodeStorageInfo storageInfo)\n  throws IOException {\n    assert (storedBlock != null && namesystem.hasWriteLock());\n    if (!namesystem.isInStartupSafeMode()\n        || isPopulatingReplQueues()) {\n      addStoredBlock(storedBlock, reported, storageInfo, null, false);\n      return;\n    }\n\n    // just add it\n    AddBlockResult result = storageInfo.addBlock(storedBlock, reported);\n\n    // Now check for completion of blocks and safe block count\n    int numCurrentReplica = countLiveNodes(storedBlock);\n    if (storedBlock.getBlockUCState() == BlockUCState.COMMITTED\n        && hasMinStorage(storedBlock, numCurrentReplica)) {\n      completeBlock(storedBlock, null, false);\n    } else if (storedBlock.isComplete() && result == AddBlockResult.ADDED) {\n      // check whether safe replication is reached for the block\n      // only complete blocks are counted towards that.\n      // In the case that the block just became complete above, completeBlock()\n      // handles the safe block count maintenance.\n      bmSafeMode.incrementSafeBlockCount(numCurrentReplica, storedBlock);\n    }\n  }\n\n  /**\n   * Modify (block-->datanode) map. Remove block from set of\n   * needed reconstruction if this takes care of the problem.\n   * @return the block that is stored in blocksMap.\n   */\n  private Block addStoredBlock(final BlockInfo block,\n                               final Block reportedBlock,\n                               DatanodeStorageInfo storageInfo,\n                               DatanodeDescriptor delNodeHint,\n                               boolean logEveryBlock)\n  throws IOException {\n    assert block != null && namesystem.hasWriteLock();\n    BlockInfo storedBlock;\n    DatanodeDescriptor node = storageInfo.getDatanodeDescriptor();\n    if (!block.isComplete()) {\n      //refresh our copy in case the block got completed in another thread\n      storedBlock = getStoredBlock(block);\n    } else {\n      storedBlock = block;\n    }\n    if (storedBlock == null || storedBlock.isDeleted()) {\n      // If this block does not belong to anyfile, then we are done.\n      blockLog.debug(\"BLOCK* addStoredBlock: {} on {} size {} but it does not\" +\n          \" belong to any file\", block, node, block.getNumBytes());"}
{"mp_id": "hdfs::BM::removeStoredBlock->DSI::removeBlock", "caller": "BlockManager:removeStoredBlock(BlockInfo,DatanodeDescriptor)", "callee": "DatanodeStorageInfo:removeBlock(BlockInfo)", "gt": "valid", "stratum": "simple", "infeasible_reason": "", "rationale": "Unregisters block when datanode reports removal/corruption.", "caller_code": "}\n\n  private void removeStoredBlock(DatanodeStorageInfo storageInfo, Block block,\n      DatanodeDescriptor node) {\n    if (shouldPostponeBlocksFromFuture && isGenStampInFuture(block)) {\n      queueReportedBlock(storageInfo, block, null,\n          QUEUE_REASON_FUTURE_GENSTAMP);\n      return;\n    }\n    removeStoredBlock(getStoredBlock(block), node);\n  }\n\n  /**\n   * Modify (block{@literal -->}datanode) map. Possibly generate replication\n   * tasks, if the removed block is still valid.\n   */\n  public void removeStoredBlock(BlockInfo storedBlock, DatanodeDescriptor node) {\n    blockLog.debug(\"BLOCK* removeStoredBlock: {} from {}\", storedBlock, node);\n    assert (namesystem.hasWriteLock());\n    {\n      if (storedBlock == null || !blocksMap.removeNode(storedBlock, node)) {\n        blockLog.debug(\"BLOCK* removeStoredBlock: {} has already been\" +\n            \" removed from node {}\", storedBlock, node);\n        return;\n      }\n\n      CachedBlock cblock = namesystem.getCacheManager().getCachedBlocks()\n          .get(new CachedBlock(storedBlock.getBlockId(), (short) 0, false));\n      if (cblock != null) {\n        boolean removed = false;\n        removed |= node.getPendingCached().remove(cblock);\n        removed |= node.getCached().remove(cblock);\n        removed |= node.getPendingUncached().remove(cblock);\n        if (removed) {\n          blockLog.debug(\"BLOCK* removeStoredBlock: {} removed from caching \"\n              + \"related lists on node {}\", storedBlock, node);\n        }\n      }\n\n      //\n      // It's possible that the block was removed because of a datanode\n      // failure. If the block is still valid, check if replication is\n      // necessary. In that case, put block on a possibly-will-\n      // be-replicated list.\n      //\n      if (!storedBlock.isDeleted()) {\n        bmSafeMode.decrementSafeBlockCount(storedBlock);\n        updateNeededReconstructions(storedBlock, -1, 0);\n      }\n\n      excessRedundancyMap.remove(node, storedBlock);\n      corruptReplicas.removeFromCorruptReplicasMap(storedBlock, node);\n    }\n  }\n\n  private void removeStaleReplicas(List<ReplicaUnderConstruction> staleReplicas,\n      BlockInfo block) {\n    for (ReplicaUnderConstruction r : staleReplicas) {\n      removeStoredBlock(block,\n          r.getExpectedStorageLocation().getDatanodeDescriptor());\n      NameNode.blockStateChangeLog\n          .debug(\"BLOCK* Removing stale replica {}\" + \" of {}\", r,", "callee_code": "numBlocks++;\n  }\n  boolean removeBlock(BlockInfo b) {\n    blockList = b.listRemove(blockList, this);\n    if (b.removeStorage(this)) {\n      numBlocks--;\n      return true;\n    } else {\n      return false;\n    }\n  }\n\n  int numBlocks() {\n    return numBlocks;\n  }\n\n  Iterator<BlockInfo> getBlockIterator() {\n    return new BlockIterator(blockList);\n  }\n\n  /**\n   * Move block to the head of the list of blocks belonging to the data-node.\n   * @return the index of the head of the blockList\n   */\n  int moveBlockToHead(BlockInfo b, int curIndex, int headIndex) {\n    blockList = b.moveBlockToHead(blockList, this, curIndex, headIndex);\n    return curIndex;\n  }\n\n\n  /**\n   * Used for testing only.\n   * @return the head of the blockList\n   */\n  @VisibleForTesting\n  BlockInfo getBlockListHeadForTesting(){\n    return blockList;\n  }\n\n  void updateState(StorageReport r) {\n    capacity = r.getCapacity();\n    dfsUsed = r.getDfsUsed();\n    nonDfsUsed = r.getNonDfsUsed();\n    remaining = r.getRemaining();\n    blockPoolUsed = r.getBlockPoolUsed();\n  }\n\n  public DatanodeDescriptor getDatanodeDescriptor() {\n    return dn;\n  }\n\n  /** Increment the number of blocks scheduled for each given storage */ \n  public static void incrementBlocksScheduled(DatanodeStorageInfo... storages) {\n    for (DatanodeStorageInfo s : storages) {\n      s.getDatanodeDescriptor().incrementBlocksScheduled(s.getStorageType());\n    }\n  }\n\n  /**\n   * Decrement the number of blocks scheduled for each given storage. This will\n   * be called during abandon block or delete of UC block.\n   */", "caller_log_raw_stripped": "[ENTRY] BlockManager:removeStoredBlock(...)\n  CALL: storageInfo.removeBlock(block)", "callee_log_raw_stripped": "[ENTRY] DatanodeStorageInfo:removeBlock(BlockInfo)\n  this.blocks.remove(block)\n  EXIT", "caller_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] BlockManager:removeStoredBlock(...)\n  CALL: storageInfo.removeBlock(block)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] BlockManager:removeStoredBlock(...)\n  CALL: storageInfo.removeBlock(block)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "callee_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DatanodeStorageInfo:removeBlock(BlockInfo)\n  this.blocks.remove(block)\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DatanodeStorageInfo:removeBlock(BlockInfo)\n  this.blocks.remove(block)\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "parent_info": "node name is BlockManager:removeStoredBlock(BlockInfo,DatanodeDescriptor)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] BlockManager:removeStoredBlock(...)\n  CALL: storageInfo.removeBlock(block)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] BlockManager:removeStoredBlock(...)\n  CALL: storageInfo.removeBlock(block)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```souce code:}\n\n  private void removeStoredBlock(DatanodeStorageInfo storageInfo, Block block,\n      DatanodeDescriptor node) {\n    if (shouldPostponeBlocksFromFuture && isGenStampInFuture(block)) {\n      queueReportedBlock(storageInfo, block, null,\n          QUEUE_REASON_FUTURE_GENSTAMP);\n      return;\n    }\n    removeStoredBlock(getStoredBlock(block), node);\n  }\n\n  /**\n   * Modify (block{@literal -->}datanode) map. Possibly generate replication\n   * tasks, if the removed block is still valid.\n   */\n  public void removeStoredBlock(BlockInfo storedBlock, DatanodeDescriptor node) {\n    blockLog.debug(\"BLOCK* removeStoredBlock: {} from {}\", storedBlock, node);\n    assert (namesystem.hasWriteLock());\n    {\n      if (storedBlock == null || !blocksMap.removeNode(storedBlock, node)) {\n        blockLog.debug(\"BLOCK* removeStoredBlock: {} has already been\" +\n            \" removed from node {}\", storedBlock, node);\n        return;\n      }\n\n      CachedBlock cblock = namesystem.getCacheManager().getCachedBlocks()\n          .get(new CachedBlock(storedBlock.getBlockId(), (short) 0, false));\n      if (cblock != null) {\n        boolean removed = false;\n        removed |= node.getPendingCached().remove(cblock);\n        removed |= node.getCached().remove(cblock);\n        removed |= node.getPendingUncached().remove(cblock);\n        if (removed) {\n          blockLog.debug(\"BLOCK* removeStoredBlock: {} removed from caching \"\n              + \"related lists on node {}\", storedBlock, node);\n        }\n      }\n\n      //\n      // It's possible that the block was removed because of a datanode\n      // failure. If the block is still valid, check if replication is\n      // necessary. In that case, put block on a possibly-will-\n      // be-replicated list.\n      //\n      if (!storedBlock.isDeleted()) {\n        bmSafeMode.decrementSafeBlockCount(storedBlock);\n        updateNeededReconstructions(storedBlock, -1, 0);\n      }\n\n      excessRedundancyMap.remove(node, storedBlock);\n      corruptReplicas.removeFromCorruptReplicasMap(storedBlock, node);\n    }\n  }\n\n  private void removeStaleReplicas(List<ReplicaUnderConstruction> staleReplicas,\n      BlockInfo block) {\n    for (ReplicaUnderConstruction r : staleReplicas) {\n      removeStoredBlock(block,\n          r.getExpectedStorageLocation().getDatanodeDescriptor());\n      NameNode.blockStateChangeLog\n          .debug(\"BLOCK* Removing stale replica {}\" + \" of {}\", r,", "child_info": "node name isDatanodeStorageInfo:removeBlock(BlockInfo)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DatanodeStorageInfo:removeBlock(BlockInfo)\n  this.blocks.remove(block)\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DatanodeStorageInfo:removeBlock(BlockInfo)\n  this.blocks.remove(block)\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```source code:numBlocks++;\n  }\n  boolean removeBlock(BlockInfo b) {\n    blockList = b.listRemove(blockList, this);\n    if (b.removeStorage(this)) {\n      numBlocks--;\n      return true;\n    } else {\n      return false;\n    }\n  }\n\n  int numBlocks() {\n    return numBlocks;\n  }\n\n  Iterator<BlockInfo> getBlockIterator() {\n    return new BlockIterator(blockList);\n  }\n\n  /**\n   * Move block to the head of the list of blocks belonging to the data-node.\n   * @return the index of the head of the blockList\n   */\n  int moveBlockToHead(BlockInfo b, int curIndex, int headIndex) {\n    blockList = b.moveBlockToHead(blockList, this, curIndex, headIndex);\n    return curIndex;\n  }\n\n\n  /**\n   * Used for testing only.\n   * @return the head of the blockList\n   */\n  @VisibleForTesting\n  BlockInfo getBlockListHeadForTesting(){\n    return blockList;\n  }\n\n  void updateState(StorageReport r) {\n    capacity = r.getCapacity();\n    dfsUsed = r.getDfsUsed();\n    nonDfsUsed = r.getNonDfsUsed();\n    remaining = r.getRemaining();\n    blockPoolUsed = r.getBlockPoolUsed();\n  }\n\n  public DatanodeDescriptor getDatanodeDescriptor() {\n    return dn;\n  }\n\n  /** Increment the number of blocks scheduled for each given storage */ \n  public static void incrementBlocksScheduled(DatanodeStorageInfo... storages) {\n    for (DatanodeStorageInfo s : storages) {\n      s.getDatanodeDescriptor().incrementBlocksScheduled(s.getStorageType());\n    }\n  }\n\n  /**\n   * Decrement the number of blocks scheduled for each given storage. This will\n   * be called during abandon block or delete of UC block.\n   */"}
{"mp_id": "hdfs::BM::reportDiff->DSI::removeBlock", "caller": "BlockManager:reportDiff(DatanodeStorageInfo,BlockListAsLongs,BlockListAsLongs,int)", "callee": "DatanodeStorageInfo:removeBlock(BlockInfo)", "gt": "valid", "stratum": "simple", "infeasible_reason": "", "rationale": "Removes blocks that appear in previous report but not current.", "caller_code": "}\n\n  private void reportDiff(DatanodeStorageInfo storageInfo,\n      BlockListAsLongs newReport,\n      Collection<BlockInfoToAdd> toAdd,     // add to DatanodeDescriptor\n      Collection<BlockInfo> toRemove,       // remove from DatanodeDescriptor\n      Collection<Block> toInvalidate,       // should be removed from DN\n      Collection<BlockToMarkCorrupt> toCorrupt, // add to corrupt replicas list\n      Collection<StatefulBlockInfo> toUC) { // add to under-construction list\n\n    // place a delimiter in the list which separates blocks\n    // that have been reported from those that have not\n    DatanodeDescriptor dn = storageInfo.getDatanodeDescriptor();\n    Block delimiterBlock = new Block();\n    BlockInfo delimiter = new BlockInfoContiguous(delimiterBlock,\n        (short) 1);\n    AddBlockResult result = storageInfo.addBlock(delimiter, delimiterBlock);\n    assert result == AddBlockResult.ADDED\n        : \"Delimiting block cannot be present in the node\";\n    int headIndex = 0; //currently the delimiter is in the head of the list\n    int curIndex;\n\n    if (newReport == null) {\n      newReport = BlockListAsLongs.EMPTY;\n    }\n    // scan the report and process newly reported blocks\n    for (BlockReportReplica iblk : newReport) {\n      ReplicaState iState = iblk.getState();\n      LOG.debug(\"Reported block {} on {} size {} replicaState = {}\", iblk, dn,\n          iblk.getNumBytes(), iState);\n      BlockInfo storedBlock = processReportedBlock(storageInfo,\n          iblk, iState, toAdd, toInvalidate, toCorrupt, toUC);\n\n      // move block to the head of the list\n      if (storedBlock != null) {\n        curIndex = storedBlock.findStorageInfo(storageInfo);\n        if (curIndex >= 0) {\n          headIndex =\n              storageInfo.moveBlockToHead(storedBlock, curIndex, headIndex);\n        }\n      }\n    }\n\n    // collect blocks that have not been reported\n    // all of them are next to the delimiter\n    Iterator<BlockInfo> it =\n        storageInfo.new BlockIterator(delimiter.getNext(0));\n    while (it.hasNext()) {\n      toRemove.add(it.next());\n    }\n    storageInfo.removeBlock(delimiter);\n  }\n\n  /**\n   * Process a block replica reported by the data-node.\n   * No side effects except adding to the passed-in Collections.\n   *\n   * <ol>\n   * <li>If the block is not known to the system (not in blocksMap) then the\n   * data-node should be notified to invalidate this block.</li>\n   * <li>If the reported replica is valid that is has the same generation stamp\n   * and length as recorded on the name-node, then the replica location should", "callee_code": "numBlocks++;\n  }\n  boolean removeBlock(BlockInfo b) {\n    blockList = b.listRemove(blockList, this);\n    if (b.removeStorage(this)) {\n      numBlocks--;\n      return true;\n    } else {\n      return false;\n    }\n  }\n\n  int numBlocks() {\n    return numBlocks;\n  }\n\n  Iterator<BlockInfo> getBlockIterator() {\n    return new BlockIterator(blockList);\n  }\n\n  /**\n   * Move block to the head of the list of blocks belonging to the data-node.\n   * @return the index of the head of the blockList\n   */\n  int moveBlockToHead(BlockInfo b, int curIndex, int headIndex) {\n    blockList = b.moveBlockToHead(blockList, this, curIndex, headIndex);\n    return curIndex;\n  }\n\n\n  /**\n   * Used for testing only.\n   * @return the head of the blockList\n   */\n  @VisibleForTesting\n  BlockInfo getBlockListHeadForTesting(){\n    return blockList;\n  }\n\n  void updateState(StorageReport r) {\n    capacity = r.getCapacity();\n    dfsUsed = r.getDfsUsed();\n    nonDfsUsed = r.getNonDfsUsed();\n    remaining = r.getRemaining();\n    blockPoolUsed = r.getBlockPoolUsed();\n  }\n\n  public DatanodeDescriptor getDatanodeDescriptor() {\n    return dn;\n  }\n\n  /** Increment the number of blocks scheduled for each given storage */ \n  public static void incrementBlocksScheduled(DatanodeStorageInfo... storages) {\n    for (DatanodeStorageInfo s : storages) {\n      s.getDatanodeDescriptor().incrementBlocksScheduled(s.getStorageType());\n    }\n  }\n\n  /**\n   * Decrement the number of blocks scheduled for each given storage. This will\n   * be called during abandon block or delete of UC block.\n   */", "caller_log_raw_stripped": "[ENTRY] BlockManager:reportDiff(...)\n  FOR block in (prev - current):\n  CALL: storageInfo.removeBlock(block)", "callee_log_raw_stripped": "[ENTRY] DatanodeStorageInfo:removeBlock(BlockInfo)\n  this.blocks.remove(block)\n  EXIT", "caller_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] BlockManager:reportDiff(...)\n  FOR block in (prev - current):\n  CALL: storageInfo.removeBlock(block)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] BlockManager:reportDiff(...)\n  FOR block in (prev - current):\n  CALL: storageInfo.removeBlock(block)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "callee_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DatanodeStorageInfo:removeBlock(BlockInfo)\n  this.blocks.remove(block)\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DatanodeStorageInfo:removeBlock(BlockInfo)\n  this.blocks.remove(block)\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "parent_info": "node name is BlockManager:reportDiff(DatanodeStorageInfo,BlockListAsLongs,BlockListAsLongs,int)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] BlockManager:reportDiff(...)\n  FOR block in (prev - current):\n  CALL: storageInfo.removeBlock(block)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] BlockManager:reportDiff(...)\n  FOR block in (prev - current):\n  CALL: storageInfo.removeBlock(block)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```souce code:}\n\n  private void reportDiff(DatanodeStorageInfo storageInfo,\n      BlockListAsLongs newReport,\n      Collection<BlockInfoToAdd> toAdd,     // add to DatanodeDescriptor\n      Collection<BlockInfo> toRemove,       // remove from DatanodeDescriptor\n      Collection<Block> toInvalidate,       // should be removed from DN\n      Collection<BlockToMarkCorrupt> toCorrupt, // add to corrupt replicas list\n      Collection<StatefulBlockInfo> toUC) { // add to under-construction list\n\n    // place a delimiter in the list which separates blocks\n    // that have been reported from those that have not\n    DatanodeDescriptor dn = storageInfo.getDatanodeDescriptor();\n    Block delimiterBlock = new Block();\n    BlockInfo delimiter = new BlockInfoContiguous(delimiterBlock,\n        (short) 1);\n    AddBlockResult result = storageInfo.addBlock(delimiter, delimiterBlock);\n    assert result == AddBlockResult.ADDED\n        : \"Delimiting block cannot be present in the node\";\n    int headIndex = 0; //currently the delimiter is in the head of the list\n    int curIndex;\n\n    if (newReport == null) {\n      newReport = BlockListAsLongs.EMPTY;\n    }\n    // scan the report and process newly reported blocks\n    for (BlockReportReplica iblk : newReport) {\n      ReplicaState iState = iblk.getState();\n      LOG.debug(\"Reported block {} on {} size {} replicaState = {}\", iblk, dn,\n          iblk.getNumBytes(), iState);\n      BlockInfo storedBlock = processReportedBlock(storageInfo,\n          iblk, iState, toAdd, toInvalidate, toCorrupt, toUC);\n\n      // move block to the head of the list\n      if (storedBlock != null) {\n        curIndex = storedBlock.findStorageInfo(storageInfo);\n        if (curIndex >= 0) {\n          headIndex =\n              storageInfo.moveBlockToHead(storedBlock, curIndex, headIndex);\n        }\n      }\n    }\n\n    // collect blocks that have not been reported\n    // all of them are next to the delimiter\n    Iterator<BlockInfo> it =\n        storageInfo.new BlockIterator(delimiter.getNext(0));\n    while (it.hasNext()) {\n      toRemove.add(it.next());\n    }\n    storageInfo.removeBlock(delimiter);\n  }\n\n  /**\n   * Process a block replica reported by the data-node.\n   * No side effects except adding to the passed-in Collections.\n   *\n   * <ol>\n   * <li>If the block is not known to the system (not in blocksMap) then the\n   * data-node should be notified to invalidate this block.</li>\n   * <li>If the reported replica is valid that is has the same generation stamp\n   * and length as recorded on the name-node, then the replica location should", "child_info": "node name isDatanodeStorageInfo:removeBlock(BlockInfo)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DatanodeStorageInfo:removeBlock(BlockInfo)\n  this.blocks.remove(block)\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DatanodeStorageInfo:removeBlock(BlockInfo)\n  this.blocks.remove(block)\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```source code:numBlocks++;\n  }\n  boolean removeBlock(BlockInfo b) {\n    blockList = b.listRemove(blockList, this);\n    if (b.removeStorage(this)) {\n      numBlocks--;\n      return true;\n    } else {\n      return false;\n    }\n  }\n\n  int numBlocks() {\n    return numBlocks;\n  }\n\n  Iterator<BlockInfo> getBlockIterator() {\n    return new BlockIterator(blockList);\n  }\n\n  /**\n   * Move block to the head of the list of blocks belonging to the data-node.\n   * @return the index of the head of the blockList\n   */\n  int moveBlockToHead(BlockInfo b, int curIndex, int headIndex) {\n    blockList = b.moveBlockToHead(blockList, this, curIndex, headIndex);\n    return curIndex;\n  }\n\n\n  /**\n   * Used for testing only.\n   * @return the head of the blockList\n   */\n  @VisibleForTesting\n  BlockInfo getBlockListHeadForTesting(){\n    return blockList;\n  }\n\n  void updateState(StorageReport r) {\n    capacity = r.getCapacity();\n    dfsUsed = r.getDfsUsed();\n    nonDfsUsed = r.getNonDfsUsed();\n    remaining = r.getRemaining();\n    blockPoolUsed = r.getBlockPoolUsed();\n  }\n\n  public DatanodeDescriptor getDatanodeDescriptor() {\n    return dn;\n  }\n\n  /** Increment the number of blocks scheduled for each given storage */ \n  public static void incrementBlocksScheduled(DatanodeStorageInfo... storages) {\n    for (DatanodeStorageInfo s : storages) {\n      s.getDatanodeDescriptor().incrementBlocksScheduled(s.getStorageType());\n    }\n  }\n\n  /**\n   * Decrement the number of blocks scheduled for each given storage. This will\n   * be called during abandon block or delete of UC block.\n   */"}
{"mp_id": "hdfs::BM::addStoredBlock->corruptReplicasRemoveFromMap", "caller": "BlockManager:addStoredBlock(BlockInfo,DatanodeStorageInfo,DatanodeDescriptor,boolean)", "callee": "CorruptReplicasMap:removeFromCorruptReplicasMap(Block,DatanodeDescriptor,Reason)", "gt": "valid", "stratum": "complex", "infeasible_reason": "", "rationale": "When result==ADDED and storedBlock was previously corrupt, removes from corrupt map.", "caller_code": "* Faster version of {@link #addStoredBlock},\n   * intended for use with initial block report at startup. If not in startup\n   * safe mode, will call standard addStoredBlock(). Assumes this method is\n   * called \"immediately\" so there is no need to refresh the storedBlock from\n   * blocksMap. Doesn't handle low redundancy/extra redundancy, or worry about\n   * pendingReplications or corruptReplicas, because it's in startup safe mode.\n   * Doesn't log every block, because there are typically millions of them.\n   * \n   * @throws IOException\n   */\n  private void addStoredBlockImmediate(BlockInfo storedBlock, Block reported,\n      DatanodeStorageInfo storageInfo)\n  throws IOException {\n    assert (storedBlock != null && namesystem.hasWriteLock());\n    if (!namesystem.isInStartupSafeMode()\n        || isPopulatingReplQueues()) {\n      addStoredBlock(storedBlock, reported, storageInfo, null, false);\n      return;\n    }\n\n    // just add it\n    AddBlockResult result = storageInfo.addBlock(storedBlock, reported);\n\n    // Now check for completion of blocks and safe block count\n    int numCurrentReplica = countLiveNodes(storedBlock);\n    if (storedBlock.getBlockUCState() == BlockUCState.COMMITTED\n        && hasMinStorage(storedBlock, numCurrentReplica)) {\n      completeBlock(storedBlock, null, false);\n    } else if (storedBlock.isComplete() && result == AddBlockResult.ADDED) {\n      // check whether safe replication is reached for the block\n      // only complete blocks are counted towards that.\n      // In the case that the block just became complete above, completeBlock()\n      // handles the safe block count maintenance.\n      bmSafeMode.incrementSafeBlockCount(numCurrentReplica, storedBlock);\n    }\n  }\n\n  /**\n   * Modify (block-->datanode) map. Remove block from set of\n   * needed reconstruction if this takes care of the problem.\n   * @return the block that is stored in blocksMap.\n   */\n  private Block addStoredBlock(final BlockInfo block,\n                               final Block reportedBlock,\n                               DatanodeStorageInfo storageInfo,\n                               DatanodeDescriptor delNodeHint,\n                               boolean logEveryBlock)\n  throws IOException {\n    assert block != null && namesystem.hasWriteLock();\n    BlockInfo storedBlock;\n    DatanodeDescriptor node = storageInfo.getDatanodeDescriptor();\n    if (!block.isComplete()) {\n      //refresh our copy in case the block got completed in another thread\n      storedBlock = getStoredBlock(block);\n    } else {\n      storedBlock = block;\n    }\n    if (storedBlock == null || storedBlock.isDeleted()) {\n      // If this block does not belong to anyfile, then we are done.\n      blockLog.debug(\"BLOCK* addStoredBlock: {} on {} size {} but it does not\" +\n          \" belong to any file\", block, node, block.getNumBytes());", "callee_code": "* @param blk Block to be removed\n   */\n  void removeFromCorruptReplicasMap(BlockInfo blk) {\n    if (corruptReplicasMap != null) {\n      Map<DatanodeDescriptor, Reason> value = corruptReplicasMap.remove(blk);\n      if (value != null) {\n        decrementBlockStat(blk.isStriped());\n      }\n    }\n  }\n\n  /**\n   * Remove the block at the given datanode from CorruptBlockMap\n   * @param blk block to be removed\n   * @param datanode datanode where the block is located\n   * @return true if the removal is successful; \n             false if the replica is not in the map\n   */ \n  boolean removeFromCorruptReplicasMap(\n      BlockInfo blk, DatanodeDescriptor datanode) {\n    return removeFromCorruptReplicasMap(blk, datanode, Reason.ANY);\n  }\n\n  boolean removeFromCorruptReplicasMap(\n      BlockInfo blk, DatanodeDescriptor datanode, Reason reason) {\n    Map <DatanodeDescriptor, Reason> datanodes = corruptReplicasMap.get(blk);\n    if (datanodes == null) {\n      return false;\n    }\n\n    // if reasons can be compared but don't match, return false.\n    Reason storedReason = datanodes.get(datanode);\n    if (reason != Reason.ANY && storedReason != null &&\n        reason != storedReason) {\n      return false;\n    }\n\n    if (datanodes.remove(datanode) != null) { // remove the replicas\n      if (datanodes.isEmpty()) {\n        // remove the block if there is no more corrupted replicas\n        corruptReplicasMap.remove(blk);\n        decrementBlockStat(blk.isStriped());\n      }\n      return true;\n    }\n    return false;\n  }\n\n  private void incrementBlockStat(boolean isStriped) {\n    if (isStriped) {\n      totalCorruptECBlockGroups.increment();\n    } else {\n      totalCorruptBlocks.increment();\n    }\n  }\n\n  private void decrementBlockStat(boolean isStriped) {\n    if (isStriped) {\n      totalCorruptECBlockGroups.decrement();\n    } else {\n      totalCorruptBlocks.decrement();\n    }", "caller_log_raw_stripped": "[ENTRY] BlockManager:addStoredBlock(...)\n  result = storageInfo.addBlock(...)\n  IF: result==ADDED && block was in corruptReplicas\n  CALL: corruptReplicas.removeFromCorruptReplicasMap(block,node,REASON)", "callee_log_raw_stripped": "[ENTRY] CorruptReplicasMap:removeFromCorruptReplicasMap(Block,...)\n  map.remove(block,...)\n  EXIT", "caller_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] BlockManager:addStoredBlock(...)\n  result = storageInfo.addBlock(...)\n  IF: result==ADDED && block was in corruptReplicas\n  CALL: corruptReplicas.removeFromCorruptReplicasMap(block,node,REASON)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] BlockManager:addStoredBlock(...)\n  result = storageInfo.addBlock(...)\n  IF: result==ADDED && block was in corruptReplicas\n  CALL: corruptReplicas.removeFromCorruptReplicasMap(block,node,REASON)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "callee_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] CorruptReplicasMap:removeFromCorruptReplicasMap(Block,...)\n  map.remove(block,...)\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] CorruptReplicasMap:removeFromCorruptReplicasMap(Block,...)\n  map.remove(block,...)\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "parent_info": "node name is BlockManager:addStoredBlock(BlockInfo,DatanodeStorageInfo,DatanodeDescriptor,boolean)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] BlockManager:addStoredBlock(...)\n  result = storageInfo.addBlock(...)\n  IF: result==ADDED && block was in corruptReplicas\n  CALL: corruptReplicas.removeFromCorruptReplicasMap(block,node,REASON)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] BlockManager:addStoredBlock(...)\n  result = storageInfo.addBlock(...)\n  IF: result==ADDED && block was in corruptReplicas\n  CALL: corruptReplicas.removeFromCorruptReplicasMap(block,node,REASON)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```souce code:* Faster version of {@link #addStoredBlock},\n   * intended for use with initial block report at startup. If not in startup\n   * safe mode, will call standard addStoredBlock(). Assumes this method is\n   * called \"immediately\" so there is no need to refresh the storedBlock from\n   * blocksMap. Doesn't handle low redundancy/extra redundancy, or worry about\n   * pendingReplications or corruptReplicas, because it's in startup safe mode.\n   * Doesn't log every block, because there are typically millions of them.\n   * \n   * @throws IOException\n   */\n  private void addStoredBlockImmediate(BlockInfo storedBlock, Block reported,\n      DatanodeStorageInfo storageInfo)\n  throws IOException {\n    assert (storedBlock != null && namesystem.hasWriteLock());\n    if (!namesystem.isInStartupSafeMode()\n        || isPopulatingReplQueues()) {\n      addStoredBlock(storedBlock, reported, storageInfo, null, false);\n      return;\n    }\n\n    // just add it\n    AddBlockResult result = storageInfo.addBlock(storedBlock, reported);\n\n    // Now check for completion of blocks and safe block count\n    int numCurrentReplica = countLiveNodes(storedBlock);\n    if (storedBlock.getBlockUCState() == BlockUCState.COMMITTED\n        && hasMinStorage(storedBlock, numCurrentReplica)) {\n      completeBlock(storedBlock, null, false);\n    } else if (storedBlock.isComplete() && result == AddBlockResult.ADDED) {\n      // check whether safe replication is reached for the block\n      // only complete blocks are counted towards that.\n      // In the case that the block just became complete above, completeBlock()\n      // handles the safe block count maintenance.\n      bmSafeMode.incrementSafeBlockCount(numCurrentReplica, storedBlock);\n    }\n  }\n\n  /**\n   * Modify (block-->datanode) map. Remove block from set of\n   * needed reconstruction if this takes care of the problem.\n   * @return the block that is stored in blocksMap.\n   */\n  private Block addStoredBlock(final BlockInfo block,\n                               final Block reportedBlock,\n                               DatanodeStorageInfo storageInfo,\n                               DatanodeDescriptor delNodeHint,\n                               boolean logEveryBlock)\n  throws IOException {\n    assert block != null && namesystem.hasWriteLock();\n    BlockInfo storedBlock;\n    DatanodeDescriptor node = storageInfo.getDatanodeDescriptor();\n    if (!block.isComplete()) {\n      //refresh our copy in case the block got completed in another thread\n      storedBlock = getStoredBlock(block);\n    } else {\n      storedBlock = block;\n    }\n    if (storedBlock == null || storedBlock.isDeleted()) {\n      // If this block does not belong to anyfile, then we are done.\n      blockLog.debug(\"BLOCK* addStoredBlock: {} on {} size {} but it does not\" +\n          \" belong to any file\", block, node, block.getNumBytes());", "child_info": "node name isCorruptReplicasMap:removeFromCorruptReplicasMap(Block,DatanodeDescriptor,Reason)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] CorruptReplicasMap:removeFromCorruptReplicasMap(Block,...)\n  map.remove(block,...)\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] CorruptReplicasMap:removeFromCorruptReplicasMap(Block,...)\n  map.remove(block,...)\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```source code:* @param blk Block to be removed\n   */\n  void removeFromCorruptReplicasMap(BlockInfo blk) {\n    if (corruptReplicasMap != null) {\n      Map<DatanodeDescriptor, Reason> value = corruptReplicasMap.remove(blk);\n      if (value != null) {\n        decrementBlockStat(blk.isStriped());\n      }\n    }\n  }\n\n  /**\n   * Remove the block at the given datanode from CorruptBlockMap\n   * @param blk block to be removed\n   * @param datanode datanode where the block is located\n   * @return true if the removal is successful; \n             false if the replica is not in the map\n   */ \n  boolean removeFromCorruptReplicasMap(\n      BlockInfo blk, DatanodeDescriptor datanode) {\n    return removeFromCorruptReplicasMap(blk, datanode, Reason.ANY);\n  }\n\n  boolean removeFromCorruptReplicasMap(\n      BlockInfo blk, DatanodeDescriptor datanode, Reason reason) {\n    Map <DatanodeDescriptor, Reason> datanodes = corruptReplicasMap.get(blk);\n    if (datanodes == null) {\n      return false;\n    }\n\n    // if reasons can be compared but don't match, return false.\n    Reason storedReason = datanodes.get(datanode);\n    if (reason != Reason.ANY && storedReason != null &&\n        reason != storedReason) {\n      return false;\n    }\n\n    if (datanodes.remove(datanode) != null) { // remove the replicas\n      if (datanodes.isEmpty()) {\n        // remove the block if there is no more corrupted replicas\n        corruptReplicasMap.remove(blk);\n        decrementBlockStat(blk.isStriped());\n      }\n      return true;\n    }\n    return false;\n  }\n\n  private void incrementBlockStat(boolean isStriped) {\n    if (isStriped) {\n      totalCorruptECBlockGroups.increment();\n    } else {\n      totalCorruptBlocks.increment();\n    }\n  }\n\n  private void decrementBlockStat(boolean isStriped) {\n    if (isStriped) {\n      totalCorruptECBlockGroups.decrement();\n    } else {\n      totalCorruptBlocks.decrement();\n    }"}
{"mp_id": "hdfs::BM::addStoredBlock->corruptReplicasRemoveFromMap-INFEASIBLE", "caller": "BlockManager:addStoredBlock(...)", "callee": "CorruptReplicasMap:removeFromCorruptReplicasMap(...)", "gt": "infeasible", "stratum": "complex", "infeasible_reason": "result==REPLACED (not ADDED) — removeFromCorruptReplicasMap only in ADDED branch.", "rationale": "", "caller_code": "* Faster version of {@link #addStoredBlock},\n   * intended for use with initial block report at startup. If not in startup\n   * safe mode, will call standard addStoredBlock(). Assumes this method is\n   * called \"immediately\" so there is no need to refresh the storedBlock from\n   * blocksMap. Doesn't handle low redundancy/extra redundancy, or worry about\n   * pendingReplications or corruptReplicas, because it's in startup safe mode.\n   * Doesn't log every block, because there are typically millions of them.\n   * \n   * @throws IOException\n   */\n  private void addStoredBlockImmediate(BlockInfo storedBlock, Block reported,\n      DatanodeStorageInfo storageInfo)\n  throws IOException {\n    assert (storedBlock != null && namesystem.hasWriteLock());\n    if (!namesystem.isInStartupSafeMode()\n        || isPopulatingReplQueues()) {\n      addStoredBlock(storedBlock, reported, storageInfo, null, false);\n      return;\n    }\n\n    // just add it\n    AddBlockResult result = storageInfo.addBlock(storedBlock, reported);\n\n    // Now check for completion of blocks and safe block count\n    int numCurrentReplica = countLiveNodes(storedBlock);\n    if (storedBlock.getBlockUCState() == BlockUCState.COMMITTED\n        && hasMinStorage(storedBlock, numCurrentReplica)) {\n      completeBlock(storedBlock, null, false);\n    } else if (storedBlock.isComplete() && result == AddBlockResult.ADDED) {\n      // check whether safe replication is reached for the block\n      // only complete blocks are counted towards that.\n      // In the case that the block just became complete above, completeBlock()\n      // handles the safe block count maintenance.\n      bmSafeMode.incrementSafeBlockCount(numCurrentReplica, storedBlock);\n    }\n  }\n\n  /**\n   * Modify (block-->datanode) map. Remove block from set of\n   * needed reconstruction if this takes care of the problem.\n   * @return the block that is stored in blocksMap.\n   */\n  private Block addStoredBlock(final BlockInfo block,\n                               final Block reportedBlock,\n                               DatanodeStorageInfo storageInfo,\n                               DatanodeDescriptor delNodeHint,\n                               boolean logEveryBlock)\n  throws IOException {\n    assert block != null && namesystem.hasWriteLock();\n    BlockInfo storedBlock;\n    DatanodeDescriptor node = storageInfo.getDatanodeDescriptor();\n    if (!block.isComplete()) {\n      //refresh our copy in case the block got completed in another thread\n      storedBlock = getStoredBlock(block);\n    } else {\n      storedBlock = block;\n    }\n    if (storedBlock == null || storedBlock.isDeleted()) {\n      // If this block does not belong to anyfile, then we are done.\n      blockLog.debug(\"BLOCK* addStoredBlock: {} on {} size {} but it does not\" +\n          \" belong to any file\", block, node, block.getNumBytes());", "callee_code": "* @param blk Block to be removed\n   */\n  void removeFromCorruptReplicasMap(BlockInfo blk) {\n    if (corruptReplicasMap != null) {\n      Map<DatanodeDescriptor, Reason> value = corruptReplicasMap.remove(blk);\n      if (value != null) {\n        decrementBlockStat(blk.isStriped());\n      }\n    }\n  }\n\n  /**\n   * Remove the block at the given datanode from CorruptBlockMap\n   * @param blk block to be removed\n   * @param datanode datanode where the block is located\n   * @return true if the removal is successful; \n             false if the replica is not in the map\n   */ \n  boolean removeFromCorruptReplicasMap(\n      BlockInfo blk, DatanodeDescriptor datanode) {\n    return removeFromCorruptReplicasMap(blk, datanode, Reason.ANY);\n  }\n\n  boolean removeFromCorruptReplicasMap(\n      BlockInfo blk, DatanodeDescriptor datanode, Reason reason) {\n    Map <DatanodeDescriptor, Reason> datanodes = corruptReplicasMap.get(blk);\n    if (datanodes == null) {\n      return false;\n    }\n\n    // if reasons can be compared but don't match, return false.\n    Reason storedReason = datanodes.get(datanode);\n    if (reason != Reason.ANY && storedReason != null &&\n        reason != storedReason) {\n      return false;\n    }\n\n    if (datanodes.remove(datanode) != null) { // remove the replicas\n      if (datanodes.isEmpty()) {\n        // remove the block if there is no more corrupted replicas\n        corruptReplicasMap.remove(blk);\n        decrementBlockStat(blk.isStriped());\n      }\n      return true;\n    }\n    return false;\n  }\n\n  private void incrementBlockStat(boolean isStriped) {\n    if (isStriped) {\n      totalCorruptECBlockGroups.increment();\n    } else {\n      totalCorruptBlocks.increment();\n    }\n  }\n\n  private void decrementBlockStat(boolean isStriped) {\n    if (isStriped) {\n      totalCorruptECBlockGroups.decrement();\n    } else {\n      totalCorruptBlocks.decrement();\n    }", "caller_log_raw_stripped": "[ENTRY] BlockManager:addStoredBlock(...)\n  result==REPLACED (not ADDED) \n  _note: corrupt map cleanup only in ADDED branch", "callee_log_raw_stripped": "[ENTRY] CorruptReplicasMap:removeFromCorruptReplicasMap(...)", "caller_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] BlockManager:addStoredBlock(...)\n  result==REPLACED (not ADDED) \n  _note: corrupt map cleanup only in ADDED branch\n      </exec_flow>\n      <log_sequence>\n[ENTRY] BlockManager:addStoredBlock(...)\n  result==REPLACED (not ADDED) \n  _note: corrupt map cleanup only in ADDED branch\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "callee_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] CorruptReplicasMap:removeFromCorruptReplicasMap(...)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] CorruptReplicasMap:removeFromCorruptReplicasMap(...)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "parent_info": "node name is BlockManager:addStoredBlock(...)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] BlockManager:addStoredBlock(...)\n  result==REPLACED (not ADDED) \n  _note: corrupt map cleanup only in ADDED branch\n      </exec_flow>\n      <log_sequence>\n[ENTRY] BlockManager:addStoredBlock(...)\n  result==REPLACED (not ADDED) \n  _note: corrupt map cleanup only in ADDED branch\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```souce code:* Faster version of {@link #addStoredBlock},\n   * intended for use with initial block report at startup. If not in startup\n   * safe mode, will call standard addStoredBlock(). Assumes this method is\n   * called \"immediately\" so there is no need to refresh the storedBlock from\n   * blocksMap. Doesn't handle low redundancy/extra redundancy, or worry about\n   * pendingReplications or corruptReplicas, because it's in startup safe mode.\n   * Doesn't log every block, because there are typically millions of them.\n   * \n   * @throws IOException\n   */\n  private void addStoredBlockImmediate(BlockInfo storedBlock, Block reported,\n      DatanodeStorageInfo storageInfo)\n  throws IOException {\n    assert (storedBlock != null && namesystem.hasWriteLock());\n    if (!namesystem.isInStartupSafeMode()\n        || isPopulatingReplQueues()) {\n      addStoredBlock(storedBlock, reported, storageInfo, null, false);\n      return;\n    }\n\n    // just add it\n    AddBlockResult result = storageInfo.addBlock(storedBlock, reported);\n\n    // Now check for completion of blocks and safe block count\n    int numCurrentReplica = countLiveNodes(storedBlock);\n    if (storedBlock.getBlockUCState() == BlockUCState.COMMITTED\n        && hasMinStorage(storedBlock, numCurrentReplica)) {\n      completeBlock(storedBlock, null, false);\n    } else if (storedBlock.isComplete() && result == AddBlockResult.ADDED) {\n      // check whether safe replication is reached for the block\n      // only complete blocks are counted towards that.\n      // In the case that the block just became complete above, completeBlock()\n      // handles the safe block count maintenance.\n      bmSafeMode.incrementSafeBlockCount(numCurrentReplica, storedBlock);\n    }\n  }\n\n  /**\n   * Modify (block-->datanode) map. Remove block from set of\n   * needed reconstruction if this takes care of the problem.\n   * @return the block that is stored in blocksMap.\n   */\n  private Block addStoredBlock(final BlockInfo block,\n                               final Block reportedBlock,\n                               DatanodeStorageInfo storageInfo,\n                               DatanodeDescriptor delNodeHint,\n                               boolean logEveryBlock)\n  throws IOException {\n    assert block != null && namesystem.hasWriteLock();\n    BlockInfo storedBlock;\n    DatanodeDescriptor node = storageInfo.getDatanodeDescriptor();\n    if (!block.isComplete()) {\n      //refresh our copy in case the block got completed in another thread\n      storedBlock = getStoredBlock(block);\n    } else {\n      storedBlock = block;\n    }\n    if (storedBlock == null || storedBlock.isDeleted()) {\n      // If this block does not belong to anyfile, then we are done.\n      blockLog.debug(\"BLOCK* addStoredBlock: {} on {} size {} but it does not\" +\n          \" belong to any file\", block, node, block.getNumBytes());", "child_info": "node name isCorruptReplicasMap:removeFromCorruptReplicasMap(...)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] CorruptReplicasMap:removeFromCorruptReplicasMap(...)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] CorruptReplicasMap:removeFromCorruptReplicasMap(...)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```source code:* @param blk Block to be removed\n   */\n  void removeFromCorruptReplicasMap(BlockInfo blk) {\n    if (corruptReplicasMap != null) {\n      Map<DatanodeDescriptor, Reason> value = corruptReplicasMap.remove(blk);\n      if (value != null) {\n        decrementBlockStat(blk.isStriped());\n      }\n    }\n  }\n\n  /**\n   * Remove the block at the given datanode from CorruptBlockMap\n   * @param blk block to be removed\n   * @param datanode datanode where the block is located\n   * @return true if the removal is successful; \n             false if the replica is not in the map\n   */ \n  boolean removeFromCorruptReplicasMap(\n      BlockInfo blk, DatanodeDescriptor datanode) {\n    return removeFromCorruptReplicasMap(blk, datanode, Reason.ANY);\n  }\n\n  boolean removeFromCorruptReplicasMap(\n      BlockInfo blk, DatanodeDescriptor datanode, Reason reason) {\n    Map <DatanodeDescriptor, Reason> datanodes = corruptReplicasMap.get(blk);\n    if (datanodes == null) {\n      return false;\n    }\n\n    // if reasons can be compared but don't match, return false.\n    Reason storedReason = datanodes.get(datanode);\n    if (reason != Reason.ANY && storedReason != null &&\n        reason != storedReason) {\n      return false;\n    }\n\n    if (datanodes.remove(datanode) != null) { // remove the replicas\n      if (datanodes.isEmpty()) {\n        // remove the block if there is no more corrupted replicas\n        corruptReplicasMap.remove(blk);\n        decrementBlockStat(blk.isStriped());\n      }\n      return true;\n    }\n    return false;\n  }\n\n  private void incrementBlockStat(boolean isStriped) {\n    if (isStriped) {\n      totalCorruptECBlockGroups.increment();\n    } else {\n      totalCorruptBlocks.increment();\n    }\n  }\n\n  private void decrementBlockStat(boolean isStriped) {\n    if (isStriped) {\n      totalCorruptECBlockGroups.decrement();\n    } else {\n      totalCorruptBlocks.decrement();\n    }"}
{"mp_id": "hdfs::BM::addStoredBlock->incrementSafeBlockCount", "caller": "BlockManager:addStoredBlock(BlockInfo,DatanodeStorageInfo,DatanodeDescriptor,boolean)", "callee": "BlockManagerSafeMode:incrementSafeBlockCount(int,Block)", "gt": "valid", "stratum": "complex", "infeasible_reason": "", "rationale": "After ADDED result, if block is complete and safe mode is active.", "caller_code": "* Faster version of {@link #addStoredBlock},\n   * intended for use with initial block report at startup. If not in startup\n   * safe mode, will call standard addStoredBlock(). Assumes this method is\n   * called \"immediately\" so there is no need to refresh the storedBlock from\n   * blocksMap. Doesn't handle low redundancy/extra redundancy, or worry about\n   * pendingReplications or corruptReplicas, because it's in startup safe mode.\n   * Doesn't log every block, because there are typically millions of them.\n   * \n   * @throws IOException\n   */\n  private void addStoredBlockImmediate(BlockInfo storedBlock, Block reported,\n      DatanodeStorageInfo storageInfo)\n  throws IOException {\n    assert (storedBlock != null && namesystem.hasWriteLock());\n    if (!namesystem.isInStartupSafeMode()\n        || isPopulatingReplQueues()) {\n      addStoredBlock(storedBlock, reported, storageInfo, null, false);\n      return;\n    }\n\n    // just add it\n    AddBlockResult result = storageInfo.addBlock(storedBlock, reported);\n\n    // Now check for completion of blocks and safe block count\n    int numCurrentReplica = countLiveNodes(storedBlock);\n    if (storedBlock.getBlockUCState() == BlockUCState.COMMITTED\n        && hasMinStorage(storedBlock, numCurrentReplica)) {\n      completeBlock(storedBlock, null, false);\n    } else if (storedBlock.isComplete() && result == AddBlockResult.ADDED) {\n      // check whether safe replication is reached for the block\n      // only complete blocks are counted towards that.\n      // In the case that the block just became complete above, completeBlock()\n      // handles the safe block count maintenance.\n      bmSafeMode.incrementSafeBlockCount(numCurrentReplica, storedBlock);\n    }\n  }\n\n  /**\n   * Modify (block-->datanode) map. Remove block from set of\n   * needed reconstruction if this takes care of the problem.\n   * @return the block that is stored in blocksMap.\n   */\n  private Block addStoredBlock(final BlockInfo block,\n                               final Block reportedBlock,\n                               DatanodeStorageInfo storageInfo,\n                               DatanodeDescriptor delNodeHint,\n                               boolean logEveryBlock)\n  throws IOException {\n    assert block != null && namesystem.hasWriteLock();\n    BlockInfo storedBlock;\n    DatanodeDescriptor node = storageInfo.getDatanodeDescriptor();\n    if (!block.isComplete()) {\n      //refresh our copy in case the block got completed in another thread\n      storedBlock = getStoredBlock(block);\n    } else {\n      storedBlock = block;\n    }\n    if (storedBlock == null || storedBlock.isDeleted()) {\n      // If this block does not belong to anyfile, then we are done.\n      blockLog.debug(\"BLOCK* addStoredBlock: {} on {} size {} but it does not\" +\n          \" belong to any file\", block, node, block.getNumBytes());", "callee_code": "*                    BlockInfoContiguous or a BlockInfoStriped\n   */\n  synchronized void incrementSafeBlockCount(int storageNum,\n      BlockInfo storedBlock) {\n    assert namesystem.hasWriteLock();\n    if (status == BMSafeModeStatus.OFF) {\n      return;\n    }\n\n    final int safeNumberOfNodes = storedBlock.isStriped() ?\n        ((BlockInfoStriped)storedBlock).getRealDataBlockNum() : safeReplication;\n    if (storageNum == safeNumberOfNodes) {\n      this.blockSafe++;\n\n      // Report startup progress only if we haven't completed startup yet.\n      StartupProgress prog = NameNode.getStartupProgress();\n      if (prog.getStatus(Phase.SAFEMODE) != Status.COMPLETE) {\n        if (this.awaitingReportedBlocksCounter == null) {\n          this.awaitingReportedBlocksCounter = prog.getCounter(Phase.SAFEMODE,\n              STEP_AWAITING_REPORTED_BLOCKS);\n        }\n        this.awaitingReportedBlocksCounter.increment();\n      }\n\n      checkSafeMode();\n    }\n  }\n\n  /**\n   * Decrement number of safe blocks if the current block is contiguous\n   * and it has just fallen below minimal replication or\n   * if the current block is striped and its actual data blocks has just fallen\n   * below the number of data units specified by erasure coding policy.\n   * If safe mode is not currently on, this is a no-op.\n   */\n  synchronized void decrementSafeBlockCount(BlockInfo b) {\n    assert namesystem.hasWriteLock();\n    if (status == BMSafeModeStatus.OFF) {\n      return;\n    }\n\n    final int safeNumberOfNodes = b.isStriped() ?\n        ((BlockInfoStriped)b).getRealDataBlockNum() : safeReplication;\n    BlockInfo storedBlock = blockManager.getStoredBlock(b);\n    if (storedBlock.isComplete() &&\n        blockManager.countNodes(b).liveReplicas() == safeNumberOfNodes - 1) {\n      this.blockSafe--;\n      assert blockSafe >= 0;\n      checkSafeMode();\n    }\n  }\n\n  /**\n   * Check if the block report replica has a generation stamp (GS) in future.\n   * If safe mode is not currently on, this is a no-op.\n   *\n   * @param brr block report replica which belongs to no file in BlockManager\n   */\n  void checkBlocksWithFutureGS(BlockReportReplica brr) {\n    assert namesystem.hasWriteLock();\n    if (status == BMSafeModeStatus.OFF) {\n      return;", "caller_log_raw_stripped": "[ENTRY] BlockManager:addStoredBlock(...)\n  IF: result==ADDED && storedBlock.isComplete()\n  CALL: bmSafeMode.incrementSafeBlockCount(num, block)", "callee_log_raw_stripped": "[ENTRY] BlockManagerSafeMode:incrementSafeBlockCount(int,Block)\n  IF: not in safe mode -> no-op\n  ELSE: update count\n  EXIT", "caller_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] BlockManager:addStoredBlock(...)\n  IF: result==ADDED && storedBlock.isComplete()\n  CALL: bmSafeMode.incrementSafeBlockCount(num, block)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] BlockManager:addStoredBlock(...)\n  IF: result==ADDED && storedBlock.isComplete()\n  CALL: bmSafeMode.incrementSafeBlockCount(num, block)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "callee_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] BlockManagerSafeMode:incrementSafeBlockCount(int,Block)\n  IF: not in safe mode -> no-op\n  ELSE: update count\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] BlockManagerSafeMode:incrementSafeBlockCount(int,Block)\n  IF: not in safe mode -> no-op\n  ELSE: update count\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "parent_info": "node name is BlockManager:addStoredBlock(BlockInfo,DatanodeStorageInfo,DatanodeDescriptor,boolean)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] BlockManager:addStoredBlock(...)\n  IF: result==ADDED && storedBlock.isComplete()\n  CALL: bmSafeMode.incrementSafeBlockCount(num, block)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] BlockManager:addStoredBlock(...)\n  IF: result==ADDED && storedBlock.isComplete()\n  CALL: bmSafeMode.incrementSafeBlockCount(num, block)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```souce code:* Faster version of {@link #addStoredBlock},\n   * intended for use with initial block report at startup. If not in startup\n   * safe mode, will call standard addStoredBlock(). Assumes this method is\n   * called \"immediately\" so there is no need to refresh the storedBlock from\n   * blocksMap. Doesn't handle low redundancy/extra redundancy, or worry about\n   * pendingReplications or corruptReplicas, because it's in startup safe mode.\n   * Doesn't log every block, because there are typically millions of them.\n   * \n   * @throws IOException\n   */\n  private void addStoredBlockImmediate(BlockInfo storedBlock, Block reported,\n      DatanodeStorageInfo storageInfo)\n  throws IOException {\n    assert (storedBlock != null && namesystem.hasWriteLock());\n    if (!namesystem.isInStartupSafeMode()\n        || isPopulatingReplQueues()) {\n      addStoredBlock(storedBlock, reported, storageInfo, null, false);\n      return;\n    }\n\n    // just add it\n    AddBlockResult result = storageInfo.addBlock(storedBlock, reported);\n\n    // Now check for completion of blocks and safe block count\n    int numCurrentReplica = countLiveNodes(storedBlock);\n    if (storedBlock.getBlockUCState() == BlockUCState.COMMITTED\n        && hasMinStorage(storedBlock, numCurrentReplica)) {\n      completeBlock(storedBlock, null, false);\n    } else if (storedBlock.isComplete() && result == AddBlockResult.ADDED) {\n      // check whether safe replication is reached for the block\n      // only complete blocks are counted towards that.\n      // In the case that the block just became complete above, completeBlock()\n      // handles the safe block count maintenance.\n      bmSafeMode.incrementSafeBlockCount(numCurrentReplica, storedBlock);\n    }\n  }\n\n  /**\n   * Modify (block-->datanode) map. Remove block from set of\n   * needed reconstruction if this takes care of the problem.\n   * @return the block that is stored in blocksMap.\n   */\n  private Block addStoredBlock(final BlockInfo block,\n                               final Block reportedBlock,\n                               DatanodeStorageInfo storageInfo,\n                               DatanodeDescriptor delNodeHint,\n                               boolean logEveryBlock)\n  throws IOException {\n    assert block != null && namesystem.hasWriteLock();\n    BlockInfo storedBlock;\n    DatanodeDescriptor node = storageInfo.getDatanodeDescriptor();\n    if (!block.isComplete()) {\n      //refresh our copy in case the block got completed in another thread\n      storedBlock = getStoredBlock(block);\n    } else {\n      storedBlock = block;\n    }\n    if (storedBlock == null || storedBlock.isDeleted()) {\n      // If this block does not belong to anyfile, then we are done.\n      blockLog.debug(\"BLOCK* addStoredBlock: {} on {} size {} but it does not\" +\n          \" belong to any file\", block, node, block.getNumBytes());", "child_info": "node name isBlockManagerSafeMode:incrementSafeBlockCount(int,Block)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] BlockManagerSafeMode:incrementSafeBlockCount(int,Block)\n  IF: not in safe mode -> no-op\n  ELSE: update count\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] BlockManagerSafeMode:incrementSafeBlockCount(int,Block)\n  IF: not in safe mode -> no-op\n  ELSE: update count\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```source code:*                    BlockInfoContiguous or a BlockInfoStriped\n   */\n  synchronized void incrementSafeBlockCount(int storageNum,\n      BlockInfo storedBlock) {\n    assert namesystem.hasWriteLock();\n    if (status == BMSafeModeStatus.OFF) {\n      return;\n    }\n\n    final int safeNumberOfNodes = storedBlock.isStriped() ?\n        ((BlockInfoStriped)storedBlock).getRealDataBlockNum() : safeReplication;\n    if (storageNum == safeNumberOfNodes) {\n      this.blockSafe++;\n\n      // Report startup progress only if we haven't completed startup yet.\n      StartupProgress prog = NameNode.getStartupProgress();\n      if (prog.getStatus(Phase.SAFEMODE) != Status.COMPLETE) {\n        if (this.awaitingReportedBlocksCounter == null) {\n          this.awaitingReportedBlocksCounter = prog.getCounter(Phase.SAFEMODE,\n              STEP_AWAITING_REPORTED_BLOCKS);\n        }\n        this.awaitingReportedBlocksCounter.increment();\n      }\n\n      checkSafeMode();\n    }\n  }\n\n  /**\n   * Decrement number of safe blocks if the current block is contiguous\n   * and it has just fallen below minimal replication or\n   * if the current block is striped and its actual data blocks has just fallen\n   * below the number of data units specified by erasure coding policy.\n   * If safe mode is not currently on, this is a no-op.\n   */\n  synchronized void decrementSafeBlockCount(BlockInfo b) {\n    assert namesystem.hasWriteLock();\n    if (status == BMSafeModeStatus.OFF) {\n      return;\n    }\n\n    final int safeNumberOfNodes = b.isStriped() ?\n        ((BlockInfoStriped)b).getRealDataBlockNum() : safeReplication;\n    BlockInfo storedBlock = blockManager.getStoredBlock(b);\n    if (storedBlock.isComplete() &&\n        blockManager.countNodes(b).liveReplicas() == safeNumberOfNodes - 1) {\n      this.blockSafe--;\n      assert blockSafe >= 0;\n      checkSafeMode();\n    }\n  }\n\n  /**\n   * Check if the block report replica has a generation stamp (GS) in future.\n   * If safe mode is not currently on, this is a no-op.\n   *\n   * @param brr block report replica which belongs to no file in BlockManager\n   */\n  void checkBlocksWithFutureGS(BlockReportReplica brr) {\n    assert namesystem.hasWriteLock();\n    if (status == BMSafeModeStatus.OFF) {\n      return;"}
{"mp_id": "hdfs::BM::addStoredBlock->incrementSafeBlockCount-INFEASIBLE", "caller": "BlockManager:addStoredBlock(...)", "callee": "BlockManagerSafeMode:incrementSafeBlockCount(...)", "gt": "infeasible", "stratum": "complex", "infeasible_reason": "storedBlock.isComplete()==false — guarded by isComplete() && result==ADDED.", "rationale": "", "caller_code": "* Faster version of {@link #addStoredBlock},\n   * intended for use with initial block report at startup. If not in startup\n   * safe mode, will call standard addStoredBlock(). Assumes this method is\n   * called \"immediately\" so there is no need to refresh the storedBlock from\n   * blocksMap. Doesn't handle low redundancy/extra redundancy, or worry about\n   * pendingReplications or corruptReplicas, because it's in startup safe mode.\n   * Doesn't log every block, because there are typically millions of them.\n   * \n   * @throws IOException\n   */\n  private void addStoredBlockImmediate(BlockInfo storedBlock, Block reported,\n      DatanodeStorageInfo storageInfo)\n  throws IOException {\n    assert (storedBlock != null && namesystem.hasWriteLock());\n    if (!namesystem.isInStartupSafeMode()\n        || isPopulatingReplQueues()) {\n      addStoredBlock(storedBlock, reported, storageInfo, null, false);\n      return;\n    }\n\n    // just add it\n    AddBlockResult result = storageInfo.addBlock(storedBlock, reported);\n\n    // Now check for completion of blocks and safe block count\n    int numCurrentReplica = countLiveNodes(storedBlock);\n    if (storedBlock.getBlockUCState() == BlockUCState.COMMITTED\n        && hasMinStorage(storedBlock, numCurrentReplica)) {\n      completeBlock(storedBlock, null, false);\n    } else if (storedBlock.isComplete() && result == AddBlockResult.ADDED) {\n      // check whether safe replication is reached for the block\n      // only complete blocks are counted towards that.\n      // In the case that the block just became complete above, completeBlock()\n      // handles the safe block count maintenance.\n      bmSafeMode.incrementSafeBlockCount(numCurrentReplica, storedBlock);\n    }\n  }\n\n  /**\n   * Modify (block-->datanode) map. Remove block from set of\n   * needed reconstruction if this takes care of the problem.\n   * @return the block that is stored in blocksMap.\n   */\n  private Block addStoredBlock(final BlockInfo block,\n                               final Block reportedBlock,\n                               DatanodeStorageInfo storageInfo,\n                               DatanodeDescriptor delNodeHint,\n                               boolean logEveryBlock)\n  throws IOException {\n    assert block != null && namesystem.hasWriteLock();\n    BlockInfo storedBlock;\n    DatanodeDescriptor node = storageInfo.getDatanodeDescriptor();\n    if (!block.isComplete()) {\n      //refresh our copy in case the block got completed in another thread\n      storedBlock = getStoredBlock(block);\n    } else {\n      storedBlock = block;\n    }\n    if (storedBlock == null || storedBlock.isDeleted()) {\n      // If this block does not belong to anyfile, then we are done.\n      blockLog.debug(\"BLOCK* addStoredBlock: {} on {} size {} but it does not\" +\n          \" belong to any file\", block, node, block.getNumBytes());", "callee_code": "*                    BlockInfoContiguous or a BlockInfoStriped\n   */\n  synchronized void incrementSafeBlockCount(int storageNum,\n      BlockInfo storedBlock) {\n    assert namesystem.hasWriteLock();\n    if (status == BMSafeModeStatus.OFF) {\n      return;\n    }\n\n    final int safeNumberOfNodes = storedBlock.isStriped() ?\n        ((BlockInfoStriped)storedBlock).getRealDataBlockNum() : safeReplication;\n    if (storageNum == safeNumberOfNodes) {\n      this.blockSafe++;\n\n      // Report startup progress only if we haven't completed startup yet.\n      StartupProgress prog = NameNode.getStartupProgress();\n      if (prog.getStatus(Phase.SAFEMODE) != Status.COMPLETE) {\n        if (this.awaitingReportedBlocksCounter == null) {\n          this.awaitingReportedBlocksCounter = prog.getCounter(Phase.SAFEMODE,\n              STEP_AWAITING_REPORTED_BLOCKS);\n        }\n        this.awaitingReportedBlocksCounter.increment();\n      }\n\n      checkSafeMode();\n    }\n  }\n\n  /**\n   * Decrement number of safe blocks if the current block is contiguous\n   * and it has just fallen below minimal replication or\n   * if the current block is striped and its actual data blocks has just fallen\n   * below the number of data units specified by erasure coding policy.\n   * If safe mode is not currently on, this is a no-op.\n   */\n  synchronized void decrementSafeBlockCount(BlockInfo b) {\n    assert namesystem.hasWriteLock();\n    if (status == BMSafeModeStatus.OFF) {\n      return;\n    }\n\n    final int safeNumberOfNodes = b.isStriped() ?\n        ((BlockInfoStriped)b).getRealDataBlockNum() : safeReplication;\n    BlockInfo storedBlock = blockManager.getStoredBlock(b);\n    if (storedBlock.isComplete() &&\n        blockManager.countNodes(b).liveReplicas() == safeNumberOfNodes - 1) {\n      this.blockSafe--;\n      assert blockSafe >= 0;\n      checkSafeMode();\n    }\n  }\n\n  /**\n   * Check if the block report replica has a generation stamp (GS) in future.\n   * If safe mode is not currently on, this is a no-op.\n   *\n   * @param brr block report replica which belongs to no file in BlockManager\n   */\n  void checkBlocksWithFutureGS(BlockReportReplica brr) {\n    assert namesystem.hasWriteLock();\n    if (status == BMSafeModeStatus.OFF) {\n      return;", "caller_log_raw_stripped": "[ENTRY] BlockManager:addStoredBlock(...)\n  result==ADDED && storedBlock.isComplete()==false  -> return", "callee_log_raw_stripped": "[ENTRY] BlockManagerSafeMode:incrementSafeBlockCount(...)", "caller_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] BlockManager:addStoredBlock(...)\n  result==ADDED && storedBlock.isComplete()==false  -> return\n      </exec_flow>\n      <log_sequence>\n[ENTRY] BlockManager:addStoredBlock(...)\n  result==ADDED && storedBlock.isComplete()==false  -> return\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "callee_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] BlockManagerSafeMode:incrementSafeBlockCount(...)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] BlockManagerSafeMode:incrementSafeBlockCount(...)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "parent_info": "node name is BlockManager:addStoredBlock(...)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] BlockManager:addStoredBlock(...)\n  result==ADDED && storedBlock.isComplete()==false  -> return\n      </exec_flow>\n      <log_sequence>\n[ENTRY] BlockManager:addStoredBlock(...)\n  result==ADDED && storedBlock.isComplete()==false  -> return\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```souce code:* Faster version of {@link #addStoredBlock},\n   * intended for use with initial block report at startup. If not in startup\n   * safe mode, will call standard addStoredBlock(). Assumes this method is\n   * called \"immediately\" so there is no need to refresh the storedBlock from\n   * blocksMap. Doesn't handle low redundancy/extra redundancy, or worry about\n   * pendingReplications or corruptReplicas, because it's in startup safe mode.\n   * Doesn't log every block, because there are typically millions of them.\n   * \n   * @throws IOException\n   */\n  private void addStoredBlockImmediate(BlockInfo storedBlock, Block reported,\n      DatanodeStorageInfo storageInfo)\n  throws IOException {\n    assert (storedBlock != null && namesystem.hasWriteLock());\n    if (!namesystem.isInStartupSafeMode()\n        || isPopulatingReplQueues()) {\n      addStoredBlock(storedBlock, reported, storageInfo, null, false);\n      return;\n    }\n\n    // just add it\n    AddBlockResult result = storageInfo.addBlock(storedBlock, reported);\n\n    // Now check for completion of blocks and safe block count\n    int numCurrentReplica = countLiveNodes(storedBlock);\n    if (storedBlock.getBlockUCState() == BlockUCState.COMMITTED\n        && hasMinStorage(storedBlock, numCurrentReplica)) {\n      completeBlock(storedBlock, null, false);\n    } else if (storedBlock.isComplete() && result == AddBlockResult.ADDED) {\n      // check whether safe replication is reached for the block\n      // only complete blocks are counted towards that.\n      // In the case that the block just became complete above, completeBlock()\n      // handles the safe block count maintenance.\n      bmSafeMode.incrementSafeBlockCount(numCurrentReplica, storedBlock);\n    }\n  }\n\n  /**\n   * Modify (block-->datanode) map. Remove block from set of\n   * needed reconstruction if this takes care of the problem.\n   * @return the block that is stored in blocksMap.\n   */\n  private Block addStoredBlock(final BlockInfo block,\n                               final Block reportedBlock,\n                               DatanodeStorageInfo storageInfo,\n                               DatanodeDescriptor delNodeHint,\n                               boolean logEveryBlock)\n  throws IOException {\n    assert block != null && namesystem.hasWriteLock();\n    BlockInfo storedBlock;\n    DatanodeDescriptor node = storageInfo.getDatanodeDescriptor();\n    if (!block.isComplete()) {\n      //refresh our copy in case the block got completed in another thread\n      storedBlock = getStoredBlock(block);\n    } else {\n      storedBlock = block;\n    }\n    if (storedBlock == null || storedBlock.isDeleted()) {\n      // If this block does not belong to anyfile, then we are done.\n      blockLog.debug(\"BLOCK* addStoredBlock: {} on {} size {} but it does not\" +\n          \" belong to any file\", block, node, block.getNumBytes());", "child_info": "node name isBlockManagerSafeMode:incrementSafeBlockCount(...)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] BlockManagerSafeMode:incrementSafeBlockCount(...)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] BlockManagerSafeMode:incrementSafeBlockCount(...)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```source code:*                    BlockInfoContiguous or a BlockInfoStriped\n   */\n  synchronized void incrementSafeBlockCount(int storageNum,\n      BlockInfo storedBlock) {\n    assert namesystem.hasWriteLock();\n    if (status == BMSafeModeStatus.OFF) {\n      return;\n    }\n\n    final int safeNumberOfNodes = storedBlock.isStriped() ?\n        ((BlockInfoStriped)storedBlock).getRealDataBlockNum() : safeReplication;\n    if (storageNum == safeNumberOfNodes) {\n      this.blockSafe++;\n\n      // Report startup progress only if we haven't completed startup yet.\n      StartupProgress prog = NameNode.getStartupProgress();\n      if (prog.getStatus(Phase.SAFEMODE) != Status.COMPLETE) {\n        if (this.awaitingReportedBlocksCounter == null) {\n          this.awaitingReportedBlocksCounter = prog.getCounter(Phase.SAFEMODE,\n              STEP_AWAITING_REPORTED_BLOCKS);\n        }\n        this.awaitingReportedBlocksCounter.increment();\n      }\n\n      checkSafeMode();\n    }\n  }\n\n  /**\n   * Decrement number of safe blocks if the current block is contiguous\n   * and it has just fallen below minimal replication or\n   * if the current block is striped and its actual data blocks has just fallen\n   * below the number of data units specified by erasure coding policy.\n   * If safe mode is not currently on, this is a no-op.\n   */\n  synchronized void decrementSafeBlockCount(BlockInfo b) {\n    assert namesystem.hasWriteLock();\n    if (status == BMSafeModeStatus.OFF) {\n      return;\n    }\n\n    final int safeNumberOfNodes = b.isStriped() ?\n        ((BlockInfoStriped)b).getRealDataBlockNum() : safeReplication;\n    BlockInfo storedBlock = blockManager.getStoredBlock(b);\n    if (storedBlock.isComplete() &&\n        blockManager.countNodes(b).liveReplicas() == safeNumberOfNodes - 1) {\n      this.blockSafe--;\n      assert blockSafe >= 0;\n      checkSafeMode();\n    }\n  }\n\n  /**\n   * Check if the block report replica has a generation stamp (GS) in future.\n   * If safe mode is not currently on, this is a no-op.\n   *\n   * @param brr block report replica which belongs to no file in BlockManager\n   */\n  void checkBlocksWithFutureGS(BlockReportReplica brr) {\n    assert namesystem.hasWriteLock();\n    if (status == BMSafeModeStatus.OFF) {\n      return;"}
{"mp_id": "hdfs::BM::addStoredBlock->isNeededReconstruction", "caller": "BlockManager:addStoredBlock(BlockInfo,DatanodeStorageInfo,DatanodeDescriptor,boolean)", "callee": "BlockManager:isNeededReconstruction(Block,int,int)", "gt": "valid", "stratum": "complex", "infeasible_reason": "", "rationale": "After redundancy computed, checks isNeededReconstruction for reconstruction queue.", "caller_code": "* Faster version of {@link #addStoredBlock},\n   * intended for use with initial block report at startup. If not in startup\n   * safe mode, will call standard addStoredBlock(). Assumes this method is\n   * called \"immediately\" so there is no need to refresh the storedBlock from\n   * blocksMap. Doesn't handle low redundancy/extra redundancy, or worry about\n   * pendingReplications or corruptReplicas, because it's in startup safe mode.\n   * Doesn't log every block, because there are typically millions of them.\n   * \n   * @throws IOException\n   */\n  private void addStoredBlockImmediate(BlockInfo storedBlock, Block reported,\n      DatanodeStorageInfo storageInfo)\n  throws IOException {\n    assert (storedBlock != null && namesystem.hasWriteLock());\n    if (!namesystem.isInStartupSafeMode()\n        || isPopulatingReplQueues()) {\n      addStoredBlock(storedBlock, reported, storageInfo, null, false);\n      return;\n    }\n\n    // just add it\n    AddBlockResult result = storageInfo.addBlock(storedBlock, reported);\n\n    // Now check for completion of blocks and safe block count\n    int numCurrentReplica = countLiveNodes(storedBlock);\n    if (storedBlock.getBlockUCState() == BlockUCState.COMMITTED\n        && hasMinStorage(storedBlock, numCurrentReplica)) {\n      completeBlock(storedBlock, null, false);\n    } else if (storedBlock.isComplete() && result == AddBlockResult.ADDED) {\n      // check whether safe replication is reached for the block\n      // only complete blocks are counted towards that.\n      // In the case that the block just became complete above, completeBlock()\n      // handles the safe block count maintenance.\n      bmSafeMode.incrementSafeBlockCount(numCurrentReplica, storedBlock);\n    }\n  }\n\n  /**\n   * Modify (block-->datanode) map. Remove block from set of\n   * needed reconstruction if this takes care of the problem.\n   * @return the block that is stored in blocksMap.\n   */\n  private Block addStoredBlock(final BlockInfo block,\n                               final Block reportedBlock,\n                               DatanodeStorageInfo storageInfo,\n                               DatanodeDescriptor delNodeHint,\n                               boolean logEveryBlock)\n  throws IOException {\n    assert block != null && namesystem.hasWriteLock();\n    BlockInfo storedBlock;\n    DatanodeDescriptor node = storageInfo.getDatanodeDescriptor();\n    if (!block.isComplete()) {\n      //refresh our copy in case the block got completed in another thread\n      storedBlock = getStoredBlock(block);\n    } else {\n      storedBlock = block;\n    }\n    if (storedBlock == null || storedBlock.isDeleted()) {\n      // If this block does not belong to anyfile, then we are done.\n      blockLog.debug(\"BLOCK* addStoredBlock: {} on {} size {} but it does not\" +\n          \" belong to any file\", block, node, block.getNumBytes());", "callee_code": "}\n\n  boolean isNeededReconstruction(BlockInfo storedBlock,\n      NumberReplicas numberReplicas) {\n    return isNeededReconstruction(storedBlock, numberReplicas, 0);\n  }\n\n  /**\n   * A block needs reconstruction if the number of redundancies is less than\n   * expected or if it does not have enough racks.\n   */\n  boolean isNeededReconstruction(BlockInfo storedBlock,\n      NumberReplicas numberReplicas, int pending) {\n    return storedBlock.isComplete() &&\n        !hasEnoughEffectiveReplicas(storedBlock, numberReplicas, pending);\n  }\n\n  // Exclude maintenance, but make sure it has minimal live replicas\n  // to satisfy the maintenance requirement.\n  public short getExpectedLiveRedundancyNum(BlockInfo block,\n      NumberReplicas numberReplicas) {\n    final short expectedRedundancy = getExpectedRedundancyNum(block);\n    return (short)Math.max(expectedRedundancy -\n        numberReplicas.maintenanceReplicas(),\n        getMinMaintenanceStorageNum(block));\n  }\n\n  public short getExpectedRedundancyNum(BlockInfo block) {\n    return block.isStriped() ?\n        ((BlockInfoStriped) block).getRealTotalBlockNum() :\n        block.getReplication();\n  }\n\n  public long getMissingBlocksCount() {\n    // not locking\n    return this.neededReconstruction.getCorruptBlockSize();\n  }\n\n  public long getMissingReplOneBlocksCount() {\n    // not locking\n    return this.neededReconstruction.getCorruptReplicationOneBlockSize();\n  }\n\n  public long getHighestPriorityReplicatedBlockCount(){\n    return this.neededReconstruction.getHighestPriorityReplicatedBlockCount();\n  }\n\n  public long getHighestPriorityECBlockCount(){\n    return this.neededReconstruction.getHighestPriorityECBlockCount();\n  }\n\n  public BlockInfo addBlockCollection(BlockInfo block,\n      BlockCollection bc) {\n    BlockInfo blockInfo = blocksMap.addBlockCollection(block, bc);\n    blockIdManager.setGenerationStampIfGreater(block.getGenerationStamp());\n    return blockInfo;\n  }\n\n  /**\n   * Do some check when adding a block to blocksmap.\n   * For HDFS-7994 to check whether then block is a NonEcBlockUsingStripedID.\n   *", "caller_log_raw_stripped": "[ENTRY] BlockManager:addStoredBlock(...)\n  IF: isPopulatingReplQueues() && storedBlock.isCompleteOrCommitted()\n  CALL: isNeededReconstruction(storedBlock, num, pending)", "callee_log_raw_stripped": "[ENTRY] BlockManager:isNeededReconstruction(Block,int,int)\n  IF: numLiveReplicas >= expected -> false\n  ELSE -> true\n  EXIT", "caller_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] BlockManager:addStoredBlock(...)\n  IF: isPopulatingReplQueues() && storedBlock.isCompleteOrCommitted()\n  CALL: isNeededReconstruction(storedBlock, num, pending)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] BlockManager:addStoredBlock(...)\n  IF: isPopulatingReplQueues() && storedBlock.isCompleteOrCommitted()\n  CALL: isNeededReconstruction(storedBlock, num, pending)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "callee_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] BlockManager:isNeededReconstruction(Block,int,int)\n  IF: numLiveReplicas >= expected -> false\n  ELSE -> true\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] BlockManager:isNeededReconstruction(Block,int,int)\n  IF: numLiveReplicas >= expected -> false\n  ELSE -> true\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "parent_info": "node name is BlockManager:addStoredBlock(BlockInfo,DatanodeStorageInfo,DatanodeDescriptor,boolean)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] BlockManager:addStoredBlock(...)\n  IF: isPopulatingReplQueues() && storedBlock.isCompleteOrCommitted()\n  CALL: isNeededReconstruction(storedBlock, num, pending)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] BlockManager:addStoredBlock(...)\n  IF: isPopulatingReplQueues() && storedBlock.isCompleteOrCommitted()\n  CALL: isNeededReconstruction(storedBlock, num, pending)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```souce code:* Faster version of {@link #addStoredBlock},\n   * intended for use with initial block report at startup. If not in startup\n   * safe mode, will call standard addStoredBlock(). Assumes this method is\n   * called \"immediately\" so there is no need to refresh the storedBlock from\n   * blocksMap. Doesn't handle low redundancy/extra redundancy, or worry about\n   * pendingReplications or corruptReplicas, because it's in startup safe mode.\n   * Doesn't log every block, because there are typically millions of them.\n   * \n   * @throws IOException\n   */\n  private void addStoredBlockImmediate(BlockInfo storedBlock, Block reported,\n      DatanodeStorageInfo storageInfo)\n  throws IOException {\n    assert (storedBlock != null && namesystem.hasWriteLock());\n    if (!namesystem.isInStartupSafeMode()\n        || isPopulatingReplQueues()) {\n      addStoredBlock(storedBlock, reported, storageInfo, null, false);\n      return;\n    }\n\n    // just add it\n    AddBlockResult result = storageInfo.addBlock(storedBlock, reported);\n\n    // Now check for completion of blocks and safe block count\n    int numCurrentReplica = countLiveNodes(storedBlock);\n    if (storedBlock.getBlockUCState() == BlockUCState.COMMITTED\n        && hasMinStorage(storedBlock, numCurrentReplica)) {\n      completeBlock(storedBlock, null, false);\n    } else if (storedBlock.isComplete() && result == AddBlockResult.ADDED) {\n      // check whether safe replication is reached for the block\n      // only complete blocks are counted towards that.\n      // In the case that the block just became complete above, completeBlock()\n      // handles the safe block count maintenance.\n      bmSafeMode.incrementSafeBlockCount(numCurrentReplica, storedBlock);\n    }\n  }\n\n  /**\n   * Modify (block-->datanode) map. Remove block from set of\n   * needed reconstruction if this takes care of the problem.\n   * @return the block that is stored in blocksMap.\n   */\n  private Block addStoredBlock(final BlockInfo block,\n                               final Block reportedBlock,\n                               DatanodeStorageInfo storageInfo,\n                               DatanodeDescriptor delNodeHint,\n                               boolean logEveryBlock)\n  throws IOException {\n    assert block != null && namesystem.hasWriteLock();\n    BlockInfo storedBlock;\n    DatanodeDescriptor node = storageInfo.getDatanodeDescriptor();\n    if (!block.isComplete()) {\n      //refresh our copy in case the block got completed in another thread\n      storedBlock = getStoredBlock(block);\n    } else {\n      storedBlock = block;\n    }\n    if (storedBlock == null || storedBlock.isDeleted()) {\n      // If this block does not belong to anyfile, then we are done.\n      blockLog.debug(\"BLOCK* addStoredBlock: {} on {} size {} but it does not\" +\n          \" belong to any file\", block, node, block.getNumBytes());", "child_info": "node name isBlockManager:isNeededReconstruction(Block,int,int)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] BlockManager:isNeededReconstruction(Block,int,int)\n  IF: numLiveReplicas >= expected -> false\n  ELSE -> true\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] BlockManager:isNeededReconstruction(Block,int,int)\n  IF: numLiveReplicas >= expected -> false\n  ELSE -> true\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```source code:}\n\n  boolean isNeededReconstruction(BlockInfo storedBlock,\n      NumberReplicas numberReplicas) {\n    return isNeededReconstruction(storedBlock, numberReplicas, 0);\n  }\n\n  /**\n   * A block needs reconstruction if the number of redundancies is less than\n   * expected or if it does not have enough racks.\n   */\n  boolean isNeededReconstruction(BlockInfo storedBlock,\n      NumberReplicas numberReplicas, int pending) {\n    return storedBlock.isComplete() &&\n        !hasEnoughEffectiveReplicas(storedBlock, numberReplicas, pending);\n  }\n\n  // Exclude maintenance, but make sure it has minimal live replicas\n  // to satisfy the maintenance requirement.\n  public short getExpectedLiveRedundancyNum(BlockInfo block,\n      NumberReplicas numberReplicas) {\n    final short expectedRedundancy = getExpectedRedundancyNum(block);\n    return (short)Math.max(expectedRedundancy -\n        numberReplicas.maintenanceReplicas(),\n        getMinMaintenanceStorageNum(block));\n  }\n\n  public short getExpectedRedundancyNum(BlockInfo block) {\n    return block.isStriped() ?\n        ((BlockInfoStriped) block).getRealTotalBlockNum() :\n        block.getReplication();\n  }\n\n  public long getMissingBlocksCount() {\n    // not locking\n    return this.neededReconstruction.getCorruptBlockSize();\n  }\n\n  public long getMissingReplOneBlocksCount() {\n    // not locking\n    return this.neededReconstruction.getCorruptReplicationOneBlockSize();\n  }\n\n  public long getHighestPriorityReplicatedBlockCount(){\n    return this.neededReconstruction.getHighestPriorityReplicatedBlockCount();\n  }\n\n  public long getHighestPriorityECBlockCount(){\n    return this.neededReconstruction.getHighestPriorityECBlockCount();\n  }\n\n  public BlockInfo addBlockCollection(BlockInfo block,\n      BlockCollection bc) {\n    BlockInfo blockInfo = blocksMap.addBlockCollection(block, bc);\n    blockIdManager.setGenerationStampIfGreater(block.getGenerationStamp());\n    return blockInfo;\n  }\n\n  /**\n   * Do some check when adding a block to blocksmap.\n   * For HDFS-7994 to check whether then block is a NonEcBlockUsingStripedID.\n   *"}
{"mp_id": "hdfs::DM::registerDatanode->heartbeatManagerRegister", "caller": "DatanodeManager:registerDatanode(DatanodeRegistration)", "callee": "HeartbeatManager:register(DatanodeDescriptor)", "gt": "valid", "stratum": "complex", "infeasible_reason": "", "rationale": "After resolving network location, registers node with heartbeatManager.", "caller_code": "*    denied because resolving datanode network location fails.\n   */\n  public void registerDatanode(DatanodeRegistration nodeReg)\n      throws DisallowedDatanodeException, UnresolvedTopologyException {\n    InetAddress dnAddress = Server.getRemoteIp();\n    if (dnAddress != null) {\n      // Mostly called inside an RPC, update ip and peer hostname\n      String hostname = dnAddress.getHostName();\n      String ip = dnAddress.getHostAddress();\n      if (checkIpHostnameInRegistration && !isNameResolved(dnAddress)) {\n        // Reject registration of unresolved datanode to prevent performance\n        // impact of repetitive DNS lookups later.\n        final String message = \"hostname cannot be resolved (ip=\"\n            + ip + \", hostname=\" + hostname + \")\";\n        LOG.warn(\"Unresolved datanode registration: \" + message);\n        throw new DisallowedDatanodeException(nodeReg, message);\n      }\n      // update node registration with the ip and hostname from rpc request\n      nodeReg.setIpAddr(ip);\n      nodeReg.setPeerHostName(hostname);\n    }\n    \n    try {\n      nodeReg.setExportedKeys(blockManager.getBlockKeys());\n  \n      // Checks if the node is not on the hosts list.  If it is not, then\n      // it will be disallowed from registering. \n      if (!hostConfigManager.isIncluded(nodeReg)) {\n        throw new DisallowedDatanodeException(nodeReg);\n      }\n        \n      NameNode.stateChangeLog.info(\"BLOCK* registerDatanode: from \"\n          + nodeReg + \" storage \" + nodeReg.getDatanodeUuid());\n  \n      DatanodeDescriptor nodeS = getDatanode(nodeReg.getDatanodeUuid());\n      DatanodeDescriptor nodeN = host2DatanodeMap.getDatanodeByXferAddr(\n          nodeReg.getIpAddr(), nodeReg.getXferPort());\n        \n      // Non-change. REMOVE. Number 2\n      if (nodeN != null && nodeN != nodeS) {\n        NameNode.LOG.info(\"BLOCK* registerDatanode: \" + nodeN);\n        // nodeN previously served a different data storage, \n        // which is not served by anybody anymore.\n        removeDatanode(nodeN);\n        // physically remove node from datanodeMap\n        wipeDatanode(nodeN);\n        nodeN = null;\n      }\n  \n      if (nodeS != null) {\n        if (nodeN == nodeS) {\n          // The same datanode has been just restarted to serve the same data \n          // storage. We do not need to remove old data blocks, the delta will\n          // be calculated on the next block report from the datanode\n          if(NameNode.stateChangeLog.isDebugEnabled()) {\n            NameNode.stateChangeLog.debug(\"BLOCK* registerDatanode: \"\n                + \"node restarted.\");\n          }\n        } else {\n          // nodeS is found\n          /* The registering datanode is a replacement node for the existing\n            data storage, which from now on will be served by a new node.", "callee_code": "}\n\n  synchronized void register(final DatanodeDescriptor d) {\n    if (!d.isAlive()) {\n      addDatanode(d);\n\n      //update its timestamp\n      d.updateHeartbeatState(StorageReport.EMPTY_ARRAY, 0L, 0L, 0, 0, null);\n      stats.add(d);\n    }\n  }\n\n  synchronized DatanodeDescriptor[] getDatanodes() {\n    return datanodes.toArray(new DatanodeDescriptor[datanodes.size()]);\n  }\n\n  synchronized void addDatanode(final DatanodeDescriptor d) {\n    // update in-service node count\n    datanodes.add(d);\n    d.setAlive(true);\n  }\n\n  void updateDnStat(final DatanodeDescriptor d){\n    stats.add(d);\n  }\n\n  synchronized void removeDatanode(DatanodeDescriptor node) {\n    if (node.isAlive()) {\n      stats.subtract(node);\n      datanodes.remove(node);\n      removeNodeFromStaleList(node);\n      node.setAlive(false);\n    }\n  }\n\n  synchronized void updateHeartbeat(final DatanodeDescriptor node,\n      StorageReport[] reports, long cacheCapacity, long cacheUsed,\n      int xceiverCount, int failedVolumes,\n      VolumeFailureSummary volumeFailureSummary) {\n    stats.subtract(node);\n    blockManager.updateHeartbeat(node, reports, cacheCapacity, cacheUsed,\n        xceiverCount, failedVolumes, volumeFailureSummary);\n    stats.add(node);\n  }\n\n  synchronized void updateLifeline(final DatanodeDescriptor node,\n      StorageReport[] reports, long cacheCapacity, long cacheUsed,\n      int xceiverCount, int failedVolumes,\n      VolumeFailureSummary volumeFailureSummary) {\n    stats.subtract(node);\n    // This intentionally calls updateHeartbeatState instead of\n    // updateHeartbeat, because we don't want to modify the\n    // heartbeatedSinceRegistration flag.  Arrival of a lifeline message does\n    // not count as arrival of the first heartbeat.\n    blockManager.updateHeartbeatState(node, reports, cacheCapacity, cacheUsed,\n        xceiverCount, failedVolumes, volumeFailureSummary);\n    stats.add(node);\n  }\n\n  synchronized void startDecommission(final DatanodeDescriptor node) {\n    if (!node.isAlive()) {\n      LOG.info(\"Dead node {} is decommissioned immediately.\", node);", "caller_log_raw_stripped": "[ENTRY] DatanodeManager:registerDatanode(DatanodeRegistration)\n  IF: isIncluded(nodeReg) && topology resolved\n  CALL: heartbeatManager.register(nodeS)", "callee_log_raw_stripped": "[ENTRY] HeartbeatManager:register(DatanodeDescriptor)\n  add to tracking\n  EXIT", "caller_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DatanodeManager:registerDatanode(DatanodeRegistration)\n  IF: isIncluded(nodeReg) && topology resolved\n  CALL: heartbeatManager.register(nodeS)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DatanodeManager:registerDatanode(DatanodeRegistration)\n  IF: isIncluded(nodeReg) && topology resolved\n  CALL: heartbeatManager.register(nodeS)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "callee_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] HeartbeatManager:register(DatanodeDescriptor)\n  add to tracking\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] HeartbeatManager:register(DatanodeDescriptor)\n  add to tracking\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "parent_info": "node name is DatanodeManager:registerDatanode(DatanodeRegistration)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DatanodeManager:registerDatanode(DatanodeRegistration)\n  IF: isIncluded(nodeReg) && topology resolved\n  CALL: heartbeatManager.register(nodeS)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DatanodeManager:registerDatanode(DatanodeRegistration)\n  IF: isIncluded(nodeReg) && topology resolved\n  CALL: heartbeatManager.register(nodeS)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```souce code:*    denied because resolving datanode network location fails.\n   */\n  public void registerDatanode(DatanodeRegistration nodeReg)\n      throws DisallowedDatanodeException, UnresolvedTopologyException {\n    InetAddress dnAddress = Server.getRemoteIp();\n    if (dnAddress != null) {\n      // Mostly called inside an RPC, update ip and peer hostname\n      String hostname = dnAddress.getHostName();\n      String ip = dnAddress.getHostAddress();\n      if (checkIpHostnameInRegistration && !isNameResolved(dnAddress)) {\n        // Reject registration of unresolved datanode to prevent performance\n        // impact of repetitive DNS lookups later.\n        final String message = \"hostname cannot be resolved (ip=\"\n            + ip + \", hostname=\" + hostname + \")\";\n        LOG.warn(\"Unresolved datanode registration: \" + message);\n        throw new DisallowedDatanodeException(nodeReg, message);\n      }\n      // update node registration with the ip and hostname from rpc request\n      nodeReg.setIpAddr(ip);\n      nodeReg.setPeerHostName(hostname);\n    }\n    \n    try {\n      nodeReg.setExportedKeys(blockManager.getBlockKeys());\n  \n      // Checks if the node is not on the hosts list.  If it is not, then\n      // it will be disallowed from registering. \n      if (!hostConfigManager.isIncluded(nodeReg)) {\n        throw new DisallowedDatanodeException(nodeReg);\n      }\n        \n      NameNode.stateChangeLog.info(\"BLOCK* registerDatanode: from \"\n          + nodeReg + \" storage \" + nodeReg.getDatanodeUuid());\n  \n      DatanodeDescriptor nodeS = getDatanode(nodeReg.getDatanodeUuid());\n      DatanodeDescriptor nodeN = host2DatanodeMap.getDatanodeByXferAddr(\n          nodeReg.getIpAddr(), nodeReg.getXferPort());\n        \n      // Non-change. REMOVE. Number 2\n      if (nodeN != null && nodeN != nodeS) {\n        NameNode.LOG.info(\"BLOCK* registerDatanode: \" + nodeN);\n        // nodeN previously served a different data storage, \n        // which is not served by anybody anymore.\n        removeDatanode(nodeN);\n        // physically remove node from datanodeMap\n        wipeDatanode(nodeN);\n        nodeN = null;\n      }\n  \n      if (nodeS != null) {\n        if (nodeN == nodeS) {\n          // The same datanode has been just restarted to serve the same data \n          // storage. We do not need to remove old data blocks, the delta will\n          // be calculated on the next block report from the datanode\n          if(NameNode.stateChangeLog.isDebugEnabled()) {\n            NameNode.stateChangeLog.debug(\"BLOCK* registerDatanode: \"\n                + \"node restarted.\");\n          }\n        } else {\n          // nodeS is found\n          /* The registering datanode is a replacement node for the existing\n            data storage, which from now on will be served by a new node.", "child_info": "node name isHeartbeatManager:register(DatanodeDescriptor)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] HeartbeatManager:register(DatanodeDescriptor)\n  add to tracking\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] HeartbeatManager:register(DatanodeDescriptor)\n  add to tracking\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```source code:}\n\n  synchronized void register(final DatanodeDescriptor d) {\n    if (!d.isAlive()) {\n      addDatanode(d);\n\n      //update its timestamp\n      d.updateHeartbeatState(StorageReport.EMPTY_ARRAY, 0L, 0L, 0, 0, null);\n      stats.add(d);\n    }\n  }\n\n  synchronized DatanodeDescriptor[] getDatanodes() {\n    return datanodes.toArray(new DatanodeDescriptor[datanodes.size()]);\n  }\n\n  synchronized void addDatanode(final DatanodeDescriptor d) {\n    // update in-service node count\n    datanodes.add(d);\n    d.setAlive(true);\n  }\n\n  void updateDnStat(final DatanodeDescriptor d){\n    stats.add(d);\n  }\n\n  synchronized void removeDatanode(DatanodeDescriptor node) {\n    if (node.isAlive()) {\n      stats.subtract(node);\n      datanodes.remove(node);\n      removeNodeFromStaleList(node);\n      node.setAlive(false);\n    }\n  }\n\n  synchronized void updateHeartbeat(final DatanodeDescriptor node,\n      StorageReport[] reports, long cacheCapacity, long cacheUsed,\n      int xceiverCount, int failedVolumes,\n      VolumeFailureSummary volumeFailureSummary) {\n    stats.subtract(node);\n    blockManager.updateHeartbeat(node, reports, cacheCapacity, cacheUsed,\n        xceiverCount, failedVolumes, volumeFailureSummary);\n    stats.add(node);\n  }\n\n  synchronized void updateLifeline(final DatanodeDescriptor node,\n      StorageReport[] reports, long cacheCapacity, long cacheUsed,\n      int xceiverCount, int failedVolumes,\n      VolumeFailureSummary volumeFailureSummary) {\n    stats.subtract(node);\n    // This intentionally calls updateHeartbeatState instead of\n    // updateHeartbeat, because we don't want to modify the\n    // heartbeatedSinceRegistration flag.  Arrival of a lifeline message does\n    // not count as arrival of the first heartbeat.\n    blockManager.updateHeartbeatState(node, reports, cacheCapacity, cacheUsed,\n        xceiverCount, failedVolumes, volumeFailureSummary);\n    stats.add(node);\n  }\n\n  synchronized void startDecommission(final DatanodeDescriptor node) {\n    if (!node.isAlive()) {\n      LOG.info(\"Dead node {} is decommissioned immediately.\", node);"}
{"mp_id": "hdfs::DM::registerDatanode->heartbeatManagerRegister-INFEASIBLE", "caller": "DatanodeManager:registerDatanode(...)", "callee": "HeartbeatManager:register(...)", "gt": "infeasible", "stratum": "complex", "infeasible_reason": "rejectUnresolvedTopologyDN==true && resolve returns /default-rack — DisallowedDatanodeException thrown.", "rationale": "", "caller_code": "*    denied because resolving datanode network location fails.\n   */\n  public void registerDatanode(DatanodeRegistration nodeReg)\n      throws DisallowedDatanodeException, UnresolvedTopologyException {\n    InetAddress dnAddress = Server.getRemoteIp();\n    if (dnAddress != null) {\n      // Mostly called inside an RPC, update ip and peer hostname\n      String hostname = dnAddress.getHostName();\n      String ip = dnAddress.getHostAddress();\n      if (checkIpHostnameInRegistration && !isNameResolved(dnAddress)) {\n        // Reject registration of unresolved datanode to prevent performance\n        // impact of repetitive DNS lookups later.\n        final String message = \"hostname cannot be resolved (ip=\"\n            + ip + \", hostname=\" + hostname + \")\";\n        LOG.warn(\"Unresolved datanode registration: \" + message);\n        throw new DisallowedDatanodeException(nodeReg, message);\n      }\n      // update node registration with the ip and hostname from rpc request\n      nodeReg.setIpAddr(ip);\n      nodeReg.setPeerHostName(hostname);\n    }\n    \n    try {\n      nodeReg.setExportedKeys(blockManager.getBlockKeys());\n  \n      // Checks if the node is not on the hosts list.  If it is not, then\n      // it will be disallowed from registering. \n      if (!hostConfigManager.isIncluded(nodeReg)) {\n        throw new DisallowedDatanodeException(nodeReg);\n      }\n        \n      NameNode.stateChangeLog.info(\"BLOCK* registerDatanode: from \"\n          + nodeReg + \" storage \" + nodeReg.getDatanodeUuid());\n  \n      DatanodeDescriptor nodeS = getDatanode(nodeReg.getDatanodeUuid());\n      DatanodeDescriptor nodeN = host2DatanodeMap.getDatanodeByXferAddr(\n          nodeReg.getIpAddr(), nodeReg.getXferPort());\n        \n      // Non-change. REMOVE. Number 2\n      if (nodeN != null && nodeN != nodeS) {\n        NameNode.LOG.info(\"BLOCK* registerDatanode: \" + nodeN);\n        // nodeN previously served a different data storage, \n        // which is not served by anybody anymore.\n        removeDatanode(nodeN);\n        // physically remove node from datanodeMap\n        wipeDatanode(nodeN);\n        nodeN = null;\n      }\n  \n      if (nodeS != null) {\n        if (nodeN == nodeS) {\n          // The same datanode has been just restarted to serve the same data \n          // storage. We do not need to remove old data blocks, the delta will\n          // be calculated on the next block report from the datanode\n          if(NameNode.stateChangeLog.isDebugEnabled()) {\n            NameNode.stateChangeLog.debug(\"BLOCK* registerDatanode: \"\n                + \"node restarted.\");\n          }\n        } else {\n          // nodeS is found\n          /* The registering datanode is a replacement node for the existing\n            data storage, which from now on will be served by a new node.", "callee_code": "}\n\n  synchronized void register(final DatanodeDescriptor d) {\n    if (!d.isAlive()) {\n      addDatanode(d);\n\n      //update its timestamp\n      d.updateHeartbeatState(StorageReport.EMPTY_ARRAY, 0L, 0L, 0, 0, null);\n      stats.add(d);\n    }\n  }\n\n  synchronized DatanodeDescriptor[] getDatanodes() {\n    return datanodes.toArray(new DatanodeDescriptor[datanodes.size()]);\n  }\n\n  synchronized void addDatanode(final DatanodeDescriptor d) {\n    // update in-service node count\n    datanodes.add(d);\n    d.setAlive(true);\n  }\n\n  void updateDnStat(final DatanodeDescriptor d){\n    stats.add(d);\n  }\n\n  synchronized void removeDatanode(DatanodeDescriptor node) {\n    if (node.isAlive()) {\n      stats.subtract(node);\n      datanodes.remove(node);\n      removeNodeFromStaleList(node);\n      node.setAlive(false);\n    }\n  }\n\n  synchronized void updateHeartbeat(final DatanodeDescriptor node,\n      StorageReport[] reports, long cacheCapacity, long cacheUsed,\n      int xceiverCount, int failedVolumes,\n      VolumeFailureSummary volumeFailureSummary) {\n    stats.subtract(node);\n    blockManager.updateHeartbeat(node, reports, cacheCapacity, cacheUsed,\n        xceiverCount, failedVolumes, volumeFailureSummary);\n    stats.add(node);\n  }\n\n  synchronized void updateLifeline(final DatanodeDescriptor node,\n      StorageReport[] reports, long cacheCapacity, long cacheUsed,\n      int xceiverCount, int failedVolumes,\n      VolumeFailureSummary volumeFailureSummary) {\n    stats.subtract(node);\n    // This intentionally calls updateHeartbeatState instead of\n    // updateHeartbeat, because we don't want to modify the\n    // heartbeatedSinceRegistration flag.  Arrival of a lifeline message does\n    // not count as arrival of the first heartbeat.\n    blockManager.updateHeartbeatState(node, reports, cacheCapacity, cacheUsed,\n        xceiverCount, failedVolumes, volumeFailureSummary);\n    stats.add(node);\n  }\n\n  synchronized void startDecommission(final DatanodeDescriptor node) {\n    if (!node.isAlive()) {\n      LOG.info(\"Dead node {} is decommissioned immediately.\", node);", "caller_log_raw_stripped": "[ENTRY] DatanodeManager:registerDatanode(...)\n  IF: rejectUnresolvedTopologyDN && resolve(...)==/default-rack \n  -> throw DisallowedDatanodeException", "callee_log_raw_stripped": "[ENTRY] HeartbeatManager:register(...)", "caller_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DatanodeManager:registerDatanode(...)\n  IF: rejectUnresolvedTopologyDN && resolve(...)==/default-rack \n  -> throw DisallowedDatanodeException\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DatanodeManager:registerDatanode(...)\n  IF: rejectUnresolvedTopologyDN && resolve(...)==/default-rack \n  -> throw DisallowedDatanodeException\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "callee_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] HeartbeatManager:register(...)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] HeartbeatManager:register(...)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "parent_info": "node name is DatanodeManager:registerDatanode(...)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DatanodeManager:registerDatanode(...)\n  IF: rejectUnresolvedTopologyDN && resolve(...)==/default-rack \n  -> throw DisallowedDatanodeException\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DatanodeManager:registerDatanode(...)\n  IF: rejectUnresolvedTopologyDN && resolve(...)==/default-rack \n  -> throw DisallowedDatanodeException\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```souce code:*    denied because resolving datanode network location fails.\n   */\n  public void registerDatanode(DatanodeRegistration nodeReg)\n      throws DisallowedDatanodeException, UnresolvedTopologyException {\n    InetAddress dnAddress = Server.getRemoteIp();\n    if (dnAddress != null) {\n      // Mostly called inside an RPC, update ip and peer hostname\n      String hostname = dnAddress.getHostName();\n      String ip = dnAddress.getHostAddress();\n      if (checkIpHostnameInRegistration && !isNameResolved(dnAddress)) {\n        // Reject registration of unresolved datanode to prevent performance\n        // impact of repetitive DNS lookups later.\n        final String message = \"hostname cannot be resolved (ip=\"\n            + ip + \", hostname=\" + hostname + \")\";\n        LOG.warn(\"Unresolved datanode registration: \" + message);\n        throw new DisallowedDatanodeException(nodeReg, message);\n      }\n      // update node registration with the ip and hostname from rpc request\n      nodeReg.setIpAddr(ip);\n      nodeReg.setPeerHostName(hostname);\n    }\n    \n    try {\n      nodeReg.setExportedKeys(blockManager.getBlockKeys());\n  \n      // Checks if the node is not on the hosts list.  If it is not, then\n      // it will be disallowed from registering. \n      if (!hostConfigManager.isIncluded(nodeReg)) {\n        throw new DisallowedDatanodeException(nodeReg);\n      }\n        \n      NameNode.stateChangeLog.info(\"BLOCK* registerDatanode: from \"\n          + nodeReg + \" storage \" + nodeReg.getDatanodeUuid());\n  \n      DatanodeDescriptor nodeS = getDatanode(nodeReg.getDatanodeUuid());\n      DatanodeDescriptor nodeN = host2DatanodeMap.getDatanodeByXferAddr(\n          nodeReg.getIpAddr(), nodeReg.getXferPort());\n        \n      // Non-change. REMOVE. Number 2\n      if (nodeN != null && nodeN != nodeS) {\n        NameNode.LOG.info(\"BLOCK* registerDatanode: \" + nodeN);\n        // nodeN previously served a different data storage, \n        // which is not served by anybody anymore.\n        removeDatanode(nodeN);\n        // physically remove node from datanodeMap\n        wipeDatanode(nodeN);\n        nodeN = null;\n      }\n  \n      if (nodeS != null) {\n        if (nodeN == nodeS) {\n          // The same datanode has been just restarted to serve the same data \n          // storage. We do not need to remove old data blocks, the delta will\n          // be calculated on the next block report from the datanode\n          if(NameNode.stateChangeLog.isDebugEnabled()) {\n            NameNode.stateChangeLog.debug(\"BLOCK* registerDatanode: \"\n                + \"node restarted.\");\n          }\n        } else {\n          // nodeS is found\n          /* The registering datanode is a replacement node for the existing\n            data storage, which from now on will be served by a new node.", "child_info": "node name isHeartbeatManager:register(...)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] HeartbeatManager:register(...)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] HeartbeatManager:register(...)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```source code:}\n\n  synchronized void register(final DatanodeDescriptor d) {\n    if (!d.isAlive()) {\n      addDatanode(d);\n\n      //update its timestamp\n      d.updateHeartbeatState(StorageReport.EMPTY_ARRAY, 0L, 0L, 0, 0, null);\n      stats.add(d);\n    }\n  }\n\n  synchronized DatanodeDescriptor[] getDatanodes() {\n    return datanodes.toArray(new DatanodeDescriptor[datanodes.size()]);\n  }\n\n  synchronized void addDatanode(final DatanodeDescriptor d) {\n    // update in-service node count\n    datanodes.add(d);\n    d.setAlive(true);\n  }\n\n  void updateDnStat(final DatanodeDescriptor d){\n    stats.add(d);\n  }\n\n  synchronized void removeDatanode(DatanodeDescriptor node) {\n    if (node.isAlive()) {\n      stats.subtract(node);\n      datanodes.remove(node);\n      removeNodeFromStaleList(node);\n      node.setAlive(false);\n    }\n  }\n\n  synchronized void updateHeartbeat(final DatanodeDescriptor node,\n      StorageReport[] reports, long cacheCapacity, long cacheUsed,\n      int xceiverCount, int failedVolumes,\n      VolumeFailureSummary volumeFailureSummary) {\n    stats.subtract(node);\n    blockManager.updateHeartbeat(node, reports, cacheCapacity, cacheUsed,\n        xceiverCount, failedVolumes, volumeFailureSummary);\n    stats.add(node);\n  }\n\n  synchronized void updateLifeline(final DatanodeDescriptor node,\n      StorageReport[] reports, long cacheCapacity, long cacheUsed,\n      int xceiverCount, int failedVolumes,\n      VolumeFailureSummary volumeFailureSummary) {\n    stats.subtract(node);\n    // This intentionally calls updateHeartbeatState instead of\n    // updateHeartbeat, because we don't want to modify the\n    // heartbeatedSinceRegistration flag.  Arrival of a lifeline message does\n    // not count as arrival of the first heartbeat.\n    blockManager.updateHeartbeatState(node, reports, cacheCapacity, cacheUsed,\n        xceiverCount, failedVolumes, volumeFailureSummary);\n    stats.add(node);\n  }\n\n  synchronized void startDecommission(final DatanodeDescriptor node) {\n    if (!node.isAlive()) {\n      LOG.info(\"Dead node {} is decommissioned immediately.\", node);"}
{"mp_id": "hdfs::DM::registerDatanode->updateRegInfo", "caller": "DatanodeManager:registerDatanode(DatanodeRegistration)", "callee": "DatanodeDescriptor:updateRegInfo(DatanodeRegistration)", "gt": "valid", "stratum": "complex", "infeasible_reason": "", "rationale": "Replacement scenario: same UUID but different IP/port.", "caller_code": "*    denied because resolving datanode network location fails.\n   */\n  public void registerDatanode(DatanodeRegistration nodeReg)\n      throws DisallowedDatanodeException, UnresolvedTopologyException {\n    InetAddress dnAddress = Server.getRemoteIp();\n    if (dnAddress != null) {\n      // Mostly called inside an RPC, update ip and peer hostname\n      String hostname = dnAddress.getHostName();\n      String ip = dnAddress.getHostAddress();\n      if (checkIpHostnameInRegistration && !isNameResolved(dnAddress)) {\n        // Reject registration of unresolved datanode to prevent performance\n        // impact of repetitive DNS lookups later.\n        final String message = \"hostname cannot be resolved (ip=\"\n            + ip + \", hostname=\" + hostname + \")\";\n        LOG.warn(\"Unresolved datanode registration: \" + message);\n        throw new DisallowedDatanodeException(nodeReg, message);\n      }\n      // update node registration with the ip and hostname from rpc request\n      nodeReg.setIpAddr(ip);\n      nodeReg.setPeerHostName(hostname);\n    }\n    \n    try {\n      nodeReg.setExportedKeys(blockManager.getBlockKeys());\n  \n      // Checks if the node is not on the hosts list.  If it is not, then\n      // it will be disallowed from registering. \n      if (!hostConfigManager.isIncluded(nodeReg)) {\n        throw new DisallowedDatanodeException(nodeReg);\n      }\n        \n      NameNode.stateChangeLog.info(\"BLOCK* registerDatanode: from \"\n          + nodeReg + \" storage \" + nodeReg.getDatanodeUuid());\n  \n      DatanodeDescriptor nodeS = getDatanode(nodeReg.getDatanodeUuid());\n      DatanodeDescriptor nodeN = host2DatanodeMap.getDatanodeByXferAddr(\n          nodeReg.getIpAddr(), nodeReg.getXferPort());\n        \n      // Non-change. REMOVE. Number 2\n      if (nodeN != null && nodeN != nodeS) {\n        NameNode.LOG.info(\"BLOCK* registerDatanode: \" + nodeN);\n        // nodeN previously served a different data storage, \n        // which is not served by anybody anymore.\n        removeDatanode(nodeN);\n        // physically remove node from datanodeMap\n        wipeDatanode(nodeN);\n        nodeN = null;\n      }\n  \n      if (nodeS != null) {\n        if (nodeN == nodeS) {\n          // The same datanode has been just restarted to serve the same data \n          // storage. We do not need to remove old data blocks, the delta will\n          // be calculated on the next block report from the datanode\n          if(NameNode.stateChangeLog.isDebugEnabled()) {\n            NameNode.stateChangeLog.debug(\"BLOCK* registerDatanode: \"\n                + \"node restarted.\");\n          }\n        } else {\n          // nodeS is found\n          /* The registering datanode is a replacement node for the existing\n            data storage, which from now on will be served by a new node.", "callee_code": "*/\n  @Override\n  public void updateRegInfo(DatanodeID nodeReg) {\n    super.updateRegInfo(nodeReg);\n    \n    // must re-process IBR after re-registration\n    for(DatanodeStorageInfo storage : getStorageInfos()) {\n      if (storage.getStorageType() != StorageType.PROVIDED) {\n        storage.setBlockReportCount(0);\n      }\n    }\n    heartbeatedSinceRegistration = false;\n    forceRegistration = false;\n  }\n\n  /**\n   * @return balancer bandwidth in bytes per second for this datanode\n   */\n  public synchronized long getBalancerBandwidth() {\n    return this.bandwidth;\n  }\n\n  /**\n   * @param bandwidth balancer bandwidth in bytes per second for this datanode\n   */\n  public synchronized void setBalancerBandwidth(long bandwidth) {\n    this.bandwidth = bandwidth;\n  }\n\n  @Override\n  public String dumpDatanode() {\n    StringBuilder sb = new StringBuilder(super.dumpDatanode());\n    int repl = replicateBlocks.size();\n    if (repl > 0) {\n      sb.append(\" \").append(repl).append(\" blocks to be replicated;\");\n    }\n    int ec = erasurecodeBlocks.size();\n    if(ec > 0) {\n      sb.append(\" \").append(ec).append(\" blocks to be erasure coded;\");\n    }\n    int inval = invalidateBlocks.size();\n    if (inval > 0) {\n      sb.append(\" \").append(inval).append(\" blocks to be invalidated;\");      \n    }\n    int recover = recoverBlocks.size();\n    if (recover > 0) {\n      sb.append(\" \").append(recover).append(\" blocks to be recovered;\");\n    }\n    return sb.toString();\n  }\n\n  DatanodeStorageInfo updateStorage(DatanodeStorage s) {\n    synchronized (storageMap) {\n      DatanodeStorageInfo storage = storageMap.get(s.getStorageID());\n      DFSTopologyNodeImpl parent = null;\n      if (getParent() instanceof DFSTopologyNodeImpl) {\n        parent = (DFSTopologyNodeImpl) getParent();\n      }\n\n      if (storage == null) {\n        LOG.info(\"Adding new storage ID {} for DN {}\", s.getStorageID(),\n            getXferAddr());", "caller_log_raw_stripped": "[ENTRY] DatanodeManager:registerDatanode(...)\n  nodeS=getDatanode(uuid); nodeN=getDatanodeByXferAddr(ip,port)\n  IF: nodeS!=null && nodeS!=nodeN (replacement)\n  CALL: nodeS.updateRegInfo(nodeReg)", "callee_log_raw_stripped": "[ENTRY] DatanodeDescriptor:updateRegInfo(DatanodeRegistration)\n  update ip/port/version\n  EXIT", "caller_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DatanodeManager:registerDatanode(...)\n  nodeS=getDatanode(uuid); nodeN=getDatanodeByXferAddr(ip,port)\n  IF: nodeS!=null && nodeS!=nodeN (replacement)\n  CALL: nodeS.updateRegInfo(nodeReg)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DatanodeManager:registerDatanode(...)\n  nodeS=getDatanode(uuid); nodeN=getDatanodeByXferAddr(ip,port)\n  IF: nodeS!=null && nodeS!=nodeN (replacement)\n  CALL: nodeS.updateRegInfo(nodeReg)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "callee_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DatanodeDescriptor:updateRegInfo(DatanodeRegistration)\n  update ip/port/version\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DatanodeDescriptor:updateRegInfo(DatanodeRegistration)\n  update ip/port/version\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "parent_info": "node name is DatanodeManager:registerDatanode(DatanodeRegistration)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DatanodeManager:registerDatanode(...)\n  nodeS=getDatanode(uuid); nodeN=getDatanodeByXferAddr(ip,port)\n  IF: nodeS!=null && nodeS!=nodeN (replacement)\n  CALL: nodeS.updateRegInfo(nodeReg)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DatanodeManager:registerDatanode(...)\n  nodeS=getDatanode(uuid); nodeN=getDatanodeByXferAddr(ip,port)\n  IF: nodeS!=null && nodeS!=nodeN (replacement)\n  CALL: nodeS.updateRegInfo(nodeReg)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```souce code:*    denied because resolving datanode network location fails.\n   */\n  public void registerDatanode(DatanodeRegistration nodeReg)\n      throws DisallowedDatanodeException, UnresolvedTopologyException {\n    InetAddress dnAddress = Server.getRemoteIp();\n    if (dnAddress != null) {\n      // Mostly called inside an RPC, update ip and peer hostname\n      String hostname = dnAddress.getHostName();\n      String ip = dnAddress.getHostAddress();\n      if (checkIpHostnameInRegistration && !isNameResolved(dnAddress)) {\n        // Reject registration of unresolved datanode to prevent performance\n        // impact of repetitive DNS lookups later.\n        final String message = \"hostname cannot be resolved (ip=\"\n            + ip + \", hostname=\" + hostname + \")\";\n        LOG.warn(\"Unresolved datanode registration: \" + message);\n        throw new DisallowedDatanodeException(nodeReg, message);\n      }\n      // update node registration with the ip and hostname from rpc request\n      nodeReg.setIpAddr(ip);\n      nodeReg.setPeerHostName(hostname);\n    }\n    \n    try {\n      nodeReg.setExportedKeys(blockManager.getBlockKeys());\n  \n      // Checks if the node is not on the hosts list.  If it is not, then\n      // it will be disallowed from registering. \n      if (!hostConfigManager.isIncluded(nodeReg)) {\n        throw new DisallowedDatanodeException(nodeReg);\n      }\n        \n      NameNode.stateChangeLog.info(\"BLOCK* registerDatanode: from \"\n          + nodeReg + \" storage \" + nodeReg.getDatanodeUuid());\n  \n      DatanodeDescriptor nodeS = getDatanode(nodeReg.getDatanodeUuid());\n      DatanodeDescriptor nodeN = host2DatanodeMap.getDatanodeByXferAddr(\n          nodeReg.getIpAddr(), nodeReg.getXferPort());\n        \n      // Non-change. REMOVE. Number 2\n      if (nodeN != null && nodeN != nodeS) {\n        NameNode.LOG.info(\"BLOCK* registerDatanode: \" + nodeN);\n        // nodeN previously served a different data storage, \n        // which is not served by anybody anymore.\n        removeDatanode(nodeN);\n        // physically remove node from datanodeMap\n        wipeDatanode(nodeN);\n        nodeN = null;\n      }\n  \n      if (nodeS != null) {\n        if (nodeN == nodeS) {\n          // The same datanode has been just restarted to serve the same data \n          // storage. We do not need to remove old data blocks, the delta will\n          // be calculated on the next block report from the datanode\n          if(NameNode.stateChangeLog.isDebugEnabled()) {\n            NameNode.stateChangeLog.debug(\"BLOCK* registerDatanode: \"\n                + \"node restarted.\");\n          }\n        } else {\n          // nodeS is found\n          /* The registering datanode is a replacement node for the existing\n            data storage, which from now on will be served by a new node.", "child_info": "node name isDatanodeDescriptor:updateRegInfo(DatanodeRegistration)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DatanodeDescriptor:updateRegInfo(DatanodeRegistration)\n  update ip/port/version\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DatanodeDescriptor:updateRegInfo(DatanodeRegistration)\n  update ip/port/version\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```source code:*/\n  @Override\n  public void updateRegInfo(DatanodeID nodeReg) {\n    super.updateRegInfo(nodeReg);\n    \n    // must re-process IBR after re-registration\n    for(DatanodeStorageInfo storage : getStorageInfos()) {\n      if (storage.getStorageType() != StorageType.PROVIDED) {\n        storage.setBlockReportCount(0);\n      }\n    }\n    heartbeatedSinceRegistration = false;\n    forceRegistration = false;\n  }\n\n  /**\n   * @return balancer bandwidth in bytes per second for this datanode\n   */\n  public synchronized long getBalancerBandwidth() {\n    return this.bandwidth;\n  }\n\n  /**\n   * @param bandwidth balancer bandwidth in bytes per second for this datanode\n   */\n  public synchronized void setBalancerBandwidth(long bandwidth) {\n    this.bandwidth = bandwidth;\n  }\n\n  @Override\n  public String dumpDatanode() {\n    StringBuilder sb = new StringBuilder(super.dumpDatanode());\n    int repl = replicateBlocks.size();\n    if (repl > 0) {\n      sb.append(\" \").append(repl).append(\" blocks to be replicated;\");\n    }\n    int ec = erasurecodeBlocks.size();\n    if(ec > 0) {\n      sb.append(\" \").append(ec).append(\" blocks to be erasure coded;\");\n    }\n    int inval = invalidateBlocks.size();\n    if (inval > 0) {\n      sb.append(\" \").append(inval).append(\" blocks to be invalidated;\");      \n    }\n    int recover = recoverBlocks.size();\n    if (recover > 0) {\n      sb.append(\" \").append(recover).append(\" blocks to be recovered;\");\n    }\n    return sb.toString();\n  }\n\n  DatanodeStorageInfo updateStorage(DatanodeStorage s) {\n    synchronized (storageMap) {\n      DatanodeStorageInfo storage = storageMap.get(s.getStorageID());\n      DFSTopologyNodeImpl parent = null;\n      if (getParent() instanceof DFSTopologyNodeImpl) {\n        parent = (DFSTopologyNodeImpl) getParent();\n      }\n\n      if (storage == null) {\n        LOG.info(\"Adding new storage ID {} for DN {}\", s.getStorageID(),\n            getXferAddr());"}
{"mp_id": "hdfs::DM::registerDatanode->addDatanode", "caller": "DatanodeManager:registerDatanode(DatanodeRegistration)", "callee": "HeartbeatManager:addDatanode(DatanodeDescriptor)", "gt": "valid", "stratum": "complex", "infeasible_reason": "", "rationale": "Genuinely new node (no existing node with same UUID).", "caller_code": "*    denied because resolving datanode network location fails.\n   */\n  public void registerDatanode(DatanodeRegistration nodeReg)\n      throws DisallowedDatanodeException, UnresolvedTopologyException {\n    InetAddress dnAddress = Server.getRemoteIp();\n    if (dnAddress != null) {\n      // Mostly called inside an RPC, update ip and peer hostname\n      String hostname = dnAddress.getHostName();\n      String ip = dnAddress.getHostAddress();\n      if (checkIpHostnameInRegistration && !isNameResolved(dnAddress)) {\n        // Reject registration of unresolved datanode to prevent performance\n        // impact of repetitive DNS lookups later.\n        final String message = \"hostname cannot be resolved (ip=\"\n            + ip + \", hostname=\" + hostname + \")\";\n        LOG.warn(\"Unresolved datanode registration: \" + message);\n        throw new DisallowedDatanodeException(nodeReg, message);\n      }\n      // update node registration with the ip and hostname from rpc request\n      nodeReg.setIpAddr(ip);\n      nodeReg.setPeerHostName(hostname);\n    }\n    \n    try {\n      nodeReg.setExportedKeys(blockManager.getBlockKeys());\n  \n      // Checks if the node is not on the hosts list.  If it is not, then\n      // it will be disallowed from registering. \n      if (!hostConfigManager.isIncluded(nodeReg)) {\n        throw new DisallowedDatanodeException(nodeReg);\n      }\n        \n      NameNode.stateChangeLog.info(\"BLOCK* registerDatanode: from \"\n          + nodeReg + \" storage \" + nodeReg.getDatanodeUuid());\n  \n      DatanodeDescriptor nodeS = getDatanode(nodeReg.getDatanodeUuid());\n      DatanodeDescriptor nodeN = host2DatanodeMap.getDatanodeByXferAddr(\n          nodeReg.getIpAddr(), nodeReg.getXferPort());\n        \n      // Non-change. REMOVE. Number 2\n      if (nodeN != null && nodeN != nodeS) {\n        NameNode.LOG.info(\"BLOCK* registerDatanode: \" + nodeN);\n        // nodeN previously served a different data storage, \n        // which is not served by anybody anymore.\n        removeDatanode(nodeN);\n        // physically remove node from datanodeMap\n        wipeDatanode(nodeN);\n        nodeN = null;\n      }\n  \n      if (nodeS != null) {\n        if (nodeN == nodeS) {\n          // The same datanode has been just restarted to serve the same data \n          // storage. We do not need to remove old data blocks, the delta will\n          // be calculated on the next block report from the datanode\n          if(NameNode.stateChangeLog.isDebugEnabled()) {\n            NameNode.stateChangeLog.debug(\"BLOCK* registerDatanode: \"\n                + \"node restarted.\");\n          }\n        } else {\n          // nodeS is found\n          /* The registering datanode is a replacement node for the existing\n            data storage, which from now on will be served by a new node.", "callee_code": "}\n\n  synchronized void addDatanode(final DatanodeDescriptor d) {\n    // update in-service node count\n    datanodes.add(d);\n    d.setAlive(true);\n  }\n\n  void updateDnStat(final DatanodeDescriptor d){\n    stats.add(d);\n  }\n\n  synchronized void removeDatanode(DatanodeDescriptor node) {\n    if (node.isAlive()) {\n      stats.subtract(node);\n      datanodes.remove(node);\n      removeNodeFromStaleList(node);\n      node.setAlive(false);\n    }\n  }\n\n  synchronized void updateHeartbeat(final DatanodeDescriptor node,\n      StorageReport[] reports, long cacheCapacity, long cacheUsed,\n      int xceiverCount, int failedVolumes,\n      VolumeFailureSummary volumeFailureSummary) {\n    stats.subtract(node);\n    blockManager.updateHeartbeat(node, reports, cacheCapacity, cacheUsed,\n        xceiverCount, failedVolumes, volumeFailureSummary);\n    stats.add(node);\n  }\n\n  synchronized void updateLifeline(final DatanodeDescriptor node,\n      StorageReport[] reports, long cacheCapacity, long cacheUsed,\n      int xceiverCount, int failedVolumes,\n      VolumeFailureSummary volumeFailureSummary) {\n    stats.subtract(node);\n    // This intentionally calls updateHeartbeatState instead of\n    // updateHeartbeat, because we don't want to modify the\n    // heartbeatedSinceRegistration flag.  Arrival of a lifeline message does\n    // not count as arrival of the first heartbeat.\n    blockManager.updateHeartbeatState(node, reports, cacheCapacity, cacheUsed,\n        xceiverCount, failedVolumes, volumeFailureSummary);\n    stats.add(node);\n  }\n\n  synchronized void startDecommission(final DatanodeDescriptor node) {\n    if (!node.isAlive()) {\n      LOG.info(\"Dead node {} is decommissioned immediately.\", node);\n      node.setDecommissioned();\n    } else {\n      stats.subtract(node);\n      node.startDecommission();\n      stats.add(node);\n    }\n  }\n\n  synchronized void startMaintenance(final DatanodeDescriptor node) {\n    if (!node.isAlive()) {\n      LOG.info(\"Dead node {} is put in maintenance state immediately.\", node);\n      node.setInMaintenance();\n    } else {\n      stats.subtract(node);", "caller_log_raw_stripped": "[ENTRY] DatanodeManager:registerDatanode(...)\n  IF: nodeS==null (no existing node with same UUID)\n  CALL: heartbeatManager.addDatanode(nodeDescr)", "callee_log_raw_stripped": "[ENTRY] HeartbeatManager:addDatanode(DatanodeDescriptor)\n  insert new node\n  EXIT", "caller_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DatanodeManager:registerDatanode(...)\n  IF: nodeS==null (no existing node with same UUID)\n  CALL: heartbeatManager.addDatanode(nodeDescr)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DatanodeManager:registerDatanode(...)\n  IF: nodeS==null (no existing node with same UUID)\n  CALL: heartbeatManager.addDatanode(nodeDescr)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "callee_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] HeartbeatManager:addDatanode(DatanodeDescriptor)\n  insert new node\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] HeartbeatManager:addDatanode(DatanodeDescriptor)\n  insert new node\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "parent_info": "node name is DatanodeManager:registerDatanode(DatanodeRegistration)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DatanodeManager:registerDatanode(...)\n  IF: nodeS==null (no existing node with same UUID)\n  CALL: heartbeatManager.addDatanode(nodeDescr)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DatanodeManager:registerDatanode(...)\n  IF: nodeS==null (no existing node with same UUID)\n  CALL: heartbeatManager.addDatanode(nodeDescr)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```souce code:*    denied because resolving datanode network location fails.\n   */\n  public void registerDatanode(DatanodeRegistration nodeReg)\n      throws DisallowedDatanodeException, UnresolvedTopologyException {\n    InetAddress dnAddress = Server.getRemoteIp();\n    if (dnAddress != null) {\n      // Mostly called inside an RPC, update ip and peer hostname\n      String hostname = dnAddress.getHostName();\n      String ip = dnAddress.getHostAddress();\n      if (checkIpHostnameInRegistration && !isNameResolved(dnAddress)) {\n        // Reject registration of unresolved datanode to prevent performance\n        // impact of repetitive DNS lookups later.\n        final String message = \"hostname cannot be resolved (ip=\"\n            + ip + \", hostname=\" + hostname + \")\";\n        LOG.warn(\"Unresolved datanode registration: \" + message);\n        throw new DisallowedDatanodeException(nodeReg, message);\n      }\n      // update node registration with the ip and hostname from rpc request\n      nodeReg.setIpAddr(ip);\n      nodeReg.setPeerHostName(hostname);\n    }\n    \n    try {\n      nodeReg.setExportedKeys(blockManager.getBlockKeys());\n  \n      // Checks if the node is not on the hosts list.  If it is not, then\n      // it will be disallowed from registering. \n      if (!hostConfigManager.isIncluded(nodeReg)) {\n        throw new DisallowedDatanodeException(nodeReg);\n      }\n        \n      NameNode.stateChangeLog.info(\"BLOCK* registerDatanode: from \"\n          + nodeReg + \" storage \" + nodeReg.getDatanodeUuid());\n  \n      DatanodeDescriptor nodeS = getDatanode(nodeReg.getDatanodeUuid());\n      DatanodeDescriptor nodeN = host2DatanodeMap.getDatanodeByXferAddr(\n          nodeReg.getIpAddr(), nodeReg.getXferPort());\n        \n      // Non-change. REMOVE. Number 2\n      if (nodeN != null && nodeN != nodeS) {\n        NameNode.LOG.info(\"BLOCK* registerDatanode: \" + nodeN);\n        // nodeN previously served a different data storage, \n        // which is not served by anybody anymore.\n        removeDatanode(nodeN);\n        // physically remove node from datanodeMap\n        wipeDatanode(nodeN);\n        nodeN = null;\n      }\n  \n      if (nodeS != null) {\n        if (nodeN == nodeS) {\n          // The same datanode has been just restarted to serve the same data \n          // storage. We do not need to remove old data blocks, the delta will\n          // be calculated on the next block report from the datanode\n          if(NameNode.stateChangeLog.isDebugEnabled()) {\n            NameNode.stateChangeLog.debug(\"BLOCK* registerDatanode: \"\n                + \"node restarted.\");\n          }\n        } else {\n          // nodeS is found\n          /* The registering datanode is a replacement node for the existing\n            data storage, which from now on will be served by a new node.", "child_info": "node name isHeartbeatManager:addDatanode(DatanodeDescriptor)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] HeartbeatManager:addDatanode(DatanodeDescriptor)\n  insert new node\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] HeartbeatManager:addDatanode(DatanodeDescriptor)\n  insert new node\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```source code:}\n\n  synchronized void addDatanode(final DatanodeDescriptor d) {\n    // update in-service node count\n    datanodes.add(d);\n    d.setAlive(true);\n  }\n\n  void updateDnStat(final DatanodeDescriptor d){\n    stats.add(d);\n  }\n\n  synchronized void removeDatanode(DatanodeDescriptor node) {\n    if (node.isAlive()) {\n      stats.subtract(node);\n      datanodes.remove(node);\n      removeNodeFromStaleList(node);\n      node.setAlive(false);\n    }\n  }\n\n  synchronized void updateHeartbeat(final DatanodeDescriptor node,\n      StorageReport[] reports, long cacheCapacity, long cacheUsed,\n      int xceiverCount, int failedVolumes,\n      VolumeFailureSummary volumeFailureSummary) {\n    stats.subtract(node);\n    blockManager.updateHeartbeat(node, reports, cacheCapacity, cacheUsed,\n        xceiverCount, failedVolumes, volumeFailureSummary);\n    stats.add(node);\n  }\n\n  synchronized void updateLifeline(final DatanodeDescriptor node,\n      StorageReport[] reports, long cacheCapacity, long cacheUsed,\n      int xceiverCount, int failedVolumes,\n      VolumeFailureSummary volumeFailureSummary) {\n    stats.subtract(node);\n    // This intentionally calls updateHeartbeatState instead of\n    // updateHeartbeat, because we don't want to modify the\n    // heartbeatedSinceRegistration flag.  Arrival of a lifeline message does\n    // not count as arrival of the first heartbeat.\n    blockManager.updateHeartbeatState(node, reports, cacheCapacity, cacheUsed,\n        xceiverCount, failedVolumes, volumeFailureSummary);\n    stats.add(node);\n  }\n\n  synchronized void startDecommission(final DatanodeDescriptor node) {\n    if (!node.isAlive()) {\n      LOG.info(\"Dead node {} is decommissioned immediately.\", node);\n      node.setDecommissioned();\n    } else {\n      stats.subtract(node);\n      node.startDecommission();\n      stats.add(node);\n    }\n  }\n\n  synchronized void startMaintenance(final DatanodeDescriptor node) {\n    if (!node.isAlive()) {\n      LOG.info(\"Dead node {} is put in maintenance state immediately.\", node);\n      node.setInMaintenance();\n    } else {\n      stats.subtract(node);"}
{"mp_id": "hdfs::DS::writeTo->createBlockOutputStream", "caller": "DataStreamer:writeTo(DataOutputStream)", "callee": "DataStreamer:createBlockOutputStream(List,int,long)", "gt": "valid", "stratum": "simple", "infeasible_reason": "", "rationale": "writeTo triggers pipeline re-creation on write failure.", "caller_code": "", "callee_code": "", "caller_log_raw_stripped": "[ENTRY] DataStreamer:writeTo()\n  IF: write fails (IOException)\n  CALL: createBlockOutputStream(nodes,...)", "callee_log_raw_stripped": "[ENTRY] DataStreamer:createBlockOutputStream(List,int,long)\n  connect to datanodes\n  IF: fail -> setInternalError()\n  EXIT", "caller_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:writeTo()\n  IF: write fails (IOException)\n  CALL: createBlockOutputStream(nodes,...)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:writeTo()\n  IF: write fails (IOException)\n  CALL: createBlockOutputStream(nodes,...)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "callee_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:createBlockOutputStream(List,int,long)\n  connect to datanodes\n  IF: fail -> setInternalError()\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:createBlockOutputStream(List,int,long)\n  connect to datanodes\n  IF: fail -> setInternalError()\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "parent_info": "node name is DataStreamer:writeTo(DataOutputStream)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:writeTo()\n  IF: write fails (IOException)\n  CALL: createBlockOutputStream(nodes,...)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:writeTo()\n  IF: write fails (IOException)\n  CALL: createBlockOutputStream(nodes,...)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```souce code:", "child_info": "node name isDataStreamer:createBlockOutputStream(List,int,long)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:createBlockOutputStream(List,int,long)\n  connect to datanodes\n  IF: fail -> setInternalError()\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:createBlockOutputStream(List,int,long)\n  connect to datanodes\n  IF: fail -> setInternalError()\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```source code:"}
{"mp_id": "hdfs::DS::run->backOffIfNecessary", "caller": "DataStreamer:run()", "callee": "DataStreamer:backOffIfNecessary()", "gt": "valid", "stratum": "simple", "infeasible_reason": "", "rationale": "Between retry attempts, run() calls backOffIfNecessary for exponential backoff.", "caller_code": "", "callee_code": "", "caller_log_raw_stripped": "[ENTRY] DataStreamer:run()\n  IF: errorState.hasError() && !errorState.isRestartingNode()\n  CALL: backOffIfNecessary()", "callee_log_raw_stripped": "[ENTRY] DataStreamer:backOffIfNecessary()\n  sleep for retry backoff\n  EXIT", "caller_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:run()\n  IF: errorState.hasError() && !errorState.isRestartingNode()\n  CALL: backOffIfNecessary()\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:run()\n  IF: errorState.hasError() && !errorState.isRestartingNode()\n  CALL: backOffIfNecessary()\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "callee_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:backOffIfNecessary()\n  sleep for retry backoff\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:backOffIfNecessary()\n  sleep for retry backoff\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "parent_info": "node name is DataStreamer:run()node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:run()\n  IF: errorState.hasError() && !errorState.isRestartingNode()\n  CALL: backOffIfNecessary()\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:run()\n  IF: errorState.hasError() && !errorState.isRestartingNode()\n  CALL: backOffIfNecessary()\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```souce code:", "child_info": "node name isDataStreamer:backOffIfNecessary()node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:backOffIfNecessary()\n  sleep for retry backoff\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:backOffIfNecessary()\n  sleep for retry backoff\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```source code:"}
{"mp_id": "hdfs::DS::run->waitForAllAcks", "caller": "DataStreamer:run()", "callee": "DataStreamer:waitForAllAcks()", "gt": "valid", "stratum": "simple", "infeasible_reason": "", "rationale": "After sending the last packet, blocks on waitForAllAcks.", "caller_code": "", "callee_code": "", "caller_log_raw_stripped": "[ENTRY] DataStreamer:run()\n  IF: one.isLastPacketInBlock() && packet sent\n  CALL: waitForAllAcks()", "callee_log_raw_stripped": "[ENTRY] DataStreamer:waitForAllAcks()\n  wait until all acks received or timeout\n  EXIT", "caller_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:run()\n  IF: one.isLastPacketInBlock() && packet sent\n  CALL: waitForAllAcks()\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:run()\n  IF: one.isLastPacketInBlock() && packet sent\n  CALL: waitForAllAcks()\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "callee_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:waitForAllAcks()\n  wait until all acks received or timeout\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:waitForAllAcks()\n  wait until all acks received or timeout\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "parent_info": "node name is DataStreamer:run()node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:run()\n  IF: one.isLastPacketInBlock() && packet sent\n  CALL: waitForAllAcks()\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:run()\n  IF: one.isLastPacketInBlock() && packet sent\n  CALL: waitForAllAcks()\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```souce code:", "child_info": "node name isDataStreamer:waitForAllAcks()node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:waitForAllAcks()\n  wait until all acks received or timeout\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:waitForAllAcks()\n  wait until all acks received or timeout\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```source code:"}
{"mp_id": "hdfs::DS::createBlockOutputStream->checkRestartingNode", "caller": "DataStreamer:createBlockOutputStream(List,int,long)", "callee": "DataStreamer$ErrorState:checkRestartingNode()", "gt": "valid", "stratum": "simple", "infeasible_reason": "", "rationale": "Checks restart state at entry; if restarting, returns without creating stream.", "caller_code": "", "callee_code": "", "caller_log_raw_stripped": "[ENTRY] DataStreamer:createBlockOutputStream(...)\n  IF: errorState.checkRestartingNode() == true\n  CALL: checkRestartingNode()", "callee_log_raw_stripped": "[ENTRY] ErrorState:checkRestartingNode()\n  IF: isRestartingNode() -> reset + return true\n  ELSE: return false\n  EXIT", "caller_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:createBlockOutputStream(...)\n  IF: errorState.checkRestartingNode() == true\n  CALL: checkRestartingNode()\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:createBlockOutputStream(...)\n  IF: errorState.checkRestartingNode() == true\n  CALL: checkRestartingNode()\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "callee_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] ErrorState:checkRestartingNode()\n  IF: isRestartingNode() -> reset + return true\n  ELSE: return false\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] ErrorState:checkRestartingNode()\n  IF: isRestartingNode() -> reset + return true\n  ELSE: return false\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "parent_info": "node name is DataStreamer:createBlockOutputStream(List,int,long)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:createBlockOutputStream(...)\n  IF: errorState.checkRestartingNode() == true\n  CALL: checkRestartingNode()\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:createBlockOutputStream(...)\n  IF: errorState.checkRestartingNode() == true\n  CALL: checkRestartingNode()\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```souce code:", "child_info": "node name isDataStreamer$ErrorState:checkRestartingNode()node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] ErrorState:checkRestartingNode()\n  IF: isRestartingNode() -> reset + return true\n  ELSE: return false\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] ErrorState:checkRestartingNode()\n  IF: isRestartingNode() -> reset + return true\n  ELSE: return false\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```source code:"}
{"mp_id": "hdfs::DS::cbo->getXferAddr", "caller": "DataStreamer:createBlockOutputStream(List,int,long)", "callee": "DatanodeInfo:getXferAddr()", "gt": "valid", "stratum": "simple", "infeasible_reason": "", "rationale": "Reads datanode transfer addresses to establish socket connections.", "caller_code": "", "callee_code": "", "caller_log_raw_stripped": "[ENTRY] DataStreamer:createBlockOutputStream(...)\n  FOR each node in pipeline:\n  CALL: node.getXferAddr()", "callee_log_raw_stripped": "[ENTRY] DatanodeInfo:getXferAddr()\n  return transfer address string\n  EXIT", "caller_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:createBlockOutputStream(...)\n  FOR each node in pipeline:\n  CALL: node.getXferAddr()\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:createBlockOutputStream(...)\n  FOR each node in pipeline:\n  CALL: node.getXferAddr()\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "callee_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DatanodeInfo:getXferAddr()\n  return transfer address string\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DatanodeInfo:getXferAddr()\n  return transfer address string\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "parent_info": "node name is DataStreamer:createBlockOutputStream(List,int,long)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:createBlockOutputStream(...)\n  FOR each node in pipeline:\n  CALL: node.getXferAddr()\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:createBlockOutputStream(...)\n  FOR each node in pipeline:\n  CALL: node.getXferAddr()\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```souce code:", "child_info": "node name isDatanodeInfo:getXferAddr()node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DatanodeInfo:getXferAddr()\n  return transfer address string\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DatanodeInfo:getXferAddr()\n  return transfer address string\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```source code:"}
{"mp_id": "hdfs::DS::pdoe->initDataStreaming-INFEASIBLE", "caller": "DataStreamer:processDatanodeOrExternalError()", "callee": "DataStreamer:initDataStreaming()", "gt": "infeasible", "stratum": "simple", "infeasible_reason": "shouldHandleExternalError()==false", "rationale": "", "caller_code": "", "callee_code": "", "caller_log_raw_stripped": "[ENTRY] DataStreamer:processDatanodeOrExternalError()\n  IF: shouldHandleExternalError()==false \n  CALL: initDataStreaming()", "callee_log_raw_stripped": "[ENTRY] DataStreamer:initDataStreaming()\n  EXIT", "caller_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:processDatanodeOrExternalError()\n  IF: shouldHandleExternalError()==false \n  CALL: initDataStreaming()\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:processDatanodeOrExternalError()\n  IF: shouldHandleExternalError()==false \n  CALL: initDataStreaming()\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "callee_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:initDataStreaming()\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:initDataStreaming()\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "parent_info": "node name is DataStreamer:processDatanodeOrExternalError()node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:processDatanodeOrExternalError()\n  IF: shouldHandleExternalError()==false \n  CALL: initDataStreaming()\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:processDatanodeOrExternalError()\n  IF: shouldHandleExternalError()==false \n  CALL: initDataStreaming()\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```souce code:", "child_info": "node name isDataStreamer:initDataStreaming()node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:initDataStreaming()\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:initDataStreaming()\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```source code:"}
{"mp_id": "hdfs::DS::run->endBlock-INFEASIBLE", "caller": "DataStreamer:run()", "callee": "DataStreamer:endBlock()", "gt": "infeasible", "stratum": "simple", "infeasible_reason": "stage already PIPELINE_CLOSE; second endBlock call unreachable", "rationale": "", "caller_code": "", "callee_code": "", "caller_log_raw_stripped": "[ENTRY] DataStreamer:run()\n  stage == PIPELINE_CLOSE \n  CALL: endBlock()", "callee_log_raw_stripped": "[ENTRY] DataStreamer:endBlock()\n  [INFO] Ending block", "caller_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:run()\n  stage == PIPELINE_CLOSE \n  CALL: endBlock()\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:run()\n  stage == PIPELINE_CLOSE \n  CALL: endBlock()\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "callee_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:endBlock()\n  [INFO] Ending block\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:endBlock()\n  [INFO] Ending block\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "parent_info": "node name is DataStreamer:run()node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:run()\n  stage == PIPELINE_CLOSE \n  CALL: endBlock()\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:run()\n  stage == PIPELINE_CLOSE \n  CALL: endBlock()\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```souce code:", "child_info": "node name isDataStreamer:endBlock()node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:endBlock()\n  [INFO] Ending block\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:endBlock()\n  [INFO] Ending block\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```source code:"}
{"mp_id": "hdfs::BM::removeStoredBlock->DSI::removeBlock-INFEASIBLE", "caller": "BlockManager:removeStoredBlock(BlockInfo,DatanodeDescriptor)", "callee": "DatanodeStorageInfo:removeBlock(BlockInfo)", "gt": "infeasible", "stratum": "simple", "infeasible_reason": "findStorageInfo returns null — storageInfo already removed", "rationale": "", "caller_code": "", "callee_code": "", "caller_log_raw_stripped": "[ENTRY] BlockManager:removeStoredBlock(...)\n  findStorageInfo(node) -> null \n  CALL: storageInfo.removeBlock(block)", "callee_log_raw_stripped": "[ENTRY] DatanodeStorageInfo:removeBlock(BlockInfo)", "caller_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] BlockManager:removeStoredBlock(...)\n  findStorageInfo(node) -> null \n  CALL: storageInfo.removeBlock(block)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] BlockManager:removeStoredBlock(...)\n  findStorageInfo(node) -> null \n  CALL: storageInfo.removeBlock(block)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "callee_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DatanodeStorageInfo:removeBlock(BlockInfo)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DatanodeStorageInfo:removeBlock(BlockInfo)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "parent_info": "node name is BlockManager:removeStoredBlock(BlockInfo,DatanodeDescriptor)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] BlockManager:removeStoredBlock(...)\n  findStorageInfo(node) -> null \n  CALL: storageInfo.removeBlock(block)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] BlockManager:removeStoredBlock(...)\n  findStorageInfo(node) -> null \n  CALL: storageInfo.removeBlock(block)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```souce code:", "child_info": "node name isDatanodeStorageInfo:removeBlock(BlockInfo)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DatanodeStorageInfo:removeBlock(BlockInfo)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DatanodeStorageInfo:removeBlock(BlockInfo)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```source code:"}
{"mp_id": "hdfs::DS::run->processDatanodeOrExternalError-INFEASIBLE-2", "caller": "DataStreamer:run()", "callee": "DataStreamer:processDatanodeOrExternalError()", "gt": "infeasible", "stratum": "simple", "infeasible_reason": "stage==PIPELINE_CLOSE; error recovery only applies during DATA_STREAMING", "rationale": "", "caller_code": "", "callee_code": "", "caller_log_raw_stripped": "[ENTRY] DataStreamer:run()\n  stage==PIPELINE_CLOSE \n  CALL: processDatanodeOrExternalError()", "callee_log_raw_stripped": "[ENTRY] DataStreamer:processDatanodeOrExternalError()", "caller_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:run()\n  stage==PIPELINE_CLOSE \n  CALL: processDatanodeOrExternalError()\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:run()\n  stage==PIPELINE_CLOSE \n  CALL: processDatanodeOrExternalError()\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "callee_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:processDatanodeOrExternalError()\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:processDatanodeOrExternalError()\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "parent_info": "node name is DataStreamer:run()node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:run()\n  stage==PIPELINE_CLOSE \n  CALL: processDatanodeOrExternalError()\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:run()\n  stage==PIPELINE_CLOSE \n  CALL: processDatanodeOrExternalError()\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```souce code:", "child_info": "node name isDataStreamer:processDatanodeOrExternalError()node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DataStreamer:processDatanodeOrExternalError()\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DataStreamer:processDatanodeOrExternalError()\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```source code:"}
{"mp_id": "hdfs::BM::processReport->invalidateBlocks-INFEASIBLE", "caller": "BlockManager:processReport(...)", "callee": "BlockManager:invalidateBlocks(int)", "gt": "infeasible", "stratum": "simple", "infeasible_reason": "invalidateBlocks list is empty; method skipped", "rationale": "", "caller_code": "", "callee_code": "", "caller_log_raw_stripped": "[ENTRY] BlockManager:processReport(...)\n  invalidateBlocks is empty \n  CALL: invalidateBlocks(...)", "callee_log_raw_stripped": "[ENTRY] BlockManager:invalidateBlocks(int)", "caller_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] BlockManager:processReport(...)\n  invalidateBlocks is empty \n  CALL: invalidateBlocks(...)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] BlockManager:processReport(...)\n  invalidateBlocks is empty \n  CALL: invalidateBlocks(...)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "callee_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] BlockManager:invalidateBlocks(int)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] BlockManager:invalidateBlocks(int)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "parent_info": "node name is BlockManager:processReport(...)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] BlockManager:processReport(...)\n  invalidateBlocks is empty \n  CALL: invalidateBlocks(...)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] BlockManager:processReport(...)\n  invalidateBlocks is empty \n  CALL: invalidateBlocks(...)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```souce code:", "child_info": "node name isBlockManager:invalidateBlocks(int)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] BlockManager:invalidateBlocks(int)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] BlockManager:invalidateBlocks(int)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```source code:"}
{"mp_id": "hdfs::BM::addStoredBlock->countNodes", "caller": "BlockManager:addStoredBlock(BlockInfo,DatanodeStorageInfo,DatanodeDescriptor,boolean)", "callee": "BlockManager:countNodes(BlockInfo)", "gt": "valid", "stratum": "complex", "infeasible_reason": "", "rationale": "Counts live/decommissioning/maintenance replica counts for safe mode and redundancy decisions.", "caller_code": "", "callee_code": "", "caller_log_raw_stripped": "[ENTRY] BlockManager:addStoredBlock(...)\n  IF: block added successfully\n  CALL: countNodes(storedBlock)", "callee_log_raw_stripped": "[ENTRY] BlockManager:countNodes(BlockInfo)\n  count live/decommissioning/corrupt replicas per storage\n  return NumberReplicas\n  EXIT", "caller_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] BlockManager:addStoredBlock(...)\n  IF: block added successfully\n  CALL: countNodes(storedBlock)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] BlockManager:addStoredBlock(...)\n  IF: block added successfully\n  CALL: countNodes(storedBlock)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "callee_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] BlockManager:countNodes(BlockInfo)\n  count live/decommissioning/corrupt replicas per storage\n  return NumberReplicas\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] BlockManager:countNodes(BlockInfo)\n  count live/decommissioning/corrupt replicas per storage\n  return NumberReplicas\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "parent_info": "node name is BlockManager:addStoredBlock(BlockInfo,DatanodeStorageInfo,DatanodeDescriptor,boolean)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] BlockManager:addStoredBlock(...)\n  IF: block added successfully\n  CALL: countNodes(storedBlock)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] BlockManager:addStoredBlock(...)\n  IF: block added successfully\n  CALL: countNodes(storedBlock)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```souce code:", "child_info": "node name isBlockManager:countNodes(BlockInfo)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] BlockManager:countNodes(BlockInfo)\n  count live/decommissioning/corrupt replicas per storage\n  return NumberReplicas\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] BlockManager:countNodes(BlockInfo)\n  count live/decommissioning/corrupt replicas per storage\n  return NumberReplicas\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```source code:"}
{"mp_id": "hdfs::BM::processReport->removeFromInvalidates", "caller": "BlockManager:processReport(...)", "callee": "InvalidateBlocks:remove(DatanodeDescriptor,Block)", "gt": "valid", "stratum": "complex", "infeasible_reason": "", "rationale": "Blocks pending invalidation are removed from the queue after processing.", "caller_code": "", "callee_code": "", "caller_log_raw_stripped": "[ENTRY] BlockManager:processReport(...)\n  FOR each block in diff:\n  CALL: invalidateBlocks.remove(node, block)", "callee_log_raw_stripped": "[ENTRY] InvalidateBlocks:remove(DatanodeDescriptor,Block)\n  remove from queue\n  EXIT", "caller_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] BlockManager:processReport(...)\n  FOR each block in diff:\n  CALL: invalidateBlocks.remove(node, block)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] BlockManager:processReport(...)\n  FOR each block in diff:\n  CALL: invalidateBlocks.remove(node, block)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "callee_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] InvalidateBlocks:remove(DatanodeDescriptor,Block)\n  remove from queue\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] InvalidateBlocks:remove(DatanodeDescriptor,Block)\n  remove from queue\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "parent_info": "node name is BlockManager:processReport(...)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] BlockManager:processReport(...)\n  FOR each block in diff:\n  CALL: invalidateBlocks.remove(node, block)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] BlockManager:processReport(...)\n  FOR each block in diff:\n  CALL: invalidateBlocks.remove(node, block)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```souce code:", "child_info": "node name isInvalidateBlocks:remove(DatanodeDescriptor,Block)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] InvalidateBlocks:remove(DatanodeDescriptor,Block)\n  remove from queue\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] InvalidateBlocks:remove(DatanodeDescriptor,Block)\n  remove from queue\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```source code:"}
{"mp_id": "hdfs::DM::registerDatanode->setNetworkLocation", "caller": "DatanodeManager:registerDatanode(DatanodeRegistration)", "callee": "DatanodeDescriptor:setNetworkLocation(String)", "gt": "valid", "stratum": "complex", "infeasible_reason": "", "rationale": "Resolves and sets rack location via DNS-to-switch mapping.", "caller_code": "", "callee_code": "", "caller_log_raw_stripped": "[ENTRY] DatanodeManager:registerDatanode(...)\n  loc = resolveNetworkLocation(nodeS)\n  CALL: nodeS.setNetworkLocation(loc)", "callee_log_raw_stripped": "[ENTRY] DatanodeDescriptor:setNetworkLocation(String)\n  set rack location for topology-aware scheduling\n  EXIT", "caller_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DatanodeManager:registerDatanode(...)\n  loc = resolveNetworkLocation(nodeS)\n  CALL: nodeS.setNetworkLocation(loc)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DatanodeManager:registerDatanode(...)\n  loc = resolveNetworkLocation(nodeS)\n  CALL: nodeS.setNetworkLocation(loc)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "callee_log_xml": "```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DatanodeDescriptor:setNetworkLocation(String)\n  set rack location for topology-aware scheduling\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DatanodeDescriptor:setNetworkLocation(String)\n  set rack location for topology-aware scheduling\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```", "parent_info": "node name is DatanodeManager:registerDatanode(DatanodeRegistration)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>P1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DatanodeManager:registerDatanode(...)\n  loc = resolveNetworkLocation(nodeS)\n  CALL: nodeS.setNetworkLocation(loc)\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DatanodeManager:registerDatanode(...)\n  loc = resolveNetworkLocation(nodeS)\n  CALL: nodeS.setNetworkLocation(loc)\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```souce code:", "child_info": "node name isDatanodeDescriptor:setNetworkLocation(String)node log is```xml\n<merge_result>\n  <valid_paths>\n    <path>\n      <id>C1</id>\n      <eval>true</eval>\n      <exec_flow>\n[ENTRY] DatanodeDescriptor:setNetworkLocation(String)\n  set rack location for topology-aware scheduling\n  EXIT\n      </exec_flow>\n      <log_sequence>\n[ENTRY] DatanodeDescriptor:setNetworkLocation(String)\n  set rack location for topology-aware scheduling\n  EXIT\n      </log_sequence>\n    </path>\n  </valid_paths>\n  <wrong_path></wrong_path>\n</merge_result>\n```source code:"}
